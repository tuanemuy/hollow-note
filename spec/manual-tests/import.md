# 取り込み テスト

## 概要

このドキュメントは、ファイルのアップロードと HTML への変換に関するマニュアルテストの手順書です。

対応シナリオ: `spec/scenario/import.md`（IM-01〜IM-06）

## 前提条件

### 環境

- ユーザー A（`user-a@example.com`）でサインインできる
- OpenRouter を連携済みの状態と未連携の状態を切り替えられる
- ユーザー A が editor として参加するワークスペース `設計チーム` と、viewer として参加するワークスペース `閲覧のみ` がある
- ユーザー A の公開ハンドルを設定済みの状態と未設定の状態に切り替えられる
- テスト用の画像・CSS（`theme.css` と `url()` が指す `bg.png`）を外部の URL から配信でき、任意のタイミングで 404 を返す状態に切り替えられる

### テストデータ

| ファイル名 | 内容 | 用途 |
|---|---|---|
| `sample.html` | `style` 要素を含む装飾済み HTML | 装飾の保持を確認する |
| `plain.html` | 装飾のない素朴な HTML | 既定スタイルの適用を確認する |
| `sample.md` | 見出しとリストを含む Markdown | 機械変換を確認する |
| `sample.docx` | 見出しと段落を含む Word 文書 | テキスト抽出と構造化を確認する |
| `sample.txt` | 見出し風の行と段落を含むプレーンテキスト | テキスト抽出と構造化を確認する |
| `sample.xlsx` | 表を含むシートを 2 枚持つ Excel ブック | 表構造の抽出と構造化を確認する |
| `sample.pptx` | 箇条書きを含むスライドを 3 枚持つ PowerPoint 文書 | スライドのテキスト抽出と構造化を確認する |
| `text-pdf.pdf` | テキスト層のある PDF | 機械抽出を確認する |
| `scan-pdf.pdf` | テキスト層のないスキャン PDF | AI 解析を確認する |
| `locked.pdf` | パスワード保護された PDF | 異常系 |
| `photo.jpg` | 文字が写った画像 | AI でのテキスト抽出を確認する |
| `voice.m4a` | 1 分程度の音声 | 音声認識を確認する |
| `broken.docx` | 途中で切れた壊れたファイル | 異常系 |
| `archive.zip` | ZIP ファイル | 未対応形式 |
| `big-audio.m4a` | 201 MB の音声 | 境界値 |
| `edge-audio.m4a` | 200 MB ちょうどの音声 | 境界値 |
| `ref.html` | 外部の画像 URL を 3 件参照する HTML | 参照リソースの取り込みを確認する |
| `ref-201.html` | 外部の画像 URL を 201 件参照する HTML | 参照の件数上限を確認する |
| `ref-css.html` | 外部スタイルシート（`theme.css`）を `link rel=stylesheet` で 1 件参照する装飾済み HTML | 外部 CSS の埋め込みと装飾の保持を確認する |
| `theme.css` | `ref-css.html` から参照させる CSS。配色と余白の指定に加え、背景画像の `url(./bg.png)`、`position: fixed` の宣言、`@import url(...)` の規則を含む | 埋め込んだ CSS の制約と `url()` の扱いを確認する |
| `ref-css-404.html` | 404 を返す URL の外部スタイルシートを 1 件参照する装飾済み HTML | 取り込めなかったスタイルシートの扱いを確認する |
| `ref-css-201.html` | 外部の画像 URL 200 件と外部スタイルシート 1 件を参照する装飾済み HTML | 予算を使い切ったあとのスタイルシートの扱いを確認する |
| `edge-pdf.pdf` | 50 MB ちょうどの PDF | 境界値 |
| `over-pdf.pdf` | 51 MB の PDF | 境界値 |
| `bulk-100/` | 対応形式のファイル 100 件を含むフォルダ | 件数の境界値 |
| `bulk-101/` | 対応形式のファイル 101 件を含むフォルダ | 件数の境界値 |
| `bulk-501mb/` | 合計 501 MB になるファイル群 | 合計サイズの境界値 |

### 状態・変換方式の別名

繰り返し登場する状態と変換方式は次の別名で呼ぶ。画面上の表記の正典は `spec/design/` にある。

| 別名 | 意味 |
|---|---|
| 機械抽出 | AI を使わず機械的にテキストを抽出する変換方式 |
| AI 構造化 | 抽出したテキストに AI で構造を付ける変換方式 |
| AI ページ解析 | ページを画像として AI で読み取り構造化する変換方式 |
| 要 AI 連携 | AI が必要な変換を、連携がないため保留しているノートの状態 |
| 元装飾の表示 | 取り込んだ元の装飾だけで表示する表示スタイル |
| 既定スタイルの表示 | サービス既定のスタイルを適用する表示スタイル |

### 事前準備

1. ユーザー A でサインインし、個人の文脈でノート一覧を開く
2. 各テストの前に、OpenRouter の連携状態を指定された状態に揃える
3. 上限に達した状態を作るテスト（TC-24 / TC-25）のために、テスト環境の上限値を一時的に小さくしておく
   - 保存容量の上限を 10 MB に、当月の AI 実行回数の上限を 2 回に設定する
   - 設定できない環境では、上限まで実データを作るか、これらの TC を対象外とする
4. サイズの大きいテストデータは、必要な大きさになるまで内容を繰り返したファイルをスクリプトで生成する

---

## TC-01: Markdown ファイルをアップロードしてノートを作る

**種別**: 正常系
**目的**: IM-01 の基本フローを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | ノート一覧からアップロードの操作を選ぶ | アップロード画面（`/notes/upload`）に遷移する |
| 2 | `sample.md` を選ぶ | 一覧にファイル名・サイズ・判定された形式（Markdown）・変換方式が表示される |
| 3 | 取り込み先が個人、公開ステータスが非公開であることを確認する | 既定値としてそうなっていることが分かる |
| 4 | アップロードを開始する | 進捗が表示される |
| 5 | アップロード完了後の遷移を確認する | 作成されたノートの詳細に遷移し、変換が進行中であることと進捗が表示される |
| 6 | 変換の完了を待つ | 本文が表示され、見出しとリストが HTML として描画される |
| 7 | ノート一覧に戻る | そのノートが一覧の先頭に表示される |

## TC-02: 装飾済みの HTML をアップロードする

**種別**: 正常系
**目的**: IM-01 と ADR 007 の自動判定を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `sample.html` をアップロードする | ノートが作られ、本文が表示される |
| 2 | 表示を確認する | 元の装飾が保たれている |
| 3 | 表示スタイルの設定を開く | 元装飾の表示が選ばれており、自動判定である旨が分かる |
| 4 | `plain.html` をアップロードする | ノートが作られる |
| 5 | その表示スタイルの設定を開く | 既定スタイルの表示が選ばれている |

## TC-03: Word 文書を機械変換で取り込む

**種別**: 正常系
**目的**: AI 未連携時のテキスト抽出を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を未連携にする | 未連携であることが表示で確認できる |
| 2 | `sample.docx` をアップロードする | 変換方式が機械抽出であることが分かる |
| 3 | 完了後に本文を確認する | 段落が `p` 要素として並んだ素朴な HTML になっている |

## TC-04: Word 文書を AI で構造化して取り込む

**種別**: 正常系
**目的**: AI 連携時の構造化を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を連携済みにする | 連携済みであることが表示で確認できる |
| 2 | `sample.docx` をアップロードする | 変換方式が AI 構造化であることが分かる |
| 3 | 完了後に本文を確認する | 見出し・リストなどの構造が付いた HTML になっている |
| 4 | 使用量（`/settings/usage`）を確認する | 当月の AI 実行回数が 1 増えている |

## TC-05: テキスト層のない PDF を取り込む

**種別**: 正常系
**目的**: ページ画像経由の構造化を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を連携済みにする | — |
| 2 | `scan-pdf.pdf` をアップロードする | 変換方式が AI ページ解析であることが分かる |
| 3 | 完了後に本文を確認する | ページの内容がテキストとして構造化されている |

## TC-06: 画像からテキストを抽出する

**種別**: 正常系
**目的**: 画像の AI 変換を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を連携済みにする | — |
| 2 | `photo.jpg` をアップロードする | ノートが作られ、処理中になる |
| 3 | 完了後に本文を確認する | 画像内の文字がテキストとして抽出されている |

## TC-07: 音声を取り込む

**種別**: 正常系
**目的**: 音声認識と構造化を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を連携済みにする | — |
| 2 | `voice.m4a` をアップロードする | ノートが作られ、処理中になる |
| 3 | 完了後に本文を確認する | 音声の内容がテキストとして構造化されている |

## TC-08: 複数のファイルをまとめてアップロードする

**種別**: 正常系
**目的**: IM-02 の基本フローを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | アップロード画面で `sample.md` / `sample.docx` / `text-pdf.pdf` を一度に選ぶ | 3 件が一覧に並び、それぞれ形式と変換方式が表示される |
| 2 | 一覧から 1 件を取り除く | その行が消え、残り 2 件になる |
| 3 | 取り込み先と公開ステータスをまとめて指定する | 選択が全件に適用される |
| 4 | アップロードを開始する | ファイルごとに個別の進捗が表示される |
| 5 | 完了後の表示を確認する | 成功件数と失敗件数の内訳が表示される |
| 6 | ノート一覧を確認する | 2 件のノートが作られている |

## TC-09: フォルダをドロップして取り込む

**種別**: 正常系
**目的**: IM-02 のフォルダ受け入れを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 対応形式と未対応形式が混在するフォルダをアップロード画面にドロップする | 再帰的にファイルが取り出される |
| 2 | 一覧を確認する | 対応形式のみが並び、除外された件数が表示される |

## TC-10: 取り込み先にワークスペースを指定する

**種別**: 正常系
**目的**: IM-04 の基本フローを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 取り込み先セレクターを開く | 個人の取り込み先と `設計チーム` が並ぶ |
| 2 | 選択肢を確認する | viewer として参加している `閲覧のみ` は表示されない |
| 3 | `設計チーム` を選んで `sample.md` をアップロードする | ノートが `設計チーム` に作られる |
| 4 | `設計チーム` の文脈でノート一覧を開く | そのノートが表示される |
| 5 | 個人の文脈でノート一覧を開く | そのノートは表示されない |

## TC-11: 取り込み先の既定値を確認する

**種別**: 正常系
**目的**: IM-04 の既定値の規則を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `設計チーム` の文脈でノート一覧を開き、アップロード画面を開く | 取り込み先セレクターの既定値が `設計チーム` になっている |
| 2 | 個人の文脈からアップロード画面を開く | 既定値が個人になっている |

## TC-12: 参照されている外部ファイルを取り込む

**種別**: 正常系
**目的**: IM-05 の基本フローを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `ref.html` をアップロードする | ノートが作られる |
| 2 | 完了後にノート詳細を開く | 本文中の画像が表示される |
| 3 | 画像の参照先を確認する | 元の外部 URL ではなく、サービス内のストレージを指す URL になっている |
| 4 | 取り込んだリソースの一覧を確認する | 件数と一覧が表示される |

## TC-13: AI による構造化をやり直す

**種別**: 正常系
**目的**: IM-06 の基本フローを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | AI で生成済みのノートを開く | 本文が表示される |
| 2 | AI での作り直しを開始する | モデルと追加指示の入力欄が表示される |
| 3 | 追加指示に `見出しを章立てにして` と入力する | 入力値が表示される |
| 4 | 現在の本文が置き換わることを伝える警告を確認して実行する | 変換が進行中の表示になる |
| 5 | 完了後に本文を確認する | 指示が反映された内容になっている |
| 6 | タグと公開ステータスを確認する | 変わっていない |
| 7 | 元に戻す操作を実行する | 直前の本文に戻る |

## TC-14: 取り込みの進捗と結果を確認する

**種別**: 正常系
**目的**: IM-03 の基本フローを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 複数ファイルのアップロードを開始する | 全体と個別の進捗が表示される |
| 2 | 別の画面へ移動する | 処理ティッカーに進行中の件数が表示される |
| 3 | ティッカーから処理履歴を開く | 実行中のジョブが一覧される |
| 4 | 完了を待つ | 完了の通知が表示される |
| 5 | `/jobs` を開く | 取り込みの結果が履歴として確認できる |
| 6 | ブラウザを閉じて開き直す | 処理は継続しており、最新の状態が表示される |

---

## TC-15: 未対応の形式をアップロードしようとする

**種別**: 異常系
**目的**: IM-01 の形式チェックを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | アップロード画面で `archive.zip` を選ぶ | 対応していない形式であることが理由として示され、対象から除外される |
| 2 | 対応形式のファイルも一緒に選ぶ | 対応形式のみが `accepted` として並び、ZIP は除外される |
| 3 | アップロードを開始する | 対応形式のファイルだけが取り込まれる |

## TC-16: ファイルサイズの上限の境界を確認する

**種別**: 境界値
**目的**: 形式別のサイズ上限を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `edge-audio.m4a`（200 MB ちょうど）を選ぶ | 受け付けられる |
| 2 | `big-audio.m4a`（201 MB）を選ぶ | 音声の上限 200 MB を超えていることが示され、除外される |
| 3 | `edge-pdf.pdf`（50 MB ちょうど）を選ぶ | 受け付けられる |
| 4 | `over-pdf.pdf`（51 MB）を選ぶ | PDF の上限 50 MB を超えていることが示される |

## TC-17: 一括アップロードの件数と合計サイズの境界を確認する

**種別**: 境界値
**目的**: IM-02 の上限を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `bulk-100/` の 100 件を選ぶ | 受け付けられる |
| 2 | `bulk-101/` の 101 件を選ぶ | 一度にアップロードできる 100 件の上限を超えていることが示される |
| 3 | `bulk-501mb/` を選ぶ | 合計 500 MB の上限を超えていることが示される |

## TC-18: 壊れたファイルをアップロードする

**種別**: 異常系
**目的**: IM-01 の変換失敗の扱いを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `broken.docx` をアップロードする | アップロードは成功し、ノートが作られる |
| 2 | 変換の完了を待つ | ノートが変換に失敗した状態になる |
| 3 | ノートを開く | ファイルの破損が失敗の理由として示され、再試行の導線が表示される |
| 4 | 再試行する | 同じ理由で再び失敗する |

## TC-19: パスワード保護された PDF をアップロードする

**種別**: 異常系
**目的**: IM-01 のパスワード保護の扱いを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `locked.pdf` をアップロードする | ノートが作られる |
| 2 | 結果を確認する | パスワード保護が原因で解除が必要なことが理由として示され、失敗として記録される |
| 3 | 変換ジョブの完了後にノートと処理履歴の理由を確認する | 取り込み直後と同じパスワード保護のままで、機械変換で取り込めない旨や AI 連携が必要という理由に入れ替わっていない |
| 4 | OpenRouter を連携した状態で同じファイルをアップロードする | 連携があっても理由はパスワード保護のままで、AI 実行回数も消費されない |
| 5 | AI を使わない機械変換を選んで同じファイルをアップロードする | 同じく理由はパスワード保護のままである |

## TC-20: 拡張子と内容が食い違うファイルをアップロードする

**種別**: 異常系
**目的**: IM-01 の内容優先の判定を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | PNG 画像の拡張子を `.md` に変えたファイルを選ぶ | 判定された形式が画像と表示される |
| 2 | アップロードする | 画像として扱われ、AI 連携の有無に応じた挙動になる |
| 3 | 判定できない内容のファイルを選ぶ | 対応していない形式であることが示される |

## TC-21: 一部が失敗しても他の取り込みが継続することを確認する

**種別**: 異常系
**目的**: IM-02 の部分失敗の扱いを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `sample.md` / `broken.docx` / `text-pdf.pdf` を一括でアップロードする | 3 件が登録される |
| 2 | 完了を待つ | 2 件が成功、1 件が失敗として内訳が表示される |
| 3 | 失敗した分の一覧を確認する | ファイル名と理由が表示される |
| 4 | 失敗した分だけの再試行を実行する | 失敗した 1 件だけが再実行される |

## TC-22: 未連携で AI が必要なファイルを含めてアップロードする

**種別**: 異常系
**目的**: IM-02 の事前警告を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を未連携にする | — |
| 2 | `photo.jpg` / `voice.m4a` / `sample.md` を選ぶ | AI 連携が必要なファイルが 2 件あるという事前警告が表示される |
| 3 | そのままアップロードを開始する | 3 件とも取り込まれ、2 件が要 AI 連携の状態になる |
| 4 | 一覧を確認する | 該当ノートが要 AI 連携の状態であることが一覧で見分けられる |

## TC-23: アップロードを途中でキャンセルする

**種別**: 異常系
**目的**: IM-02 のキャンセル処理を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 大きめのファイルを 5 件アップロードし始める | 進捗が表示される |
| 2 | 途中でキャンセルを実行する | 確認のうえキャンセルされる |
| 3 | 結果を確認する | 開始済みの変換は完了し、未開始の分は取り消された旨の内訳が表示される |
| 4 | ノート一覧を確認する | 完了した分のノートは残っている |

## TC-24: 保存容量の上限に達した状態でアップロードする

**種別**: 異常系
**目的**: クォータの検査を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 事前準備 3 で上限を 10 MB に下げたうえで、10 MB 分のファイルをアップロードする | `/settings/usage` で保存容量が上限に達していることが表示される |
| 2 | 新しいファイルをアップロードしようとする | 保存容量の上限に達していることが理由として示され、開始できない |
| 3 | 画面上の導線を確認する | 使用量の画面と、不要なノートの削除への導線がある |
| 4 | ノートを削除して容量を空ける | — |
| 5 | 再度アップロードする | 成功する |

## TC-25: AI 実行回数の上限に達した状態でアップロードする

**種別**: 異常系
**目的**: AI クォータの検査を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 事前準備 3 で上限を 2 回に下げたうえで、AI を使う変換を 2 回実行する | `/settings/usage` で上限到達が表示される |
| 2 | `photo.jpg` をアップロードしようとする | 当月の AI 実行回数が上限に達していることが示される |
| 3 | 画面上の案内を確認する | 翌月まで待つか、機械変換で取り込むよう案内される |
| 4 | 案内に従い、`sample.docx` を機械変換で取り込み直す | 上限に達したままでも取り込みが成功する（詳細は TC-37） |

## TC-26: viewer のワークスペースへ取り込もうとする

**種別**: 異常系
**目的**: IM-04 の権限を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 取り込み先セレクターを開く | `閲覧のみ` は選択肢に出ない |
| 2 | `設計チーム` を選んでアップロードを開始する | 取り込みが始まる |
| 3 | 開始直後に `設計チーム` から自分を除名してもらう | — |
| 4 | 結果を確認する | 残りの取り込みが権限のないことを理由として失敗として記録される |

## TC-27: 取り込めない外部リソースが含まれる本文を取り込む

**種別**: 異常系
**目的**: IM-05 の失敗したリソース参照の扱いを検証する

取り込めなかった参照の扱いは種別で分かれる。この TC が扱うのは画像・動画・音声などの**リソース参照**で、スタイルシートは TC-39 で確認する。

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 404 を返す画像 URL と内部アドレスの画像を参照する HTML をアップロードする | ノートが作られる |
| 2 | 取り込んだリソースの一覧を確認する | 取り込めなかった参照が理由つきで一覧される |
| 3 | 本文の該当箇所を確認する | 元の URL のまま残っており、表示のたびに元の場所から読み込まれる状態が続く |
| 4 | 一覧での分類を確認する | 取り込めなかったものがリソースとスタイルシートで分けて示される（この TC ではリソースのみが並ぶ） |
| 5 | 別のアカウント（同じワークスペースの他のメンバー）で同じノートを開く | 同じ内容の取り込み結果が見える（取り込みを実行したのが自分でなくても表示される） |
| 6 | そのノートを何度か編集して保存し、`/jobs` を確認する | 参照取り込みのジョブが保存のたびに増えない（同じノートに未終端のジョブがあるあいだは新しく登録されない） |

## TC-28: 参照リソースの件数・容量の上限を確認する

**種別**: 境界値
**目的**: IM-05 の予算を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 201 件の外部画像を参照する HTML（`ref-201.html`）をアップロードする | 200 件まで取り込まれ、残りは元の URL のまま残る |
| 2 | 取り込んだリソースの一覧を確認する | 上限に達したため打ち切られた旨が表示される |
| 3 | 画像 200 件と外部スタイルシート 1 件を参照する HTML（`ref-css-201.html`）をアップロードし、完了後の表示を確認する | 画像 200 件で予算を使い切るため、スタイルシートは取得されないまま本文から取り除かれ、その装飾は失われる（取得に失敗した場合と同じ扱い） |
| 4 | 手順 3 の取り込み結果を確認する | 打ち切られたスタイルシートが取り込めなかったスタイルシートとして示され、装飾が変わっている可能性が伝わる |

## TC-29: 元ファイルのないノートで作り直そうとする

**種別**: 異常系
**目的**: IM-06 の前提条件を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | エディタで新規作成したノートを開く | AI での作り直しの操作が提供されない |
| 2 | 元ファイルが AI 必須形式（JPEG など）で OpenRouter が未連携のノートを開く | AI での作り直しを選ぶと**連携を促す**案内になり、ジョブは登録されない |
| 3 | 同じノートで OpenRouter が失効して再連携が必要な状態にして試す | **再連携を促す**案内になり、手順 2 とは案内が分かれる |
| 4 | 元ファイルが `sample.md` / `report.docx` など AI を要さない形式のノートで、未連携のまま元ファイルからの作り直しを実行する | 連携の確認は行われず、ジョブが登録されて本文が作り直される |
| 5 | Drive バックアップからの再生成を未連携のまま要求する | 要求は受け付けられてジョブが登録され、処理履歴で `failed`（AI 連携がないことが理由）として記録される（登録時には検査しない） |

## TC-30: 追加指示の文字数の境界を確認する

**種別**: 境界値
**目的**: IM-06 の追加指示の制約を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | 追加指示に 2000 文字を入力する | 実行できる |
| 2 | 2001 文字を入力する | 2000 文字の上限を超えていることが示され、実行できない |
| 3 | 空欄のまま実行する | 追加指示なしとして実行できる |

## TC-31: 同名ファイルを 2 回アップロードする

**種別**: 異常系
**目的**: IM-01 の重複の扱いを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `sample.md` をアップロードする | ノートが 1 件作られる |
| 2 | 同じ `sample.md` を再度アップロードする | 既存のノートは上書きされず、別のノートが作られる |
| 3 | 一覧を確認する | 同名のノートが 2 件並ぶ |

## TC-32: プレーンテキストを取り込む

**種別**: 正常系
**目的**: IM-01 のプレーンテキストの変換方針を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を未連携にする | 未連携であることが表示で確認できる |
| 2 | `sample.txt` を選ぶ | 形式がプレーンテキストと判定され、変換方式が機械抽出であることが分かる |
| 3 | アップロードして完了後に本文を確認する | 段落が `p` 要素として並んだ素朴な HTML になっている |
| 4 | OpenRouter を連携済みにして同じファイルをアップロードする | 変換方式が AI 構造化であることが分かる |
| 5 | 完了後に本文を確認する | 見出し・段落の構造が付いた HTML になっている |
| 6 | 使用量（`/settings/usage`）を確認する | 当月の AI 実行回数が 1 増えている |

## TC-33: Excel ブックを取り込む

**種別**: 正常系
**目的**: IM-01 の Excel の変換方針と表構造の扱いを検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を未連携にする | 未連携であることが表示で確認できる |
| 2 | `sample.xlsx` を選ぶ | 形式が Excel と判定され、変換方式が機械抽出であることが分かる |
| 3 | アップロードして完了後に本文を確認する | セルの値が読める形で並び、行と列の対応が失われていない |
| 4 | シートごとの内容を確認する | 2 枚のシートの内容がどちらも本文に含まれ、シートの区切りが分かる |
| 5 | OpenRouter を連携済みにして同じファイルをアップロードする | 変換方式が AI 構造化であることが分かる |
| 6 | 完了後に本文を確認する | 表が `table` 要素として組み立てられ、見出し行が本文の行と区別されている |

## TC-34: PowerPoint 文書を取り込む

**種別**: 正常系
**目的**: IM-01 の PowerPoint の変換方針を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を未連携にする | 未連携であることが表示で確認できる |
| 2 | `sample.pptx` を選ぶ | 形式が PowerPoint と判定され、変換方式が機械抽出であることが分かる |
| 3 | アップロードして完了後に本文を確認する | 3 枚のスライドのテキストが順に並んだ素朴な HTML になっている |
| 4 | OpenRouter を連携済みにして同じファイルをアップロードする | 変換方式が AI 構造化であることが分かる |
| 5 | 完了後に本文を確認する | スライドごとの見出しと箇条書きの構造が付いている |

## TC-35: 公開を指定してアップロードする

**種別**: 正常系
**目的**: IM-01 の公開ステータスの保留適用を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | ユーザー A の公開ハンドルが設定済みであることを確認する | `/settings/profile` にハンドルが表示される |
| 2 | アップロード画面で `sample.md` を選び、公開ステータスに公開を指定する | 誘導は表示されず、そのまま開始できる |
| 3 | アップロードの直後にノート詳細を開く | 変換が進行中であることが表示され、公開ステータスは非公開のままである |
| 4 | 公開ページ（`/@user-a`）を開く | そのノートはまだ並んでいない |
| 5 | 変換の完了を待ってノート詳細を確認する | 本文が表示され、公開ステータスが公開に変わっている |
| 6 | 公開ページを再度開く | そのノートが並んでいる |
| 7 | `broken.docx` で同じ手順を試す | 変換が失敗し、ノートは非公開のまま残る |

## TC-36: 公開ハンドル未設定のまま公開を指定してアップロードする

**種別**: 異常系
**目的**: IM-01 の公開ハンドルの事前検出と、適用時点の再確認を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | ユーザー A の公開ハンドルを未設定にする | — |
| 2 | アップロード画面でファイルを選び、公開ステータスに公開を指定する | 公開ハンドルの設定が必要なことが示され、設定画面への導線が出る |
| 3 | そのままアップロードを開始しようとする | 開始できない |
| 4 | 導線からハンドルを設定して戻る | 公開を指定したままアップロードを開始できる |
| 5 | 別のファイルで公開を指定して開始し、変換の完了前にハンドルを未設定に戻す | — |
| 6 | 変換の完了後にノートを確認する | 非公開のまま保存されている |
| 7 | `/jobs` でその変換ジョブを開く | ジョブ自体は成功として記録され、公開を適用できなかった旨が理由つきで残っている |
| 8 | 取り込み先に公開スラッグ未設定のワークスペースを選んで同じ操作をする | ワークスペースの公開スラッグについて手順 2 と同じ検出と誘導が働く |

## TC-37: AI を使わない機械変換を選んで取り込む

**種別**: 正常系
**目的**: IM-01 の機械変換の選択を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | OpenRouter を連携済みにし、`/settings/usage` で当月の AI 実行回数を控える | 現在の回数が表示される |
| 2 | アップロード画面で `sample.docx` を選び、AI を使わない機械変換を選ぶ | 変換方式の表示が機械抽出に変わる |
| 3 | アップロードして完了後に本文を確認する | 段落が `p` 要素として並んだ素朴な HTML になっている（TC-03 と同じ結果） |
| 4 | `/settings/usage` を確認する | 当月の AI 実行回数が手順 1 から増えていない |
| 5 | 同じ指定で `photo.jpg` を取り込む | AI なしでは取り込めないことを理由とする変換失敗として記録される（要 AI 連携にはならない。案内は連携ではなく変換方式の変更を指す） |
| 6 | 手順 5 のノートで再生成を実行する | AI を使う経路で実行され、本文が生成される |
| 7 | `sample.docx` と `photo.jpg` を含む一括アップロードで同じ指定をする | 機械変換では取り込めないファイルが 1 件あるという事前警告と、AI を使う設定への切り替え導線が表示される（受け付けは止まらない） |

## TC-38: 外部スタイルシートを持つ HTML を取り込む

**種別**: 正常系
**目的**: IM-05 のスタイルシートの埋め込みと、埋め込んだ CSS への制約を検証する

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `theme.css` を配信できる状態にして `ref-css.html` をアップロードする | ノートが作られる |
| 2 | 完了後にノート詳細を開く | 元の装飾（配色と余白）が保たれた状態で本文が表示される |
| 3 | 本文の HTML を確認する（編集画面の HTML モードでよい） | `link` 要素は残っておらず、元の `link` があった位置に `style` 要素として CSS が埋め込まれている。その `style` は取り込み済みを表す属性（`data-imported-stylesheet`）で配布元の URL を保持している |
| 4 | 取り込み結果を確認する | 埋め込んだスタイルシートの件数と配布元が、取り込んだリソースの件数とは別に示される |
| 5 | `theme.css` の配信を止めてノートを開き直す | 装飾は変わらない（読み込み元への参照が残らないため、配布元が消えても取り込んだ時点の見た目が保たれる） |
| 6 | 取り込み結果で、除去された内容の記録を確認する | `theme.css` に含まれていた `position: fixed` の宣言と `@import` の規則が除去された旨が示される |
| 7 | 手順 6 の宣言以外の装飾を確認する | 他の指定は失われていない（違反した宣言・規則の単位で落ちるため、スタイルシート全体は捨てられない） |
| 8 | 埋め込まれた CSS の `url(./bg.png)` を確認する | `theme.css` の URL を基準に絶対 URL へ解決されているが、外部 URL のまま残っており、表示のたびに外部から読み込まれる |
| 9 | 表示スタイルの設定を開く | 元装飾の表示が選ばれている |
| 10 | そのノートを編集して保存し、`/jobs` を確認する | 参照取り込みのジョブが新たに登録されない（取り込み済みの `style` は抽出の対象外であり、保存のたびに取りに行き直さない） |
| 11 | 取り込みを実行したアカウントを退会させ、別のメンバーでノートを開く | 取り込み結果（件数・配布元・除去された宣言）がそのまま見える（ジョブの履歴が消えても表示は残る） |

## TC-39: 取り込めなかった外部スタイルシートの扱いを確認する

**種別**: 異常系
**目的**: IM-05 のスタイルシートの失敗時の扱いと、ED-11 への逃がし方を検証する

リソース参照は元の URL のまま残るが（TC-27）、スタイルシートは元の URL のまま残せないため装飾そのものが失われる。

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | `ref-css-404.html` をアップロードする | ノートが作られる |
| 2 | 完了後にノート詳細を開く | 本文は表示されるが、装飾は当たっておらず素のブラウザ表示になっている |
| 3 | 取り込み結果を確認する | 取り込めなかったスタイルシートが理由つきで示され、見た目が変わっている可能性があることが伝わる |
| 4 | 本文の HTML を確認する | 元の URL は `link` としては残っていない。取得できなかったことを表す属性（`data-stylesheet-unavailable`）を持つ中身のない `style` 要素が、元の位置に残っている（装飾を失った事実の記録であり、表示には影響しない） |
| 5 | 表示スタイルの設定を開く | 元装飾の表示が選ばれたままである（自動判定は取り込みより前の入力に対して行われ、取り込みの結果で押し戻されない） |
| 6 | 既定スタイルの表示に切り替える（ED-11） | 既定スタイルが当たり、装飾を失った本文が読みやすい表示になる |
| 7 | ノートを開き直す | 切り替えた既定スタイルの表示が維持されている |
| 8 | そのノートを編集して保存し、`/jobs` を確認する | 参照取り込みのジョブが繰り返し登録されない（取得できなかった痕跡は抽出の対象外であり、同じ URL を取りに行き直さない） |
| 9 | 既定スタイルの表示に切り替えたまま取り込み結果を確認する | 取り込めなかったスタイルシートの表示は消えない（表示スタイルの設定と、装飾を失った事実の記録は別のものである） |

---

## カバレッジ

### ユースケースエラーケース対応表

| ユースケース | エラーケース | 対応TC | 備考 |
|---|---|---|---|
| startBulkUpload | 件数の上限超過 | TC-17 | |
| startBulkUpload | 合計サイズの上限超過 | TC-17 | |
| startBulkUpload | すべて受け付け不可 | TC-15 | |
| startBulkUpload | 容量の上限到達 | TC-24 | |
| startBulkUpload | ワークスペースの権限不足 | TC-26 | |
| startBulkUpload | 公開ハンドル未設定で公開を指定 | TC-36 | 公開設定の側からの検証は `sharing.md` の TC-09 |
| storeUpload | 未対応の MIME・拡張子 | TC-15, TC-20 | |
| storeUpload | サイズ超過 | TC-16 | |
| storeUpload | 容量の上限到達 | TC-24 | |
| storeUpload | AI 実行回数の上限到達 | TC-25 | 案内される機械変換での再取り込みは TC-37 |
| storeUpload | 権限不足 | TC-26 | |
| storeUpload | パスワード保護の検出 | TC-19 | |
| runConversion | パスワード保護の優先 | TC-19 | 手順 3〜5。連携の有無・変換方式にかかわらず理由が入れ替わらないことを確認する |
| storeUpload | オブジェクトストレージの失敗 | 対象外 | インフラ障害 |
| planConversionForUpload | 判定処理の失敗 | 対象外 | 判定基盤の障害 |
| runConversion | ノート・ファイルの不在 | TC-26 | 除名による失敗で近い状態を確認する |
| runConversion | 連携なし | TC-22 | |
| runConversion | 連携の失効 | 外部連携 TC-15 | `integration.md` で検証する |
| runConversion | モデルのレート制限 | 対象外 | プロバイダー側の状態を作れない |
| runConversion | 時間切れ | 対象外 | 実行時間を制御できない |
| runConversion | 破損 | TC-18 | |
| runConversion | 未対応形式 | TC-15 | |
| runConversion | 公開ステータスの適用不可 | TC-36 | 適用時点でハンドルが未設定に戻っている場合 |
| importExternalReferences | リソース参照の取得失敗 | TC-27 | 元の URL のまま残る |
| importExternalReferences | スタイルシートの取得失敗 | TC-39 | 元の URL のまま残せないため装飾が失われる。痕跡は取得できなかったことを表す属性を持つ空の要素として残り、それが装飾を失った事実の記録になる。既定スタイルへの切り替え（ED-11）は `editing.md` の TC-14 側でも確認する |
| importExternalReferences | 拒否された URL | TC-27 | |
| importExternalReferences | 取得した CSS が禁じた宣言・規則を含む | TC-38 | 手順 6・7。宣言・規則の単位で落ち、残りは埋め込まれたまま成功として扱われる |
| importExternalReferences | 予算超過 | TC-28 | 手順 3・4 で、打ち切られたスタイルシートが取り除かれることを確認する |
| importExternalReferences | 版の競合 | 対象外 | 取り込み中に本文を編集する状況。自動テストで担保する |
| requestRegeneration | 元ファイルもバックアップもない | TC-29 | |
| requestRegeneration | AI 必須形式で未連携（`localFile`） | TC-29 | 手順 2。連携を促す案内になる |
| requestRegeneration | AI 必須形式で失効（`localFile`） | TC-29 | 手順 3。再連携を促す案内になり、未連携とは分かれる |
| requestRegeneration | AI を要さない形式・`driveBackup` | TC-29 | 手順 4・5。前者は未連携でも登録され、後者は登録時に検査せず実行時に失敗する |
| requestRegeneration | 追加指示が長すぎる | TC-30 | |
| requestRegeneration | 多重実行 | 外部連携 TC-20 | |
| runRegeneration | 変換の失敗 | 外部連携 TC-26 | |

### 観点チェックリスト

| 観点 | 対応TC | 対象外の理由 |
|---|---|---|
| 入力バリデーション | TC-15, TC-20, TC-30, TC-36 | |
| 境界値 | TC-16, TC-17, TC-28, TC-30 | |
| 認証・権限 | TC-26 | |
| 空状態・初期状態 | TC-01 | ノート 0 件からの初回アップロードを含む |
| 重複・競合 | TC-31 | |
| 削除・変更の影響 | TC-13, TC-24, TC-36, TC-38 | 変換中にハンドルを外した場合の適用結果と、配布元が消えても埋め込んだ装飾が保たれることを含む |
| 操作の中断・逸脱 | TC-14, TC-23, TC-36 | |
| 特殊入力 | TC-20, TC-38 | 拡張子の偽装と、禁じた宣言・規則を含む CSS の埋め込みを含む |
| UI の状態 | TC-14, TC-18, TC-21, TC-22, TC-35, TC-37, TC-39 | 公開の保留適用、変換方式の切り替え表示、装飾を失ったノートの表示と切り替えを含む |
