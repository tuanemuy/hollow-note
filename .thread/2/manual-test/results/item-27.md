# 項目 27: アカウント削除 — 確認入力が一致しないと実行できない

**結果**: PASS
**対応する受け入れ基準**: AC-26、AC-28（TC-38）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/settings/danger` を開き説明文を確認 | 削除されるもの・されないものの説明が表示される | 「削除されるもの」= プロフィール（表示名・ハンドル・自己紹介・アイコン）／アイコンなどのアップロード済みファイル／すべてのログイン方法とセッション。「削除されないもの」= 個人のノートと、その公開ページ・共有リンク／ワークスペースが持つノート（作成者は「退会した利用者」と表示されます）／他のメンバーが取り込んだ元ファイル。加えて「この操作は取り消せません。実行すると直ちにサインアウトし、削除の処理が終わるまでこの画面で進捗を表示します。」 | PASS |
| 2 | 確認欄に `user-b@example.com`（誤り）を入力し実行 | 押せないまま、または押しても一致しない旨が表示される | 削除ボタンは常時 enabled（送信時検証）。押すと「メールアドレスが一致しません。アカウントのメールアドレスをそのまま入力してください。」を表示し、削除は実行されず `/settings/danger` に留まった | PASS |
| 3 | 確認欄を `user-a@example.com` に直す | 削除ボタンが押せるようになる | 削除ボタンは押せる状態（元から enabled） | PASS |

## 確認ポイント

- 唯一 owner のワークスペース警告（「先にワークスペースの引き継ぎが必要です」）は**表示されない** = 正しい（PAGE-p25-004 は #3）。

## 気づいた点（不具合ではない）

- 削除ボタンは入力内容にかかわらず常に押せる（一致判定は送信時）。testing.md の期待結果はこの実装を許容している。
- 手順 2 で出たエラー文言は、手順 3 で正しいメールアドレスに直しても消えない（再送信するまで残る）。誤解を招きうる軽微な UI 挙動。
