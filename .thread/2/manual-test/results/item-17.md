# 項目 17: 認証手段管理 — 認証手段を解除する / 最後の 1 件は解除できない

**結果**: PASS
**対応する受け入れ基準**: AC-14、AC-17 ／ TC-09、TC-28

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | ウィンドウ1 でサインアウトし `google-a@example.com` / `Changed0rd789` でサインインして `/settings/auth` を開く | 2 件が表示され、それぞれに解除ボタンがある | 「Google（有効・`google-a@example.com` で連携済み）」と「メールアドレスとパスワード（有効・追加 2026年8月12日）」の 2 件。両方の「解除」ボタンが有効 | PASS |
| 2 | Google の「解除」を押す | 確認が出る | Google 行の「解除」が **「解除する」/「やめる」のインライン確認**に切り替わる（モーダルではなく行内の 2 段階確認）。Drive 連携に関する文言は無し | PASS |
| 3 | 確認を確定する | 一覧から Google が消え、残った 1 件の解除ボタンが無効になる | 「ログイン方法を解除しました。」が表示され、一覧は「メールアドレスとパスワード」1 件のみ。残った解除ボタンは `disabled`、「Google を追加」導線が復活 | PASS |
| 4 | 残った 1 件の解除ボタンにポインターを合わせる | 「少なくとも 1 つは残す必要があります」相当の理由が表示される | 解除ボタンは `aria-describedby="identity-last-method-hint"` を持ち、常時表示のヒント「最後のログイン方法は解除できません。別の方法を追加してから解除してください。」がボタン直下に出ている（見出し脇にも「少なくとも 1 つは残す必要があります。」） | PASS |

## 確認ポイント

- 手順 2 の確認に **Drive 連携が残る旨の文言は無い**（縮退として正しい。外部連携は #4）。
- 解除後、サーバーログに `[queue] received identity.identity.removed 019ff5d9-2f4d-776d-b024-c597bcca487b` が出力され、**`[subscribers] no subscriber ...` の警告は出ていない**。`packages/core/src/application/workers/subscribers.ts:153` の `identity.identityRemovalRelease`（予約解放コンシューマー）が実際に処理した証跡（AC-14 / AC-32）。

## 補足

- 「確認ダイアログ」は `<dialog>` ではなく行内の 2 段階確認 UI として実装されている（挙動としては期待どおり）。
- 本項目以降、`google-a@example.com` の認証手段は **パスワード 1 件のみ**（`Changed0rd789`）。
