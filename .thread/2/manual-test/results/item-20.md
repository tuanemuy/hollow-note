# 項目 20: プロフィール — 表示名と公開ハンドルを設定する

**結果**: PASS
**対応する受け入れ基準**: AC-18、AC-20（TC-10 手順 1〜4。手順 5〜7 は skip → #9）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | ウィンドウ2 で `user-b@example.com` / `Passw0rd123` サインイン → `/settings/profile` でハンドル `user-a` を入力し保存 | 保存できる | 「このハンドルは使用できます」表示後、保存 →「保存しました」。再表示でハンドル欄が `user-a` | PASS |
| 2 | ウィンドウ2 をサインアウト | サインアウトされる | アカウントメニュー →「サインアウト」で `http://localhost:3100/` へ遷移 | PASS |
| 3 | ウィンドウ1（user-a）で `/settings/profile` を開く | 表示名・自己紹介・アイコン・公開ハンドルの入力欄が表示される | 4 つすべて表示（アイコンは「画像を選ぶ」ボタン + 「PNG / JPEG / WebP · 5 MB まで」注記） | PASS |
| 4 | 表示名を `テスト太郎` に変更 | 入力できる | 入力できた | PASS |
| 5 | 公開ハンドル欄に `test-taro` を入力 | 重複チェックが走り使用可能であることが示される | 「このハンドルは使用できます」が表示された | PASS |
| 6 | 保存 | 保存完了が表示される | 「保存中...」→「保存しました」 | PASS |
| 7 | 再読み込み → アカウントメニューを開く | 表示名 `テスト太郎` が保持されメニューにも反映 | 入力欄は `テスト太郎` / `test-taro`、アカウントメニューにも `テスト太郎` と表示 | PASS |

## 補足

- 公開プロフィールのプレビュー導線（`/@test-taro`）は無し = 確認ポイントどおり（P-42 は #9）。
- 手順 6 の保存後、ハンドル欄の上に「ハンドルを変えると古い URL は開けなくなります」の事前警告が表示されるようになる（項目 21 の確認ポイントを先取りで確認）。
- 自己紹介欄の注記は「500 文字まで」で、モック（300 文字）ではなく仕様どおり。
