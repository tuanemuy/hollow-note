# 項目 20: プロフィール — 表示名と公開ハンドルを設定する

**再検証（修正後）**
**結果**: PASS
**対応する受け入れ基準**: AC-18、AC-20（TC-10 手順 1〜4。手順 5〜7 は skip → #9）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 0 | 前提づくり: `/signup` で `user-b@example.com` / `Passw0rd123` / `ユーザーB` を登録し、サーバーログの `actionUrl` で確認 | 確認完了・サインイン状態になる | 確認リンク遷移後にノート一覧（空状態）が表示され、サインイン済み | PASS |
| 1 | `user-b` の `/settings/profile` でハンドル欄に `user-a` を入力し保存 | 保存できる | 入力中に「このハンドルは使用できます」→ 保存で「保存しました」。ハンドル欄は `user-a` を保持 | PASS |
| 2 | ウィンドウ2（`user-b`）をサインアウト | サインアウトされる | アカウントメニュー →「サインアウト」で未認証状態に戻った | PASS |
| 3 | `user-a@example.com` / `Passw0rd123` / `ユーザーA` を登録・確認し、`/settings/profile` を開く | 表示名・自己紹介・アイコン・公開ハンドルの入力欄が表示される | 4 つすべて表示（アイコンは「画像を選ぶ」＋「PNG / JPEG / WebP · 5 MB まで」） | PASS |
| 4 | 表示名を `テスト太郎` に変更 | 入力できる | 入力できた | PASS |
| 5 | 公開ハンドル欄に `test-taro` を入力 | 重複チェックが走り使用可能であることが示される | デバウンス後に「このハンドルは使用できます」が表示された | PASS |
| 6 | 保存 | 保存完了が表示される | 「保存しました」が表示され、「未保存の変更があります」バーが消えた。入力欄は `テスト太郎` / `test-taro` のまま | PASS |
| 7 | ページを再読み込みし、アカウントメニューを開く | 表示名 `テスト太郎` が保持されメニューにも反映 | 再読み込み後の入力欄は `テスト太郎` / `test-taro`。アカウントメニューに `テスト太郎` と表示 | PASS |

## 補足

- 公開プロフィールのプレビュー導線（`/@test-taro`）は無し = 確認ポイントどおり（P-42 は #9）。
- ハンドルを保存済みの状態では「ハンドルを変えると古い URL は開けなくなります／共有済みのリンクや検索エンジンの登録もたどれなくなります。変更前の URL は他の人が使えるようになります。」の事前警告が常時表示される。
- 自己紹介欄の注記は「500 文字まで」。
