# 項目 22: プロフィール — 予約語・不正な文字のハンドル

**再検証（修正後）**
**結果**: PASS
**対応する受け入れ基準**: AC-18、AC-20（TC-32）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | ハンドル欄に `settings` を入力 | この名前が使用できないことが示される | 入力デバウンス後に「このハンドルは予約されていて使えません。別のハンドルでお試しください。」 | PASS |
| 2 | `テスト` を入力 | 使用できる文字種（英小文字・数字・ハイフン・アンダースコア）が示される | 「ハンドルは英小文字・数字・ハイフン・アンダースコアの 3〜30 文字です。入力内容を確認してもう一度お試しください。」 | PASS |
| 3 | `Test-User` を入力して保存 | 小文字に正規化され `test-user` として保存される（保存後の表示が `test-user`） | **保存直後に入力欄が `test-user` に更新され**、「保存しました」が表示され、「未保存の変更があります」バーが消えた（修正が効いている） | PASS |
| 4 | ページを再読み込み | `test-user` が保持されている | 入力欄は `テスト太郎` / `test-user` のまま | PASS |
| 5 | （追加確認）`Alpha-One` を入力 → 保存を押した直後に同じ欄を `beta-two` へ打ち替え | 保存応答で入力が巻き戻らない | 応答後も入力欄は `beta-two` のまま。「このハンドルは使用できます」＋「未保存の変更があります」バーが正しく出ており、サーバー truth（`alpha-one`）で上書きされなかった | PASS |

## 修正の効果

- 前回 PARTIAL だった「保存は成功するのに UI が送信値（`Test-User`）のまま・成功トーストが出ず未保存バーが残る」点は解消。保存アクションの戻り値（`ProfileView`）で、送信した値が残っている欄だけサーバー truth に種まき直す挙動を確認した。
- 併せて、保存中に同じ欄を打ち替えたケースでも巻き戻りが起きないことを確認した（ステップ 5）。

## 気づいた点

- 大文字を含む入力に対して、入力中の可用性チェックは正規化後で判定しており「このハンドルは使用できます」を返す（従来どおり）。
