# 項目 1: メール確認 — 確認リンクでサインイン状態になり空状態が出る

**結果**: PASS
**対応する受け入れ基準**: AC-4、AC-5（TC-02。前提として TC-01 の手順を実行）

補足: サーバーは `http://localhost:3100`（testing.md の 3000 を読み替えて実行）。ウィンドウ1 = agent-browser セッション `mt-w1`。

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | ウィンドウ1 で `http://localhost:3100/signup` を開く | サインアップフォームが表示される | メール / パスワード / 表示名 / 規約同意チェック / 「アカウントを作る」（同意前は disabled）が表示された | PASS |
| 2 | `user-a@example.com` / `Passw0rd123` / `ユーザーA` を入力し規約に同意して送信 | 送信が受理される | 同意チェックでボタンが有効化され、送信後「確認メールを送信しました」+「サインインへ」が表示された | PASS |
| 3 | サーバーログの `mail.sent` から `actionUrl` を取得 | `/verify-email?token=...` が出る | `http://localhost:3100/verify-email?token=MDE5ZmY1YzAtMTU5NC03MmZhLTlmYjUtOTEzMGU0MjhkM2Y2.gQucWgyHyLja8HtBrRefR4GrehprjU7CRUjcJb9P4Dw`（ホストは 3100） | PASS |
| 4 | 同じウィンドウ1 でその URL を開く | 処理中表示の後 `/notes` へ遷移しサインイン状態になる | URL が `/notes` に遷移。ノート 0 件の空状態（見出し「最初のノートを作る」/「白紙のノートを作成すると、この一覧に追加されます。」/ ボタン「白紙から書く」）が表示された | PASS |
| 5 | 上部バーのアカウントメニューを開く | 表示名 `ユーザーA` が出る | メニューに `ユーザーA`、「設定」「サインアウト」が表示された | PASS |

## 確認ポイント

- 空状態にアップロード導線が無いこと: 空状態の導線は「白紙から書く」のみ。想定どおり（取り込みスライス #6 のため）。⌘K ヒントに「検索・絞り込み・アップロード（準備中）」の文言はあるが、これは無効化された準備中表示。
- 確認 URL はサインアップと同じウィンドウ1 で開いた（ADR-038 の分岐に倒れず、そのままサインイン状態になることを確認）。

## 失敗詳細

なし。
