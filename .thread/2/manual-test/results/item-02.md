# 項目 2: メール確認 — 使用済み・改ざんリンクの状態表示

**結果**: PASS
**対応する受け入れ基準**: AC-4、AC-5（TC-20）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 項目 1 で使用済みの確認 URL をウィンドウ1 でもう一度開く | 「このメールアドレスは確認済みです」相当 + サインイン画面への導線。`/notes` へは遷移しない | 見出し「このメールアドレスは確認済みです」とリンク「サインインへ」が表示。URL は `/verify-email?token=...` のままで `/notes` へは遷移しなかった | PASS |
| 2 | 同 URL の `token` 末尾 1 文字を書き換えて開く（`...P4Dw` → `...P4Dx`） | 「このリンクは使えません」相当の無効状態 + 確認メールの再送導線。`/notes` へは遷移しない | 見出し「このリンクは使えません」、本文「リンクが途中で切れているか、書き換えられている可能性があります。…」、メールアドレス入力欄 + ボタン「確認メールを再送」（未入力時は disabled）、リンク「サインインへ」が表示。URL は `/verify-email` のまま | PASS |

## 確認ポイント

- トークン消費が GET のページ表示ではなくマウント後の POST（server function）であること: agent-browser の `network requests` で確認。ドキュメント `GET /verify-email?token=...` (200) の後に `POST /_serverFn/…VerifyEmailPanel/action.ts…verifyEmailFn_createServerFn_handler` (Fetch, 200) が発生している。GET 単体ではトークンを消費していない。

## 失敗詳細

なし。
