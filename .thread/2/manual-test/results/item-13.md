# 項目 13: 認証手段管理 — パスワード認証手段を追加する

**結果**: PASS
**対応する受け入れ基準**: AC-13、AC-17 ／ TC-07 手順 1, 3, 4（手順 2「Google の再認可」は spec に無いため skip）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | ウィンドウ1 `/signin` で「Google で続ける」→ 同意画面で `google-a@example.com` / `グーグル太郎` / `email_verified` ON で「許可する」 | サインインできる | `/dev/oauth/authorize` の同意画面が出て、許可後 `/notes` へ遷移（既存の `google-a` アカウントにサインイン） | PASS |
| 2 | `/settings/auth` を開く | Google 1 件が表示され、パスワードが未設定と分かる | 「有効なログイン方法」に **Google（有効・`google-a@example.com` で連携済み）1 件のみ**。解除ボタンは `disabled` で「最後のログイン方法は解除できません。別の方法を追加してから解除してください。」。パスワード側は「パスワードを追加」導線 | PASS |
| 3 | 「パスワードを追加」で `Passw0rd123` を 2 回入力して保存 | 追加完了が表示され、一覧に「メールアドレスとパスワード」が加わる。パスワード追加の導線は消える | 「パスワードを追加しました。次回からメールアドレスとパスワードでもサインインできます。」が表示。一覧が Google ＋「メールアドレスとパスワード（有効・追加 2026年8月12日）」の 2 件になり、**両方の解除ボタンが有効**。追加フォームは「パスワードを変更」フォームに置き換わった | PASS |
| 4 | サインアウトし、`/signin` で `google-a@example.com` / `Passw0rd123` を送信 | `/notes` へ遷移 | `/notes` へ遷移（パスワードでサインインできた） | PASS |

## 補足

- 手順 3 で **Google の再認可は求められなかった**（縮退として正しい。P-22 の「再認証要求」は本 Issue 対象外）。
- 期待結果の「Google 追加ボタンは残るが」については、`google-a` は既に Google を連携済みのため `/settings/auth` に「Google を追加」ボタンは表示されない（連携済み行として出る）。矛盾ではなく、未連携時のみ追加導線が出る挙動。
