# 項目 6: OAuth — dev IdP で新規サインアップし、認証手段が Google 1 件になる

**結果**: PASS
**対応する受け入れ基準**: AC-6、AC-8、AC-9（PAGE-p01-003 / p02-003）、AC-17（TC-05 手順 1〜3。手順 4 は skip → #4）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 同意画面でメール `google-a@example.com`、表示名 `グーグル太郎`、`email_verified` ON で「許可する」 | `/auth/callback/google?code=...&state=...` を経て `/notes` へ遷移しサインイン状態 | コールバックを経由して `http://localhost:3100/notes` へ遷移。ノート 0 件の空状態（「最初のノートを作る」「白紙から書く」）が表示された | PASS |
| 2 | 遷移後の画面のアカウントメニューを確認 | 表示名 `グーグル太郎` | アカウントメニューを開くと `グーグル太郎` が表示された | PASS |
| 3 | アカウントメニュー →「設定」→「ログイン方法」（`/settings/auth`） | 有効なログイン方法が Google 1 件だけ、その解除ボタンが無効 | 「有効なログイン方法／少なくとも 1 つは残す必要があります。／Google／有効／google-a@example.com で連携済み／解除（disabled）／最後のログイン方法は解除できません。別の方法を追加してから解除してください。／パスワードを追加」。解除ボタンは `disabled` | PASS |

## 確認ポイント

- `/settings` のタブは **プロフィール / ログイン方法 / 使用量 / アカウント削除の 4 つ**。「連携」タブは無い（P-23 は #4 のため正しい）。
- TC-05 手順 4（外部連携一覧）は skip（#4）。
- `/settings/profile` の表示名欄にも `グーグル太郎` が入っていた（OAuth のプロフィール取り込みが効いている）。

## 失敗詳細

なし。
