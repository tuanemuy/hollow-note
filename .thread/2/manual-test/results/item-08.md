# 項目 8: パスワード再設定 — 未登録メールでの申請（存在を漏らさない）

**結果**: PASS
**対応する受け入れ基準**: AC-10（TC-24）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/reset-password` で `nobody@example.com` を送信 | 項目 7 手順 2 とまったく同じ完了表示 | 「メールを送りました／そのアドレスで登録されていれば、再設定のリンクが届きます。リンクは 1 時間で無効になります。／サインインに戻る」。項目 7 の文字列と完全一致（見出し・本文・導線とも同一） | PASS |
| 2 | サーバーログを確認 | `nobody@example.com` 宛のメールが 1 通も出ない | `mail.sent` の総数は送信前後とも 6 件で増えず、ログ全体を `nobody@example.com` で検索してもヒット 0 件 | PASS |

## 確認ポイント

- 応答時間・画面遷移も項目 7 と体感差なし。存在有無が画面から判別できない。

## 失敗詳細

なし。
