# 項目 21: プロフィール — 使用中のハンドルを設定しようとする

**再検証（修正後）**
**結果**: PASS
**対応する受け入れ基準**: AC-18、AC-20（TC-31）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/settings/profile`（`user-a` でサインイン中）のハンドル欄に `user-a` を入力 | 入力中に「このハンドルは使われています」相当が示され、代替候補が提示される | デバウンス後・**保存を押す前に**「このハンドルは使われています」と代替候補チップ `user-a-1` / `user-a-2` / `user-a-26` の両方が表示された（修正が効いている） | PASS |
| 2 | 保存を試みる | 保存されない | 保存されず、「このハンドルは使われています。別のハンドルでお試しください。」に文言が変わり、候補チップは引き続き表示。入力欄は `user-a` のまま | PASS |
| 3 | 提示された候補のいずれか（`user-a-1`）を選ぶ | 使用可能であることが示される | 入力欄が `user-a-1` に置換され「このハンドルは使用できます」が表示された | PASS |
| 4 | 保存 | 保存できる | 「保存しました」が表示され、「未保存の変更があります」バーが消えた。入力欄は `user-a-1` | PASS |

## 確認ポイント

- ハンドル変更の事前警告「ハンドルを変えると古い URL は開けなくなります／共有済みのリンクや検索エンジンの登録もたどれなくなります。変更前の URL は他の人が使えるようになります。」が保存前から常時表示されている。**PASS**

## 修正の効果

- 前回 PARTIAL だった「代替候補が入力中に出ない」点は解消。入力デバウンス完了時点（保存前）で候補チップが提示される。

## 気づいた点（不具合ではない）

- 候補チップは画面下部の固定バー（「未保存の変更があります／変更を取り消す／保存」）に隠れる位置に出ることがあり、1280x633 のビューポートでは 1 度スクロールしてからでないとクリックが届かなかった（前回と同じ挙動）。スクロール後は正常にクリックできる。
