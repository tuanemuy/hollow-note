# TC-10: 未確認ユーザーのサインイン（EMAIL_NOT_VERIFIED の分離）

**結果**: PASS
**対応する受け入れ基準**: AC-9、AC-16（P-02）

## 使用アカウント（本テストで新規作成）

- `pending@example.com` / `Passw0rd123` / 表示名 `Pending User` — **確認リンクは開いていない（Pending のまま）**

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 別セッション（`verify-b2`）で `/signup` から `pending@example.com` を新規サインアップ（確認リンクは開かない） | 送信完了状態になる | 「確認メールを送信しました」＋「pending@example.com 宛に確認メールを送りました。…リンクは 24 時間で無効になります。」＋「サインインへ」リンク。サーバーログに `to: 'pending@example.com'` のメール出力 | PASS |
| 2 | `/signin` から `pending@example.com` / `Passw0rd123` を送信 | 認証失敗の共通文言ではなく、メール未確認の専用状態 | status 領域に「**メールアドレスの確認が済んでいません**」＋「登録時に送った確認メールのリンクを開いてください。」。TC-8 の「メールアドレスかパスワードが違います」とは別文言 | PASS |
| 3 | レスポンスを確認 | — | `POST /_serverFn/...SignInForm/action.ts...signInFn... → 422`（サインイン成功せず `/signin` に留まる） | PASS |

## 確認ポイント

- 確認メール再送の導線は表示されていない（PAGE-p02-006 見送りのため正しい）。画面上のリンクは「アカウントを作る」のみ。
