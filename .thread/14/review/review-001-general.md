# General Review（乖離台帳）

対象: `.thread/14/research.md`（SYNC-01〜27）/ `.thread/14/research-2.md`（SYNC-201〜244）
観点: 調査メモとしての出来ではなく、**恒久的にリポジトリに残る参照ドキュメント**として成立しているか。

判定そのものは抽出検証した範囲で全件が実物と一致していた（下記カバレッジ）。指摘は**引用の誤り・件数の不整合・as-of の欠落・作業記録の残置**に集中する。

## Blockers

- **[B-001]** 実在しない ADR の引用 — `spec/adr/022` を「OCC トークンを `findById` から得る」ADR として 3 か所で引いている
  - 場所: `.thread/14/research-2.md:314`, `:317`, `:598`
  - 理由: `spec/adr/022` の実体は `022-command-palette-navigation.md`「常設サイドバーを持たず、航法をスコープトークンとコマンドパレットに二重化する」で、OCC とも `findById` とも無関係。意図しているのは `.thread/2/adr.md:525` の **ADR-022**「`StoredFileRepository` は spec の形を保ち、削除は `findById` の OCC トークン経由で行う」であり、`spec/adr/` と `.thread/2/adr.md` の 2 つの ADR 名前空間を取り違えている。SYNC-227（「3 文」をテストケース表から外す）の**判定そのものの根拠**として引かれているため、将来この判定を再検討する人が `spec/adr/022` を開いて根拠に辿り着けない。しかも `spec/adr/022` は実在するので、リンク切れとして検出されず黙って別の文書に着地する
  - 提案: 3 か所とも `.thread/2/adr.md` ADR-022 と明記する。あわせて、本 Issue で同じ論拠を昇格させた `spec/adr/056-performance-budget-placement.md:11`（in-memory は OCC トークンを行ごとに `findById` で読む、を明示的に書いている）を正典側の参照として併記する

- **[B-002]** 台帳全体が「現在の spec / 現在の実装」という現在形で書かれているのに、その「現在」が HEAD ではなく `55a5bb9`（修正**前**）である旨の断りが項目側に無い
  - 場所: `.thread/14/research.md:3` および全 SYNC 項目 / `.thread/14/research-2.md:3` および全 SYNC 項目
  - 理由: 本 PR が台帳の「要修正」59 件をすべて反映済みなので、`現在の spec:` で始まる記述はほぼ全件が HEAD では偽になっている（実測で確認: SYNC-244 の「現在の spec: 11 コード」→ 実物は 14 コード、SYNC-205 の「4 メソッド。`beginRelease` は無い」→ 実物は 5 メソッド、SYNC-235 の「認証要件の言及が無い」→ 実物は `spec/pages/index.md:487` に明記、等）。あわせて `file:line` アンカーが **research.md 99 / research-2.md 155（うち `spec/*.md` 196）** あり、これらも同じ commit 基準のまま全部ドリフトしている。実測すると:
    - `spec/domains/identity.md:519-525`（台帳: `IdentityErrorCode` union）→ HEAD ではドメインイベントの説明文。実物の union は `:546-549`
    - `spec/domains/identity.md:369` / `:379`（台帳: エラーケース行の逆転）→ `:369` は空行、`:379` は**別の内容の段落**
    - `spec/domains/index.md:124`（台帳: cleanup consumer の説明段落。SYNC-14 / 206 / 207 / 228 / 229 が全部ここを指す）→ HEAD ではコードブロック内の `terminalAt: Date | null;`。実物の段落は `:149`
    - `spec/domains/index.md:121`（`DistributedOperationStore` の 1 行言及）→ 実物は `:147`
  - 冒頭の `対象 HEAD: 55a5bb9` は「すべての判定は実ファイルを読んで裏を取った」という**判定の裏取り**に係る文として読め、本文の現在形と行番号がその commit 基準であることまでは宣言していない。台帳は AC の由来欄から SYNC ID で恒久参照される成果物（`plan.md:20`）なので、数か月後に ID から飛んだ読者が誤った行へ着地する
  - 提案: 両ファイルの冒頭に 1 行足すだけで解消する。例:「**本文の「現在の spec / 現在の実装」と全ての `file:line` は `55a5bb9` 時点の記述である。本 Issue の修正は反映後の状態であり、参照するときは `git show 55a5bb9:<path>` で当たること。**」（`plan.md:202` が steps.md についてだけ書いているドリフト注意を、台帳側にも置く）

## Warnings

- **[W-001]** 「反映先ファイル別の逆引き」表の件数が 4 行で ID 数と合わない
  - 場所: `.thread/14/research-2.md:553`, `:555`, `:568`, `:569`
  - 理由: `spec/domains/index.md` **9** に対し ID は 10 個（206,207,208,209,216,217,228,229,231,236）、`spec/database/index.md` **4** に対し 5 個（224,228,230,231,233）、`spec/inventory/domain.md` **11** に対し 10 個、`spec/inventory/adapter.md` **10** に対し 9 個。逆引き表は「どのファイルに何件当たるか」を数えるためだけの表なので、件数が信用できないと表の存在意義が消える
  - 提案: 4 行の件数を ID 数に合わせる（10 / 5 / 10 / 9）

- **[W-002]** 「分類別の内訳」表の合計が 43 で、台帳の総数 44 と合わない
  - 場所: `.thread/14/research-2.md:523-532`
  - 理由: A11 + B7 + C9 + D10 + E2 + F1 + G2 + H1 = **43**。`### I. 統合作業で追加（1 件）`（SYNC-244）の行だけが表から漏れている。`:8` が「計画レビュー R2 coverage:S-006 で 43 → 44 に訂正した」と書いているとおり、訂正が本文（I 群の新設）と `:513` の集計表には入ったのにこの表には入らなかった。**同一ファイル内に 43 と 44 の 2 つの総数が並ぶ**状態で、どちらが正かは読者には判断できない
  - 提案: `| I. 統合作業で追加 | 1 | spec/domains/identity.md |` の 1 行を足す

- **[W-003]** 自ファイル内の行番号参照が外れている
  - 場所: `.thread/14/research-2.md:8`（「L497 の集計表と一致」）
  - 理由: `:497` は `### I. 統合作業で追加（1 件）` の見出しで、集計表は `:513`。B-002 と違い**同一ファイル内の参照**なので、外部のドリフトではなく単純な誤り
  - 提案: 行番号ではなく `## 集計` という見出し名で指す（同じ理由で他の自ファイル参照も見出し名に寄せる）

- **[W-004]** PR #17 が昇格させた ADR の本数と範囲が誤っている
  - 場所: `.thread/14/research-2.md:17`
  - 理由: 「PR #17 は `spec/adr/` に 30 本の ADR を昇格させている（`038`〜`051`）」とあるが、実測（`git diff --name-only cea6134..55a5bb9 -- spec/`）は **ADR 29 本（023〜051）＋ `index.md` の計 30 ファイル**。「30 本」と「038〜051（14 本）」が同じ括弧内で食い違っている。同じ事実を `research.md:42` は「`spec/adr/*.md` の 30 ファイル追加のみ」と正しく書いており、2 台帳のあいだでも食い違う。この段落は台帳全体の前提（「Issue #2 の設計判断は既に正典に入っており未追随なのは下流だけ」）を支える箇所なので、数字が雑だと前提ごと疑われる
  - 提案: 「ADR 29 本（`023`〜`051`）と `index.md` の計 30 ファイル」に直す

- **[W-005]** 「判断に迷った点 / plan に委ねる論点」節が、決着後もそのまま残っている
  - 場所: `.thread/14/research-2.md:588-603`
  - 理由: 冒頭に「以下の 6 点は統合作業で決着した」と注記はあるが、本文は未決着時の一人称の意見表明のまま — 「**私は含める側を推す**」（`:596`）、「plan で当たり直す必要がある」（`:598`）、「番号の付け方は plan の判断」（`:592`）、「plan で明示すべき」（`:594`）。恒久参照ドキュメントに残す内容ではなく、決着は `spec/adr/052`〜`057` と `plan.md` の AC に昇格済みで二重管理になっている。加えて `:603`（項目 6）は「実数は 42（+ 本調査の 1 件 = 43）」と**訂正前の件数のまま**で、`:8` の 44 および `:519` の集計表と真正面から矛盾する（W-002 と同根）
  - 提案: 節ごと削除して「6 論点は `.thread/14/adr.md` ADR-011〜015 と `spec/adr/052`〜`057` で決着済み」の 1 行に畳む。残す場合は少なくとも `:596` の一人称と `:603` の 43 を落とす

- **[W-006]** レビューループの経緯が台帳本文に 23 か所埋まっている
  - 場所: `.thread/14/research.md`（8 か所: `:53`, `:78`, `:176`, `:184`, `:186`, `:274`, `:291`, `:334`）/ `.thread/14/research-2.md`（15 か所: `:8`, `:46`, `:65`, `:93`, `:218`, `:283`, `:288`, `:325`, `:328`, `:355`, `:364`, `:428`, `:438`, `:452`, `:601`）
  - 理由: 「計画レビュー R2（arch:P-002）で**訂正した**。旧記載は…これは**誤りだった**」（`research-2.md:218`）、「本台帳は前者だけを採番して**後者を落としていた**」（`:288`）、「当初 `:105` と記録していたが実測は `:103`」（`research.md:274`）、「progress.md は**こちらを見落としている**」（`:277`）— いずれも台帳自身の改訂履歴と、指摘に対する弁明である。`spec/index.md` が正典から「進捗・レビュー記録・日付つき改訂履歴」を排するのと同じ理由で、恒久参照ドキュメントには残さないのが本リポジトリの規律。台帳が読みたいのは「何が乖離していて、どちらが正で、なぜか」だけ
  - 提案: 訂正の結果（現在の正しい記述）だけを残し、「R2 で訂正した / 旧記載は誤りだった / 落としていた」という経緯文を落とす。どのレビューで増えた項目かの由来が要るなら、`plan.md` の AC 由来欄が既にその役を持っている

- **[W-007]** 自己否定したまま並置されている段落
  - 場所: `.thread/14/research.md:301-313`
  - 理由: `:307-311` が「ステップ 1〜20 と同一ファイル・同一節に触れる 3 件は**ステップ 1〜20 では触らない**」と制約を宣言した直後に、`:313` が「統合作業（2026-08-15）で解消済み … この制約は**もう適用されない**」と打ち消している。読者は打ち消された制約を先に読まされる。あわせて `:305` は「受け入れ基準（AC-31 以降）とステップ（22 番以降）は後続の統合作業で**追加する**。本ファイルでは扱わない」と、既に完了した作業を未来形で書いている
  - 提案: `:307-313` を「Issue #2 由来の 3 件（SYNC-244 / 205 / 206・208）は同一ファイルに触れるため、統合後はステップ 2 / 3 が扱う」の 1 文に畳む。`:305` は現在形へ

- **[W-008]** 存在しない見出しへのアンカーが反映先として残っている
  - 場所: `.thread/14/research-2.md:312`, `:566`, `:598`
  - 理由: SYNC-227 の反映先に `spec/platform/index.md#クエリ予算` を挙げているが、この見出しは `55a5bb9` にも HEAD にも存在しない（実物の見出しは `## 行サイズの予算` / `## 実行予算と分割単位`、本 Issue が足したのは `### Scope DO` 配下）。`:598` 自身が「クエリ予算節の実物の書式を確認していない」と認めており、`plan.md` の AC-47 / リスク節が「既にリンク切れ」と正しく検出して `### Scope DO` へ倒している。つまり**台帳だけが死んだアンカーを指したまま**残っている
  - 提案: 3 か所を `spec/platform/index.md` の `### Scope DO` に直す（`spec/adr/056` が正典側の根拠）

- **[W-009]** SYNC-237 の受け皿スライスの一覧が plan と食い違う
  - 場所: `.thread/14/research-2.md:542`
  - 理由: 台帳は送り先を「participant を足す各スライス（**#4 / #5 / 編集・整理スライス**）」とするが、`plan.md:171`（Phase 5 の 7.）は「受け皿が複数スライス（**#3 / #4 / #5 / #7**）に跨る」と書く。Phase 5 の起票内容は AC-60 で参照が列挙される対象なので、どちらのスライス集合を根拠に起票したのかが読めなくなる（件数・分類自体は 7 件で一致しており、齟齬はこの 1 行だけ）
  - 提案: 台帳側を plan の `#3 / #4 / #5 / #7` にそろえるか、台帳は「participant を足す後続スライス群」と抽象化して具体番号を plan に一本化する

- **[W-010]** 凍結前提で書かれた記述が、実際には凍結されていない状態と食い違う
  - 場所: `.thread/14/research.md:343`（および `:151` の同趣旨）
  - 理由: 「`.thread/1/plan.md` は**計画凍結時に削除される**ため、`apps/web/app/start.ts:10` の AC-15 参照は本 Issue で spec への参照に差し替えないと宙に浮く」とあるが、HEAD には `.thread/1/`（`plan.md` / `progress.md` / `steps.md` / `adr.md` / `testing.md`）が丸ごと残っている。AC-16 の差し替え自体は正しく実施済みなので実害は無いが、台帳が根拠として述べた前提が実物と合っていない。同種の陳腐化として `:206`「`NOTE_ROUTE_BATCH_TOO_LARGE` のヒットは `.thread/1/` の作業メモ 3 件のみ」も HEAD では 7 件（`.thread/14/` の 4 件が増えた）
  - 提案: `:343` は「`.thread/1/plan.md` は本 Issue の成果物ではないため、実装コードから受け入れ基準 ID を参照し続けるのは不適切」という理由に置き換える（結論は変わらない）。`:206` は件数を書かず「`spec/` と `packages/` に残っていない（ヒットは `.thread/` の作業メモのみ）」とする

## カバレッジ

- 確認: `.thread/14/research.md`, `.thread/14/research-2.md`
- 突き合わせに使った契約: `.thread/14/plan.md`（前提節 / 受け入れ基準 AC-1〜69 / スコープ節 / テスト方針）

### 抽出検証（判定が下流に影響する項目を優先）

実ファイル・`git show 55a5bb9:` ・`git diff` で裏を取った台帳項目 **42 件**、うち**判定が実物と食い違ったもの 0 件**。

- **要修正 → 実際に反映されているか**（30 件）: SYNC-03 / 11 / 13 / 15 / 16 / 26 / 201 / 202 / 203(1)(2) / 204 / 205 / 207 / 209 / 210 / 211 / 216 / 217 / 222 / 223 / 224 / 226 / 227 / 233 / 234 / 235 / 236 / 238 / 239 / 241 / 242 / 243 / 244 — 全件が HEAD の spec に反映済み。代表例: `IdentityErrorCode` union 14 コード（`spec/domains/identity.md:546-549`）、`beginRelease` と 4 メソッドの非対称（`:361`, `:370-376`）、`assertOwner` の completed 拒否（`spec/domains/index.md:149`）、P-25 の認証ガード例外（`spec/pages/index.md:487`）、`completeOAuthCallback` / `getProfile` / `checkHandleAvailability` の 3 節新設（`spec/usecases/identity.md:284,645,674`、テストケースファイル 24 本）、manual TC-26 / TC-13（`spec/manual-tests/account.md:193,346,347`）
- **スコープ外 → spec が狭められていないか**（8 件）: SYNC-08 / 23 / 24 / 27 / 203(3) / 214 / 231 / 237 — 全件で該当記述が残存を確認。`createDownloadUrl` 3 件 / `workspaceCursor` 系 3 件 / `next_attempt_at` 2 件 / L-02 の `**状態**: 通常 / サインイン済み`（`spec/pages/index.md:103`）/ P-25 の「削除されるもの / されないもの」（`:490`）/ `--shadow-focus` 3 か所 / integration 側 `InvalidProvider` 3 か所（identity 側は 0 件）
- **解消済み・乖離なし → 判定が今も真か**（4 件）: SYNC-21（`pruneExpiredAuthState.md:29` は「次Cronで回復する」）/ SYNC-232（`spec/usecases/identity.md:27,786` に payload 固定の記述あり）/ SYNC-13 / SYNC-216 の compact 部分（`55a5bb9` 時点で既に表にあることを `git show` で確認）— すべて真
- **件数の突き合わせ**: `research.md` 集計（21/1/1/3/1 = 27）、`research-2.md` 集計（38/1/4/1 = 44）、`plan.md` 前提節の統合表（70 = 要修正 59 / スコープ外 8 / 解消済み 2 / 乖離なし 1）は相互に整合。ID 列挙も 3 か所すべてで一致。**不整合はファイル別逆引き表と分類別内訳表の中に閉じている**（W-001 / W-002）
- **ADR 引用の実在確認**: `research.md` の 026 / 027 / 028 / 029 / 033 / 038 / 046 / 048、`research-2.md` の対応表 13 本（034 / 035 / 038 / 039 / 041 / 043 / 044 / 045 / 047 / 048 / 049 / 050 / 051）と 042 — すべて実在し表題も対応。**唯一 `spec/adr/022` だけが別文書を指していた**（B-001）
- **ベースライン照合**: `55a5bb9` は到達可能で、抜き取った台帳アンカー（`spec/domains/identity.md:519-525` / `spec/domains/index.md:121`,`:124` / `spec/inventory/{usecase,test}.md` の最大採番 UC-identity-021・TC-identity-304）は**その commit では全件正しく解決する**。台帳の調査精度そのものに問題は無く、B-002 は as-of の宣言が欠けている点のみ
- **cross-thread アンカー**: `.thread/1/progress.md:58-81`（24 行の spec-sync 対象）と `.thread/2/progress.md:203,204,208,210,242,263,271` はいずれも HEAD で正しく解決する（これらのファイルは本 PR で編集されていないためドリフトしていない）
