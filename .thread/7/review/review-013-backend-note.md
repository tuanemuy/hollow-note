### backend-note

現在のツリーを自分の目で検証した。ラウンド 012 の差分（`7-round-012.diff`）と本ラウンドの差分（`7-round-013.diff`）をファイル単位で突き合わせ、担当範囲で本ラウンドに動いたのは `adapters/conformance/noteRepository.ts`、`application/note/{trashNote.test,restoreNoteRevision,restoreNoteRevision.test,view,deleteNotesForOwner,deleteNotesForOwner.test}`、`spec/domains/{note,index}.md`、`spec/usecases/note.md`、`spec/inventory/test.md` の 11 ファイルであることを確かめたうえで、それらは現在のツリーを全文読み、関連する差分外ファイル（`domain/note/ports/noteRepository.ts`、`cloudflare/sql/session.ts` の overlay、`cloudflare/__tests__/conformanceBackend.ts` の UoW 配線、`application/note/purgeExpiredTrash.ts`、`ports/scopeTaskScheduler.ts` の状態遷移表、`workers/scopeTaskRunner.ts` の catch → `backoff` 経路、`domain/note/note.ts` の `updateBody`、`spec/database/index.md:20`、`spec/inventory/adapter.md:263`、`spec/testcases/note/{trashNote,restoreNoteRevision,emptyTrash}.md`）を突き合わせた。

修正側の報告は次の 3 点を実行で裏取りした。

- **ADP-note-057 の新ケースは契約を主張している。** `backend.scopeUnitOfWork.run` の内側で `save(Note.trash(...))` → `findNextPurgeDeadline()` を引く形で、cloudflare では `session.readRows` の overlay（stored ページは `lifecycle='trashed'` に合致する行が無いので `LIMIT 1` の拒否条件に掛からず、staged 行が `matches` を通って答えに乗る）が成立条件になる。**cloudflare の `findNextPurgeDeadline` を `session.query`（storage のみ）へ書き換えて `--project workers` で走らせたところ、新ケースと cloudflare 固有の staged-reads ケースの 2 本が `expected null to deeply equal 2026-01-31…` で赤になった**（書き換えは検証後に戻した）。memory は書き込みが即時に表へ乗るので契約を自明に満たし、同じスイートが緑
- **TC-note-793 の 2 本は検出力を持つ。** `trashNote.ts:armRetentionSweep` を (A) `dueAt: new Date(0)` に、(B) `deadline = null`（張らない）に書き換えて走らせ、どちらでも 2 本とも赤になることを確認した（書き換えは戻した）。1 本目は「空のゴミ箱へ 1 件」で `scheduledTasks` が `{ kind, operationId, priority, dueAt: view.purgeAfter }` の**ちょうど 1 行**であることを、2 本目は 1 日後に 2 件目を捨てても `dueAt` が 1 件目の `purgeAfter` に留まることを状態で主張している
- **`restoreNoteRevision` の応答は 3 者で一致している。** ポート／DTO: `view.ts:RestoredNoteRevisionView = { noteId, version, title, html }`（JSDoc が第 2 往復を消す理由を持つ）、実装: `restoreNoteRevision.ts:145-150` が `restored.note.title.value` と `processed.html`（`Note.updateBody` → `readyContentFromProcessed` がそのまま `content.html` に置く値）を返す、spec: `spec/usecases/note.md:1032-1034` の出力 DTO が `title: string`, `html: string` と同じ理由文を持つ。TC-note-470 の新テストは `getNote` の読みと `title` / `content.html` の一致、および `onclick` が落ちていること（サニタイズ後であること）を主張しており、消費側 `editor.tsx:1532-1552` は `restored.version` / `restored.title` / `restored.html` の 3 つを使っている

加えて、本ラウンドで担当範囲に紛れていた報告外の変更 — `deleteNotesForOwner.ts:93-99` の JSDoc（fault した turn の行は runner が `backoff` し、`dueAt` が前進して `SCOPE_TASK_MAX_ATTEMPTS` で `failed` に駐車される）と `spec/domains/index.md` の「継続要求」段落の同旨の書き換え — は、`scopeTaskRunner.ts:353-359` の catch → `backOff` と `ports/scopeTaskScheduler.ts:109` の遷移表（`backoff`: `attempt + 1`, `dueAt = now + base × 2^(attempt-1)`, 上限で `failed`; `schedule` が `failed` を戻す）に一致する。テスト「parks the continuation whose carried purges cannot be read」が `attempt: 1` / `state: "pending"` / `dueAt > now` を主張している。`spec/usecases/note.md` の `deleteNotesForOwner` 入力に足された `stuckPurges?` は `deleteNotesForOwner.ts:159` の入力型と一致する。

`spec/domains/note.md:745` の `NoteErrorCode` 列挙に `HtmlTooComplex` が入り、同ファイル `:389` / `:391` と `spec/usecases/note.md:502` / `:1064` は名前形 `HtmlTooComplex` に統一されている（`spec/adr/013` / `spec/domains/storage.md` / `spec/testcases` / `spec/inventory/test.md` は値形 `NOTE_HTML_TOO_COMPLEX` のままだが、これらは翻訳先や送出コードを値で語る文脈で、`errorCode.ts:15` の値と一致する）。`spec/inventory/test.md:1081` / `:1748` の TC-note-111 / 778 は `spec/testcases/note/emptyTrash.md:18` / `:23` の現行文言と一致する。

回帰: `pnpm vitest run --project node` を `application/note` / `domain/note` / `adapters/html` / `memory/__tests__/conformance.test.ts` / `application/cleanup` に対して 23 files / 1092 tests green。`--project workers` の `scopeBusiness.test.ts -t NoteRepository` は 12 tests green。担当範囲のコード・spec の `ADR nnn` 参照（003/006/007/008/009/010/011/012/013/014/017/023/026/033/049/063）はすべて `spec/adr/` に実在する。

#### Blockers

なし

#### Warnings

- **[W-001]** `spec/inventory/test.md` の TC-note-819 行に経緯を語る記述が残っている
  - 場所: `spec/inventory/test.md:2614`（「（ブロックの終端探索が二乗になる経路。**従来は 26 秒焼いてから `RangeError`**）」）
  - 理由: 本 PR が足した行で、写し元の `spec/testcases/note/updateNoteBody.md:48` は「（ブロックの終端探索がそのブロックを読み直すため、深さがそのまま走査を二乗にする）」と現在形で書いており、「従来は」の一節は inventory だけが持つ旧実装の挙動の記録である。`spec/` は経緯を持たない（CLAUDE.md「Design canon」）
  - 提案: 括弧内を testcases の文言に揃える（「従来は 26 秒焼いてから `RangeError`」を落とす）

- **[W-002]** テストが主張している契約のうち、`spec/testcases/` の表に行が無いものが 2 つある（共通原因: テストファイルへケースを足す際に testcases / inventory の行を足していない）
  - 場所:
    - `packages/core/src/application/note/__tests__/trashNote.test.ts:521`（`TC-note-793: the first note into an empty trash arms the sweep at its own purgeAfter`）に対し、`spec/testcases/note/trashNote.md:27` / `spec/inventory/test.md:1763` は「保持期限の違うノートが**複数**ゴミ箱にある」の 1 行だけ
    - `packages/core/src/application/note/__tests__/restoreNoteRevision.test.ts:103`（`TC-note-470: the response carries the same title and body a read would return`）に対し、`spec/testcases/note/restoreNoteRevision.md` に応答 DTO（`title` / `html` が読みと一致し、サニタイズ後の本文であること）の行が無い。`spec/testcases/note/trashNote.md:20-21`（TC-note-812 / 813）が応答の `version` に行を持つのと非対称
  - 理由: 前者は「同一 UoW で反転させた行を答えに含める」契約を usecase 層で唯一直接に主張するケース（複数件の行では `null` → 1 件目の期限、という契約違反の実装でも緑になる — ラウンド 012 [W-002] の指摘そのもの）であり、後者は本ラウンドで spec/usecases に足した出力 DTO の理由文（「呼び出し側は次の保存と画面の描き直しをこの応答だけで行う」）を検証する唯一のケースである。どちらも既存 TC ID に相乗りしているため、`spec/testcases/` から辿ると「その振る舞いは検証されていない」と読める
  - 提案: `spec/testcases/note/trashNote.md` に「空のゴミ箱に 1 件入れる — 保持期限アラームがそのノートの `purgeAfter` に張られる（同じ transaction で反転させた行が手順 5 の答えに含まれる）」、`spec/testcases/note/restoreNoteRevision.md` に「戻した — 応答の `title` / `html` が読み直した姿と一致し、`html` はサニタイズ後の本文である」を足し、`spec/inventory/test.md` に新 TC ID で登録してテスト名を付け替える（既存 ID の相乗りを解消する）

- **[W-003]** `spec/domains/note.md` のポート節の `findNextPurgeDeadline` の記述に「同一 UoW で反転させた行を答えに含める」節が無い
  - 場所: `spec/domains/note.md:439`
  - 理由: この節はポート JSDoc（`packages/core/src/domain/note/ports/noteRepository.ts:37-44`）、`spec/inventory/adapter.md:263`（ADP-note-057）、`spec/database/index.md:20`、`spec/usecases/note.md:783`（`trashNote` 手順 5）の 4 か所が契約として書き、本ラウンドが conformance に実行形を与えたものだが、ドメイン設計側のポート記述だけが「`now` で絞らない」までで止まっている。ポート契約の正典は「ポート定義とその JSDoc」（ADR 026）なので実装との乖離ではないが、`spec/domains/note.md#ポート` だけを読む者には `trashNote` 手順 5 が依存する条件が見えない
  - 提案: `:439` の箇条書きに 1 文足す —「同じ UoW で `active` → `trashed` に反転させたノートも答えに含める（`trashNote` 手順 5。[database/index.md](../database/index.md) の「同一 UoW の読み」(3)）」（inventory の ADP-note-057 と同文でよい）

#### テスト保証

- `NoteRepository.findNextPurgeDeadline` が同一 UoW で `active → trashed` に反転させた行を含める（`adapters/memory/repositories/noteRepository.ts:findNextPurgeDeadline` / `adapters/cloudflare/do/repositories/noteRepository.ts:findNextPurgeDeadline`） — 守っているテスト: `adapters/conformance/noteRepository.ts:ADP-note-057: findNextPurgeDeadline includes a note the same unit of work trashed`（memory / cloudflare 双方で実行）、`cloudflare/__tests__/ports/scopeBusiness.ts:ADP-note-057: findNextPurgeDeadline answers the deadline of a note trashed in the same unit of work`。storage のみを読む実装へ書き換えて 2 本とも赤になることを確認
- `NoteRepository.findNextPurgeDeadline` が `now` で絞らず最小の `purgeAfter` を返し、空なら `null`（同上） — 守っているテスト: `adapters/conformance/noteRepository.ts:ADP-note-057: findNextPurgeDeadline answers the earliest purgeAfter, whether or not it has arrived`
- `trashNote` 手順 5: 同じ transaction で期限を引き直して保持期限 task を upsert する（`application/note/trashNote.ts:armRetentionSweep`） — 守っているテスト: `application/note/__tests__/trashNote.test.ts:TC-note-793: the first note into an empty trash arms the sweep at its own purgeAfter`、同 `TC-note-793: a later note joining the trash leaves the sweep on the earliest purgeAfter`。`dueAt` の固定化・張らない、の 2 変異で 2 本とも赤 → testcases 側の行の欠落は [W-002]
- `purgeExpiredTrash` が期限の来た対象が無いとき `findNextPurgeDeadline` へ task を移し、空のときだけ完了する（`application/note/purgeExpiredTrash.ts:settle`） — 守っているテスト: `application/note/__tests__/purgeExpiredTrash.test.ts:TC-note-792`（本ラウンド差分なし・回帰実行で緑）
- `restoreNoteRevision` の応答が `version` / `title` / `html` を運び、`html` はサニタイズ後の本文で、読み直した姿と一致する（`application/note/restoreNoteRevision.ts:restoreNoteRevision`、`application/note/view.ts:RestoredNoteRevisionView`） — 守っているテスト: `application/note/__tests__/restoreNoteRevision.test.ts:TC-note-470: the response carries the same title and body a read would return`（`getNote` と比較、`onclick` の不在を主張）→ testcases 側の行の欠落は [W-002]
- `restoreNoteRevision` の `version` が本文・スタイル・タイトルの合成後の版である（同上） — 守っているテスト: `restoreNoteRevision.test.ts:TC-note-470: restores the revision's body, title and style mode`（`view.version === note.version`）、`TC-note-470: the restore emits the content, style and title events together`
- `restoreNoteRevision` が版の HTML を `HtmlProcessor.process` に通してから保存する（同上） — 守っているテスト: `restoreNoteRevision.test.ts:TC-note-478`、`TC-note-479`
- `deleteNotesForOwner` が読めない `stuckPurges` で `SystemError(DataIntegrityError)` を投げ、runner が行を `backoff` する（`application/note/deleteNotesForOwner.ts:readOwnerPurgeTurn`、`application/workers/scopeTaskRunner.ts:backOff`） — 守っているテスト: `application/note/__tests__/deleteNotesForOwner.test.ts:faults on a carried purge it cannot read, rather than dropping the entry`、同 `parks the continuation whose carried purges cannot be read, instead of acknowledging an empty scope`（`attempt: 1` / `pending` / `dueAt > now` を主張）
- `HtmlProcessor` のサニタイズ・資源上限・`editTextNodes` / `inlineStylesheets`、`emptyTrash` / `purgeNote` / `applyTextNodeEdits` / `updateNoteBody` / `renameNote` / `changeNoteStyleMode` / `listTrashedNotes` / `listNoteRevisions` / `restoreNote` の各契約 — ラウンド 012 から差分なし。本ラウンドは回帰実行（23 files / 1092 tests green）で確認

#### カバレッジ

- 確認: `packages/core/src/adapters/conformance/noteRepository.ts`, `packages/core/src/adapters/memory/repositories/noteRepository.ts`, `packages/core/src/adapters/cloudflare/do/repositories/noteRepository.ts`, `packages/core/src/adapters/cloudflare/__tests__/conformanceBackend.ts`（scope UoW の配線のみ）, `packages/core/src/application/note/trashNote.ts`, `packages/core/src/application/note/__tests__/trashNote.test.ts`, `packages/core/src/application/note/purgeExpiredTrash.ts`, `packages/core/src/application/note/restoreNoteRevision.ts`, `packages/core/src/application/note/__tests__/restoreNoteRevision.test.ts`, `packages/core/src/application/note/view.ts`, `packages/core/src/application/note/deleteNotesForOwner.ts`, `packages/core/src/application/note/__tests__/deleteNotesForOwner.test.ts`, `packages/core/src/application/workers/scopeTaskRunner.ts`（catch → `backOff` の経路のみ）, `packages/core/src/domain/note/ports/noteRepository.ts`, `packages/core/src/domain/note/errorCode.ts`, `spec/domains/note.md`, `spec/domains/index.md`（「継続要求」段落のみ）, `spec/usecases/note.md`（`restoreNoteRevision` / `trashNote` 手順 5 / `deleteNotesForOwner` 入力）, `spec/testcases/note/restoreNoteRevision.md`, `spec/testcases/note/trashNote.md`, `spec/testcases/note/emptyTrash.md`, `spec/testcases/note/updateNoteBody.md`（:48 のみ）, `spec/inventory/test.md`, `spec/inventory/usecase.md`, `spec/inventory/adapter.md`, `spec/adr/013-html-sanitization-policy.md`（`HtmlTooComplex` の表記のみ）, `apps/web/app/routes/notes/-action.tsx`（`restoreNoteRevisionFn` のみ）, `apps/web/app/components/note/NoteEditor/editor.tsx`（`restore` の応答消費箇所のみ）
- スキップ: `packages/core/src/domain/note/{__tests__/noteAccessPolicy.test.ts,__tests__/urlPolicy.test.ts,noteRevision.ts,ports/htmlProcessor.ts,services/noteAccessPolicy.ts,services/urlPolicy.ts}` — ラウンド 012 から差分なし。ラウンド 012 でゼロベース検証済み、本ラウンドは回帰実行のみ
- スキップ: `packages/core/src/application/note/{applyTextNodeEdits,changeNoteStyleMode,createBlankNote,editing,emptyTrash,getNote,jobs,listNoteRevisions,listNotes,listTrashedNotes,moveNote,purgeNote,renameNote,restoreNote,updateNoteBody}.ts` と `__tests__/{applyTextNodeEdits,changeNoteStyleMode,editingHarness,emptyTrash,getNote,listNoteRevisions,listNotes,listTrashedNotes,purgeExpiredTrash,purgeNote,renameNote,restoreNote,updateNoteBody}.test.ts` — 同上（差分なし・回帰 green）
- スキップ: `packages/core/src/adapters/html/**`, `packages/core/src/application/cleanup/notePurgeFanOut.ts`, `packages/core/src/adapters/conformance/noteRouteStore.ts`, `spec/testcases/note/{applyTextNodeEdits,changeNoteStyleMode,deleteNotesForOwner,emptyTrash（:18/:23 以外）,getExportStatus,getNote,listNotes,listTrashedNotes,purgeExpiredTrash,purgeNote,renameNote,restoreNote}.md` — 同上
- スキップ: `apps/web/**`（上記 2 ファイルの応答消費箇所を除く）, `apps/web/app/routeTree.gen.ts`, `apps/web/app/__tests__/adrReference.test.ts` — frontend / general 担当
- スキップ: `packages/core/src/application/{storage,tag,integration,usage,identity}/**`, `packages/core/src/application/cleanup/{participants,personalCleanup}.ts`, `packages/core/src/application/di/**`, `packages/core/src/application/execution/unitOfWork.ts`, `packages/core/src/application/ports/**`, `packages/core/src/application/scope.ts`, `packages/core/src/application/workers/{subscribers.ts,__tests__/subscribers.test.ts}`, `packages/core/src/domain/{integration,storage,tag}/**`, `packages/core/src/adapters/cloudflare/**`（noteRepository と conformanceBackend の UoW 配線以外）, `packages/core/src/adapters/memory/**`（noteRepository 以外）, `packages/core/src/adapters/conformance/{backend,backupRecordRepository,storedFileRepository,tagAssignmentRepository}.ts`, `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`, `packages/core/package.json`, `pnpm-lock.yaml` — backend-storage / backend-tag / general 担当
- スキップ: `spec/{database,pages,platform,presentation}/index.md`, `spec/domains/{integration,storage,tag}.md`, `spec/manual-tests/editing.md`, `spec/testcases/{integration,storage,tag}/**`, `spec/usecases/{identity,integration,job,storage,tag,usage}.md`, `docs/**` — 他担当（`spec/database/index.md:20` と `spec/domains/index.md` の「継続要求」段落は本ラウンドの照合のため差分外として参照）
