### backend-tag

前提の検証（今ラウンドの焦点）: `spec/domains/index.md` の継続要求表を全 39 行、対応する `spec/usecases/{tag,usage,job,identity,integration,storage,note}.md` の payload 表記、実際に `scopeTaskScheduler.schedule` を呼ぶ全 15 箇所（`trashNote` / `deleteNotesForOwner` ×2 / `purgeExpiredTrash` ×2 / `deleteQuota` / `deleteFilesByOwner` / `collectOrphanMedia` ×3 / `personalCleanup` ×2 / `scopeTaskRunner` / `notePurgeFanOut` / `membershipMutation` / `workspaceDeletion` ×3）を突き合わせた。

- scope Alarm 行に `scope` を payload へ積んでいる実装は 0 件。前ラウンドで直した 5 行（`identity.personalBarrierPruneContinued` / `usage.userCleanupContinued` / `tag.deleteUnusedContinued` / `job.removalManifestCompactContinued` / `integration.userCleanupContinued`）はいずれも表・usecases・実装（実装があるものは `personalCleanup.ts:67-70,110-113` / `deleteQuota.ts:80`）が一致する
- 本 PR が新設した scope 行 4 本（`note.trashExpiryContinued {}` / `storage.orphanMediaContinued { afterCreatedAt, afterId }` / `tag.noteDeleteContinued` / `integration.noteDeleteContinued` / `storage.noteDeleteContinued` `{ noteId, deletionOperationId }`、`note.ownerPurgeContinued { deletionOperationId, stuckPurges? }`）は、定数 `CURSOR_CREATED_AT = "afterCreatedAt"` / `CURSOR_ID = "afterId"`（`collectOrphanMedia.ts:149-150`）、`STUCK_PURGES = "stuckPurges"`（`deleteNotesForOwner.ts:73`）、`settleNotePurgeTurn` の payload（`notePurgeFanOut.ts:178-181`）まで表と一致する
- global 行のうち実装があるもの（`identity.*` 5 本、`global.maintenanceRunPruneContinued`）は payload に宛先 scope を要しない global 作業で、規則の第 2 文と矛盾しない。`integration.userCleanupContinued` の「global Queue 側は scope を載せない」注記は `spec/usecases/integration.md:300` の「どちらの分岐かは届いた経路が決める」と整合する
- `job.terminationContinued { origin }`（`trashNote.ts:164-178`）の `origin` は `JobTerminationOrigin`（`spec/usecases/job.md:106-115`）の `trashNote` 枝そのもので、`scope` を含まない
- `tag.noteDeleteContinued` の priority（token 無し 3 / 削除由来 0）は `spec/usecases/tag.md` 手順 2 と `notePurgeFanOut.ts:173-176` が一致し、`deleteAssignmentsForNote.test.ts` の TC-tag-027 ×2 が両クラスを検証している
- `pnpm typecheck` 通過。node project（tag / workers / domain tag / memory conformance / identity cleanup / integration / storage deleteFilesForNote）508 件と workers project（cloudflare `scopeGuards` + `conformance/scopeBusiness`）55 件を実行して green

#### Blockers

なし

#### Warnings

- **[W-001]** `scheduled_tasks.operation_id` の導出規則が、本 PR が新設した scope 唯一の周期行 2 本と食い違う
  - 場所: `spec/database/index.md:39`（「continuationは生成元IDとcursorを材料にする」）、`spec/domains/index.md` 継続要求節の規約段落（「`operation_id`は生成元operation/event/command IDと対象ID・version・cursorから決定的に導出する」）
  - 理由: `storage.orphanMediaContinued` は固定の `operationId = "storage.orphanMediaSweep"`（`collectOrphanMedia.ts:43`）の 1 行を `(kind, operationId)` で upsert し、keyset 位置は id ではなく payload で運ぶ。`note.trashExpiryContinued` も固定の `"note.trashExpiry"`（`purgeExpiredTrash.ts:22`）である。この形は `spec/usecases/storage.md` の collectOrphanMedia 節が「位置を task の payload に置くのは、payload が `(kind, operationId)` の upsert で掃引行そのものと一緒に書かれ、位置の寿命が掃引行の寿命と一致するため」と意図的に選んでおり、`workspace.deletionLocalContinued` の「同じtaskを次時刻へupsert」（`spec/database/index.md` scheduled_tasks 節）にも先例がある。しかし上の 2 箇所の規則は「cursor を鍵の材料にする」と無条件に書いており、cursor を payload に置く周期行を読んだ人がどちらを正とすべきか決められない。本 PR の表は payload 欄に `afterCreatedAt, afterId` を書いた（＝鍵ではない）ので、規則側だけが取り残されている
  - 提案: `spec/domains/index.md` の規約段落（または `spec/database/index.md:39`）に 1 文足す — 「scope に 1 本しか無い周期の掃引行（`note.trashExpiryContinued` / `storage.orphanMediaContinued`）と、cursor を持つ workspace deletion 行は、固定の `(kind, operationId)` へ upsert し、再開位置は payload で運ぶ。決定性は行が 1 本であることで担保する」。実装は変えない

- **[W-002]** 継続要求表に `workspace.membershipRemovalEdgeContinued` の行が無い — **既存の問題（本PRの変更起因ではない）**
  - 場所: `spec/domains/index.md` 継続要求表、`packages/core/src/application/workspace/membershipMutation.ts:251,287-292`、`spec/usecases/workspace.md:739,775`
  - 理由: 表は「scope task は `scheduled_tasks` と Alarm で運ぶ。どちらも購読者は1つだけ」の全列挙として置かれており、`scopeTaskHandlers` が持つ 14 kind のうちこの 1 本だけが表に無い（main の表にも無く、Issue #3 の取りこぼし）。今ラウンドの「表の全行を規則に照らす」検証は、表に無い行には届かない
  - 提案: `| \`workspace.membershipRemovalEdgeContinued\` | \`{ memberUserId }\` | workspace scope Alarm → [\`removeMember\` / \`leaveWorkspace\`](../usecases/workspace.md) の directory edge 削除 |` を `workspace.deletionManifestCompactContinued` の下に足す。本 PR で直す義務は無いが、表を書き換えたのは本 PR なので同じ commit で足すのが安い

- **[W-003]** `identity.personalBarrierPruneContinued` の payload `asOf` は積まれるだけで読まれない — **既存の問題（本PRの変更起因ではない）**
  - 場所: `packages/core/src/application/workers/scopeTaskRunner.ts`（`[PERSONAL_BARRIER_PRUNE_TASK_KIND]` handler: `asOf: container.clock.now()`）、`packages/core/src/application/cleanup/personalCleanup.ts:67-70,110-113`、`spec/usecases/identity.md:835`（「prune分岐は固定`asOf`で…100件なら同じscope Alarm taskを再登録する」）
  - 理由: 本 PR は表のこの行を実装に合わせて `{ deletionOperationId, asOf }` に書き換えたので、表を読んだ人は `asOf` が再登録の固定 deadline として効くと理解する。実際は handler が turn ごとの `clock.now()` を渡し、payload の `asOf` は書きっぱなしである。1 scope の receipt は 1 件で満ページの枝は到達不能（`personalCleanup.ts` の JSDoc が明記）なので実害は無いが、表・usecases・実装の 3 者一致という今ラウンドの基準ではこの 1 行だけ実装が spec の「固定 `asOf`」に従っていない
  - 提案: handler で `asOf` を payload から読む（`readPersonalBarrierPruneTurn` を `notePurgeFanOut.ts` の `readNotePurgeTurn` と同じ形で置き、読めなければ `clock.now()` に落とす）か、payload から `asOf` を落として表を `{ deletionOperationId }` にする。どちらでもよいが、片方に揃える

- **[W-004]** spec に修正の経緯が残っている — **既存の問題（本PRの変更起因ではない）**
  - 場所: `spec/usecases/note.md` deleteNotesForOwner 節「**継続の媒体を専用の要求にした理由**。以前は受け取ったのと同じ `identity.user.deleted` / `workspace.deleted` を再投入して継続していたが、…」
  - 理由: CLAUDE.md は `spec/` を「progress logs や superseded judgements を持たない」canon と定めており、「以前は」で始まる段落は superseded judgement そのものである。同じ理由は `spec/domains/index.md` の継続要求節「購読者を 1 つに保つ。既存のイベントを再投入して継続する形は採らない（購読者の数だけコピーが増え…）」に現在形で既に書かれている
  - 提案: 段落を「継続の媒体はジョブではなく専用の要求にする。理由は [domains/index.md](../domains/index.md) の「継続要求」」の 1 文に畳む。`deleteFilesByOwner` との「違いは解消した」の 1 文も落とす

#### テスト保証

- `note.purged` 追随者 3 本の登録（`application/workers/subscribers.ts:domainEventSubscribers`） — 守っているテスト: `application/workers/__tests__/subscribers.test.ts:registers one subscriber per follower, so no leg of the purge is acknowledged with a warning`
- 追随者が `scopeOfNoteOwner` で workspace scope へ届き personal scope を触らない（`application/scope.ts:scopeOfNoteOwner` / `subscribers.ts`） — 守っているテスト: `subscribers.test.ts:TC-integration-022: reclaims a workspace-owned note's residue in the workspace scope, leaving the personal scope untouched`
- 兄弟購読者の 1 つが投げても残りは走り、最初の失敗を再送出する（`subscribers.ts:dispatchDomainEvent`） — 守っているテスト: `subscribers.test.ts:runs the siblings of a failing subscriber and still fails the delivery`、`subscribers.test.ts:runs the global half of the cleanup wave even when the personal half fails first, and still fails the delivery`
- 再配送で 3 追随者とも no-op、outbox が増えない（`deleteAssignmentsForNote` / `deleteFilesForNote` / `deleteBackupRecordsForNote`） — 守っているテスト: `subscribers.test.ts:clears the files, assignments and backup records of the purged note through the default registry, and stays a no-op on redelivery`
- 200 件ずつ `tag.noteDeleteContinued` で再開し、payload `{ noteId, deletionOperationId }`・priority 0（削除由来）で行を書く（`application/tag/deleteAssignmentsForNote.ts` / `cleanup/notePurgeFanOut.ts:settleNotePurgeTurn`） — 守っているテスト: `application/tag/__tests__/deleteAssignmentsForNote.test.ts:TC-tag-027: reclaims 450 assignments 200 at a time, re-checking the deletion owner every turn`
- token 無しの purge は priority 3 で再登録され、runner の `scopeTaskHandlers` が同じ kind を再開して行を `complete` する（`scopeTaskRunner.ts:[NOTE_ASSIGNMENT_DELETE_TASK_KIND]`） — 守っているテスト: `deleteAssignmentsForNote.test.ts:TC-tag-027: has its continuation resumed by the scope-task runner, so an unregistered kind cannot strand the rest`
- 各 turn の deletion owner 再確認: abort 済み・不在は `ConflictError("CLEANUP_OPERATION_MISMATCH")`、`completed` は通す（`notePurgeFanOut.ts:assertNotePurgeAdmission`） — 守っているテスト: `deleteAssignmentsForNote.test.ts:TC-tag-027`（abort 後の拒否）、`TC-tag-028: refuses a token no barrier ever issued…`、`TC-tag-028: still reclaims the rows once the deletion barrier it inherited has completed`
- 短ページの turn が継続行を `complete` する（`notePurgeFanOut.ts:settleNotePurgeTurn`） — 守っているテスト: `deleteAssignmentsForNote.test.ts:TC-tag-027`（3 turn 目で `tasks(h)` が `[]`）、`TC-tag-029`（付与 0 件で行を残さない）
- 付与以外の表を触らない（`deleteAssignmentsForNote.ts`） — 守っているテスト: `deleteAssignmentsForNote.test.ts:TC-tag-024: touches nothing but the assignments`（scope store 全表の件数差分が `tagAssignments` のみ）
- 書き込み失敗を `SystemError(DatabaseError)` のまま通し行を残す（`deleteAssignmentsForNote.ts`） — 守っているテスト: `deleteAssignmentsForNote.test.ts:TC-tag-032`
- 壊れた payload（noteId 無し・空白・非文字列 token）を `SystemError(DataIntegrityError)` で拒み、token 無し／空文字は `null` に読む（`notePurgeFanOut.ts:readNotePurgeTurn`） — 守っているテスト: `subscribers.test.ts:readNotePurgeTurn` の 2 ケース
- `TagAssignmentRepository.insert` の 2 種の一意制約を別エラーへ写す（memory / cloudflare） — 守っているテスト: `adapters/conformance/tagAssignmentRepository.ts:ADP-tag-010`（pair → `ASSIGNMENT_ALREADY_EXISTS`、id 再利用 → `SystemError`）を両バックエンドが実行
- `listByNote` の `AssignmentId` 昇順と `deleteByNote` の「先頭から `limit` 件・`limit <= 0` は 0」（両バックエンド） — 守っているテスト: `conformance/tagAssignmentRepository.ts:ADP-tag-012` / `ADP-tag-019`（挿入順と逆に入れて昇順を確認、部分削除後の残りを比較）
- scope 鍵 `scope_type / scope_id` の pin 突き合わせ: 保存（memory / cloudflare）・復元・有界削除（cloudflare。交差行があれば 1 件も消さない） — 守っているテスト: `adapters/memory/__tests__/scopeGuards.test.ts:ADP-tag-010`、`adapters/cloudflare/__tests__/scopeGuards.test.ts:ADP-tag-010 / ADP-tag-012 / ADP-tag-019` ×5
- 全 conformance スイートを両バックエンドが import している（`PERSISTENCE_SUITES = 45`） — 守っているテスト: `adapters/__tests__/conformanceCoverage.test.ts`
- `note` 成分が personal cleanup の必須 component になり、3 成分の ack で barrier が閉じる（`cleanup/participants.ts` / `identity/deleteAccount/cleanupDispatch.ts`） — 守っているテスト: `application/identity/__tests__/deleteAccount.cleanup.test.ts:TC-identity-086`（`["note","storage","usage"]` を明示比較）、`deleteAccount.terminalPrune.test.ts`（`REQUIRED_PERSONAL_CLEANUP_COMPONENTS` で ack）
- `identity.personalBarrierPruneContinued` の payload `asOf` が turn の deadline として効くこと — 守られていない → [W-003]（既存）
- `workspace.membershipRemovalEdgeContinued` が継続要求表の規則に従うこと — 表に行が無いため照合不能 → [W-002]（既存）

#### カバレッジ

- 確認: `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`, `packages/core/src/adapters/cloudflare/__tests__/conformance/scopeBusiness.test.ts`, `packages/core/src/adapters/cloudflare/__tests__/conformanceBackend.ts`, `packages/core/src/adapters/cloudflare/__tests__/deleteFilesByOwner.test.ts`, `packages/core/src/adapters/cloudflare/__tests__/ports/scopeBusiness.ts`, `packages/core/src/adapters/cloudflare/__tests__/runtimeComposition.test.ts`, `packages/core/src/adapters/cloudflare/__tests__/scopeGuards.test.ts`, `packages/core/src/adapters/cloudflare/do/repositories/noteRepository.ts`（`findNextPurgeDeadline` — conformance 追加分の突き合わせのみ）, `packages/core/src/adapters/cloudflare/do/repositories/tagAssignmentRepository.ts`, `packages/core/src/adapters/cloudflare/do/schema.ts`, `packages/core/src/adapters/conformance/backend.ts`, `packages/core/src/adapters/conformance/backupRecordRepository.ts`, `packages/core/src/adapters/conformance/noteRepository.ts`, `packages/core/src/adapters/conformance/noteRouteStore.ts`, `packages/core/src/adapters/conformance/storedFileRepository.ts`, `packages/core/src/adapters/conformance/tagAssignmentRepository.ts`, `packages/core/src/adapters/memory/__tests__/conformance.test.ts`, `packages/core/src/adapters/memory/__tests__/conformanceBackend.ts`, `packages/core/src/adapters/memory/__tests__/scopeGuards.test.ts`, `packages/core/src/adapters/memory/repositories/backupRecordRepository.ts`, `packages/core/src/adapters/memory/repositories/noteRepository.ts`, `packages/core/src/adapters/memory/repositories/storedFileRepository.ts`, `packages/core/src/adapters/memory/repositories/tagAssignmentRepository.ts`, `packages/core/src/adapters/memory/scopeUnitOfWork.ts`, `packages/core/src/adapters/memory/store.ts`, `packages/core/src/application/cleanup/notePurgeFanOut.ts`, `packages/core/src/application/cleanup/participants.ts`, `packages/core/src/application/di/cloudflareRuntime.ts`, `packages/core/src/application/di/memoryRuntime.ts`, `packages/core/src/application/di/types.ts`, `packages/core/src/application/execution/unitOfWork.ts`, `packages/core/src/application/identity/__tests__/deleteAccount.cleanup.test.ts`, `packages/core/src/application/identity/__tests__/deleteAccount.terminalPrune.test.ts`, `packages/core/src/application/identity/deleteAccount/authorRedaction.ts`, `packages/core/src/application/identity/deleteAccount/cleanupDispatch.ts`, `packages/core/src/application/scope.ts`, `packages/core/src/application/tag/__tests__/deleteAssignmentsForNote.test.ts`, `packages/core/src/application/tag/deleteAssignmentsForNote.ts`, `packages/core/src/application/workers/__tests__/subscribers.test.ts`, `packages/core/src/application/workers/scopeTaskRunner.ts`, `packages/core/src/application/workers/subscribers.ts`, `packages/core/src/domain/tag/__tests__/valueObject.test.ts`, `packages/core/src/domain/tag/errorCode.ts`, `packages/core/src/domain/tag/ports/tagAssignmentRepository.ts`, `packages/core/src/domain/tag/tagAssignment.ts`, `packages/core/src/domain/tag/valueObject.ts`, `spec/database/index.md`, `spec/domains/index.md`, `spec/domains/tag.md`, `spec/inventory/adapter.md`, `spec/inventory/test.md`（TC-tag / TC-integration-016〜027 の行）, `spec/platform/index.md`, `spec/testcases/tag/deleteAssignmentsForNote.md`, `spec/usecases/tag.md`, `spec/usecases/usage.md`, `spec/usecases/job.md`, `spec/usecases/integration.md`（継続要求 payload に関わる差分）, `spec/usecases/storage.md`（継続要求 payload に関わる差分）, `docs/backend_implementation_example.md`
- 差分外で参照: `packages/core/src/application/storage/deleteFilesForNote.ts`, `packages/core/src/application/integration/deleteBackupRecordsForNote.ts`, `packages/core/src/application/note/deleteNotesForOwner.ts`（payload 生成部）, `packages/core/src/application/note/purgeExpiredTrash.ts`（定数）, `packages/core/src/application/note/trashNote.ts`（`job.terminationContinued`）, `packages/core/src/application/storage/collectOrphanMedia.ts`（payload 生成部）, `packages/core/src/application/cleanup/personalCleanup.ts`, `packages/core/src/application/identity/deleteAccount/input.ts`, `packages/core/src/application/note/jobs.ts`, `packages/core/src/adapters/memory/repositories/scopeCleanupAdmissionStore.ts`, `packages/core/src/adapters/memory/repositories/scopeTaskScheduler.ts`, `packages/core/src/adapters/cloudflare/d1/repositories/noteRouteStore.ts`（`beginPurge` / `finishPurge` の文数）, `spec/usecases/note.md`（deleteNotesForOwner / purgeExpiredTrash 節）, `spec/usecases/identity.md`, `spec/usecases/workspace.md`
- スキップ: `apps/web/**` — フロントエンド担当外; `packages/core/src/adapters/html/**` — HtmlProcessor は note ドメイン担当; `packages/core/src/adapters/cloudflare/do/repositories/{backupRecordRepository,storedFileRepository}.ts` — integration / storage ドメインのアダプター実装（conformance スイート側だけ確認）; `packages/core/src/application/note/**`, `packages/core/src/application/storage/**`（上記の参照分を除く）, `packages/core/src/application/integration/__tests__/**`, `packages/core/src/application/usage/**`, `packages/core/src/application/ports/**` — note / storage / usage 担当; `packages/core/src/domain/{note,storage,integration}/**` — 各ドメイン担当; `spec/adr/013-html-sanitization-policy.md`, `spec/domains/{note,storage,integration}.md`, `spec/testcases/{note,storage,integration}/**`, `spec/usecases/note.md`（上記節以外）, `spec/inventory/usecase.md`, `spec/manual-tests/editing.md`, `spec/pages/index.md`, `spec/presentation/index.md` — 各ドメイン / フロント担当; `packages/core/package.json`, `pnpm-lock.yaml`, `apps/web/app/routeTree.gen.ts` — 生成物 / 依存追加
