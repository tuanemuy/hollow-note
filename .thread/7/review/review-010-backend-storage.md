### backend-storage

ゼロベースで現在のツリーを検証した。前ラウンドの 5 変更（`escapeAttributeLt`・`ORPHAN_MEDIA_BODY_BUDGET_CHARS`・`listByNote` の先送り明記・TC-storage-060 の `CLEANUP_OPERATION_MISMATCH` 固定・30 日境界の記述）はいずれも実装・テスト・spec の三者が一致しており、実測でも裏が取れた。

実測したこと:

- `escapeAttributeLt` の往復: `createHtmlProcessor().process` の出力に対し `&nbsp;`→`&#160;` と属性値の `<`→`&lt;` を当てて `readSvgDocument` に通した。属性値中の `<`/`>`/`&quot;`/`&amp;`/アポストロフィ、テキスト中の `"` の後に続く属性値の `<`、`<desc>` 配下のテキスト、CDATA 由来のテキスト、`style` 属性の後の `data-*` の 8 形すべてで `wellformed: true`、テキスト側の `&lt;` は無傷、タグ区切りの `<` にも触れていない（直列化はどの属性値も `"` で囲み、値中の `"` は `&quot;`、テキストの `<`/`>` は必ずエスケープするので、`"` だけを区切りに読む走査で十分であることを確認）。
- `ORPHAN_MEDIA_BODY_BUDGET_CHARS` の拘束: 判定は「次のノートを読む前」に `charsHeld >= 16,000,000` で行われ、保持量 0 から始まるので 1 turn は必ず 1 ノートを読む。最悪保持量は予算 + 1 ノート（`NoteRevision.RETENTION = 20` + 現在本文 = 21 × 800,000 = 16,800,000）= 32,800,000 code unit で、JSDoc / spec の「≒ 33,000,000 文字・広い表現で 66 MB・isolate 128 MB の内側」と一致する。`html.length` は UTF-16 単位で `NoteHtml` の 800,000 バイト以下なので上界としても正しい。予算切れで `break` する前に `judgedThrough` は直前のファイルまで進んでおり、次 turn はそのノートを先頭で読むので、予算が原因で永久に前へ進まない状態は作れない。`judgedThrough === null` で `outOfBudget` になる経路も無い（先頭ファイルでは両予算とも 0）。
- 対象スイート（application/storage・integration・usage、domain/storage・integration、memory conformance、subscribers、`storage.delivery`）17 ファイル 674 件が green。

#### Blockers

なし

#### Warnings

- **[W-001]** 修正の経緯を語るコメント・spec 記述が残っている（記述の衛生。共通原因は「導出を降ろした」判断の説明を、決定の内容ではなく決定に至った回数・以前の版として書いていること）
  - 場所:
    - `packages/core/src/application/storage/storeMedia.ts:237-245`（`processSvg` JSDoc「Every earlier version of this boundary translated one code and rested the rest on a derivation — first about …, then about …. Each was refuted by measurement」）
    - `packages/core/src/domain/storage/services/uploadValidationPolicy.ts:34-43`（`MEDIA_SVG_MAX_BYTES` JSDoc「Four attempts to derive a promise from this number have each been refuted by measurement」）
    - `packages/core/src/application/storage/collectOrphanMedia.ts:69-72`（`ORPHAN_MEDIA_NOTE_BUDGET` JSDoc「the same order as the one-parse-per-candidate turn this sweep costed before the bodies of the retained revisions joined the reference set」）
    - `spec/usecases/storage.md:150`（「その種の論証は 128 KB について繰り返し実測に否定されている」）、`spec/usecases/storage.md:425`（「版を数えるようになる前の『1 候補につき 1 解析』と同じ桁」）、`spec/domains/storage.md:165`（「この導出が担っていた約束」）
    - `packages/core/src/application/workers/subscribers.ts:247`（「the ordering the continuation subscribers of one type used to inherit from registration order」— 担当境界上だが `note.purged` fan-out の登録と同じ JSDoc なので併記）
  - 理由: CLAUDE.md の「Default to no comments. Add one only when the WHY is non-obvious」と `spec/` の「progress logs や superseded judgements を持たない」規約に対し、これらは「何回否定されたか」「以前はどうだったか」という経緯であって、現在の姿の理由ではない。実測値（786,073 バイト・86 倍など）と「メーターはエスケープ前の長さを課金する」という事実は why-not として残す価値があるが、「earlier version」「attempts」「〜になる前」「繰り返し否定」という時系列の枠は読み手に履歴を要求する。ラウンド 008 で決着した設計判断そのもの（導出を canon から降ろす）には触れていない — 指摘は表現だけである
  - 提案: 各箇所を「事実 + 理由」の断定形に書き直す。例: `processSvg`「The set is covered, not argued unreachable: the meter charges a node's pre-escape length, so 131,072 raw `"` in a single-quoted value serialize to 786,073 bytes, and no bound on the input shape survives that.」／`MEDIA_SVG_MAX_BYTES`「Nothing is derived from this number. A well-formed input does not bound the output (131,064 → 11,300,523 bytes), refusing element names does not (…), and `maxExpansionFactor` × 131,072 < 800,000 does not (…)」／`ORPHAN_MEDIA_NOTE_BUDGET`「Five notes hold the worst case to 105 parses, the same order as one parse per candidate」／spec 側も同様に「〜は偽である（実測: …）」の形へ。`subscribers.ts` は「the dispatcher does not order the continuation subscribers of one type; each must be independent」と現在形にする。`comment-cleanup` / `doc-style` の範囲で済み、挙動は変わらない

#### テスト保証

- `storeMedia.ts:escapeAttributeLt` / `asStandaloneSvg`（属性値の `<` を `&lt;` へ、テキストの `&lt;` は無傷、`&nbsp;`→`&#160;`、`xmlns` / `xmlns:xlink` の復元）— 守っているテスト: `storeMedia.test.ts:TC-storage-275`, `TC-storage-262`, `TC-storage-256`（`</svg>` 後の残余）, `TC-storage-261`（XML の `S` 以外の空白）
- `storeMedia.ts:processSvg`（`HTML_PROCESSOR_NOTE_ERROR_CODES` 全集合を `FileTooLarge` へ翻訳）— 守っているテスト: `storeMedia.test.ts:TC-storage-272`（`NOTE_HTML_TOO_COMPLEX`）, `TC-storage-274`（`NOTE_CONTENT_TOO_LARGE`）, `TC-storage-251`, `TC-storage-273`
- `storeMedia.ts:storeMedia`（サニタイズ後の実バイト長で上限・容量を測る）— 守っているテスト: `storeMedia.test.ts:TC-storage-252`, `TC-storage-253`, `TC-storage-271`（縮む側・膨らむ側の両方）
- `storeMedia.ts:resolveEditableNote`（`NoteIsTrashed` / `NOTE_NOT_FOUND` の畳み込み）— 守っているテスト: `storeMedia.test.ts:TC-storage-260`, `TC-storage-184`, `TC-storage-185`, `TC-storage-187`
- `storeMedia.ts:storeMedia`（transaction 失敗時のオブジェクト巻き戻し）— 守っているテスト: `storeMedia.test.ts:"rolls the stored object back when the transaction fails"`
- `uploadValidationPolicy.ts:readSvgDocument` / `identifySvg`（整形式性・深さ 64・breakout tag・上限超過時は走査を省く）— 守っているテスト: `storeMedia.test.ts:TC-storage-265/266/267/269/270/264/268`, `storage.test.ts:TC-storage-269`, `TC-storage-181 / 180`（128 KB）
- `collectOrphanMedia.ts:sweepOnce`（文字数予算で次ノートの読み込み前に打ち切り、判定済み位置から継続）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-276`（現在本文 + 19 版 = 16,000,000 文字ちょうどで境界を踏み、2 turn 目が残りを回収することまで検証）
- `collectOrphanMedia.ts:sweepOnce`（ノート数予算 5 で打ち切り、解析は transaction の外）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-259`（2 件）
- `collectOrphanMedia.ts:sweepOnce`（keyset での前進・満ページ継続・翌日への復帰）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-254`（2 件。worker plane 駆動を含む）, `TC-storage-027`, `TC-storage-025`
- `collectOrphanMedia.ts:collectOrphanMedia`（turn 全体の失敗で throw せず開始位置を保って翌日へ）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-258`（2 件）
- `collectOrphanMedia.ts:readOrphanMediaSweepTurn`（読めない payload は先頭から）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-255`
- `collectOrphanMedia.ts:readNoteBodies`（保持中の版の本文も参照元に数える）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-257`
- `collectOrphanMedia.ts:sweepOnce`（30 日境界の包含）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-018/019`
- `collectOrphanMedia.ts:armOrphanMediaSweepOnFirstMedia`（初回流入時のみ登録、2 回目以降は `dueAt` を押し出さない）— 守っているテスト: `collectOrphanMedia.test.ts:TC-storage-026`, `relocateFilesForNote.test.ts:TC-storage-026`（3 件）
- `storedFileRepository.ts:listByPurposeOlderThan`（`createdAt, id` の全順序・排他的 keyset・境界包含・位置は行ではない）— 守っているテスト: `conformance/storedFileRepository.ts:ADP-storage-010`（3 件）を memory / cloudflare 双方が取り込む（`memory/__tests__/conformance.test.ts:77`, `cloudflare/__tests__/conformance/scopeBusiness.test.ts:23`）
- `storedFileRepository.ts:listDeletableByNote`（`id` 昇順・`limit` で切る・artifact 除外）— 守っているテスト: `conformance/storedFileRepository.ts:ADP-storage-007`
- `backupRecordRepository.ts`（`insert` の 2 種の一意制約の写像・`listByNote` の順序・`deleteByNote` の有界削除）— 守っているテスト: `conformance/backupRecordRepository.ts:ADP-integration-008`（2 件）, `ADP-integration-030`, `ADP-integration-014`。memory / cloudflare 双方が取り込む
- `deleteFilesForNote.ts`（receipt 一致で通し、`completed` でも通し、不一致は `CLEANUP_OPERATION_MISMATCH`）— 守っているテスト: `deleteFilesForNote.test.ts:TC-storage-059`（2 件）, `TC-storage-060`（コード固定を検証）
- `deleteFilesForNote.ts`（列挙件数で継続を判断、runner が再開、冪等）— 守っているテスト: `deleteFilesForNote.test.ts:TC-storage-058`（2 件）, `TC-storage-061`, `TC-storage-062`
- `deleteBackupRecordsForNote.ts`（一括削除・継続・admission・workspace scope での token 拒否・書き込み失敗の透過）— 守っているテスト: `deleteBackupRecordsForNote.test.ts:TC-integration-016〜025`（`TC-integration-022` / `023` / `024` は各 2 件）
- `notePurgeFanOut.ts:settleNotePurgeTurn`（出自で priority を決める）— 守っているテスト: `deleteFilesForNote.test.ts:TC-storage-058`, `deleteBackupRecordsForNote.test.ts:TC-integration-023`（`securityCleanup` の継続を検証）
- `ensureUploadAllowed.ts`（行が無い主体は初期値で判定、LLM は要求者本人、`llmCalls: 0` は LLM を読まない）— 守っているテスト: `ensureUploadAllowed.test.ts:TC-usage-034〜042`
- `subscribers.ts`（`note.purged` の 3 購読者の登録と、兄弟の失敗で他が飛ばされないこと）— 守っているテスト: `subscribers.test.ts:"note.purged fan-out"`, `"runs the siblings of a failing subscriber and still fails the delivery"`
- `routes/storage.$.tsx`（`avatar` / `media` だけを配信、`FILE_PURPOSES` の補集合は 404、`nosniff` + `sandbox` CSP）— 守っているテスト: `storage.delivery.test.ts:TC-storage-175`, `TC-storage-176`, `"serves exactly the purposes …"`, `"answers 404 …"`
- `storageUrlPolicy.ts:StorageUrlPolicy.create / isInternal` — 守っているテスト: `storage.test.ts` の既存ケース（本ラウンド差分外の挙動）
- `domain/integration`（`BackupRecordId` / `ExternalFileRef` / `BackupRecord.reconstruct`）— 守っているテスト: `domain/integration/__tests__/valueObject.test.ts:DOM-integration-002/007/009`

#### カバレッジ

- 確認: `packages/core/src/application/storage/storeMedia.ts`, `packages/core/src/application/storage/collectOrphanMedia.ts`, `packages/core/src/application/storage/deleteFilesForNote.ts`, `packages/core/src/application/storage/relocateFilesForNote.ts`, `packages/core/src/application/storage/view.ts`, `packages/core/src/application/storage/__tests__/collectOrphanMedia.test.ts`, `packages/core/src/application/storage/__tests__/storeMedia.test.ts`, `packages/core/src/application/storage/__tests__/deleteFilesForNote.test.ts`, `packages/core/src/application/storage/__tests__/deleteFiles.test.ts`, `packages/core/src/application/storage/__tests__/relocateFilesForNote.test.ts`, `packages/core/src/application/usage/ensureUploadAllowed.ts`, `packages/core/src/application/usage/__tests__/ensureUploadAllowed.test.ts`, `packages/core/src/application/integration/deleteBackupRecordsForNote.ts`, `packages/core/src/application/integration/__tests__/deleteBackupRecordsForNote.test.ts`, `packages/core/src/application/cleanup/notePurgeFanOut.ts`, `packages/core/src/application/cleanup/participants.ts`, `packages/core/src/application/ports/objectStorage.ts`, `packages/core/src/application/execution/unitOfWork.ts`, `packages/core/src/application/workers/subscribers.ts`, `packages/core/src/application/workers/__tests__/subscribers.test.ts`（`note.purged` fan-out 部分）, `packages/core/src/domain/storage/ports/storedFileRepository.ts`, `packages/core/src/domain/storage/services/uploadValidationPolicy.ts`, `packages/core/src/domain/storage/services/storageUrlPolicy.ts`, `packages/core/src/domain/storage/__tests__/storage.test.ts`, `packages/core/src/domain/integration/backupRecord.ts`, `packages/core/src/domain/integration/valueObject.ts`, `packages/core/src/domain/integration/errorCode.ts`, `packages/core/src/domain/integration/ports/backupRecordRepository.ts`, `packages/core/src/domain/integration/__tests__/valueObject.test.ts`, `packages/core/src/adapters/memory/repositories/storedFileRepository.ts`, `packages/core/src/adapters/memory/repositories/backupRecordRepository.ts`, `packages/core/src/adapters/memory/store.ts`, `packages/core/src/adapters/memory/scopeUnitOfWork.ts`, `packages/core/src/adapters/memory/__tests__/conformance.test.ts`, `packages/core/src/adapters/memory/__tests__/conformanceBackend.ts`, `packages/core/src/adapters/cloudflare/do/repositories/storedFileRepository.ts`, `packages/core/src/adapters/cloudflare/do/repositories/backupRecordRepository.ts`, `packages/core/src/adapters/cloudflare/do/schema.ts`（`stored_files` / `backup_records` 部分）, `packages/core/src/adapters/cloudflare/__tests__/conformance/scopeBusiness.test.ts`, `packages/core/src/adapters/cloudflare/__tests__/conformanceBackend.ts`, `packages/core/src/adapters/cloudflare/__tests__/deleteFilesByOwner.test.ts`, `packages/core/src/adapters/conformance/backend.ts`, `packages/core/src/adapters/conformance/storedFileRepository.ts`, `packages/core/src/adapters/conformance/backupRecordRepository.ts`, `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`, `apps/web/app/routes/storage.$.tsx`, `apps/web/app/routes/__tests__/storage.delivery.test.ts`, `spec/domains/storage.md`, `spec/domains/integration.md`, `spec/domains/index.md`（継続要求表の storage / integration 行）, `spec/usecases/storage.md`, `spec/usecases/usage.md`, `spec/usecases/integration.md`, `spec/testcases/storage/collectOrphanMedia.md`, `spec/testcases/storage/deleteFilesForNote.md`, `spec/testcases/storage/storeMedia.md`, `spec/testcases/integration/deleteBackupRecordsForNote.md`, `spec/inventory/adapter.md`（storage / integration 行）, `spec/inventory/usecase.md`（UC-storage / UC-integration 行）, `spec/inventory/test.md`（TC-storage / TC-integration 行）, `spec/database/index.md`（`stored_files` 索引・scope 検証の `backup_records.user_id`）, `spec/platform/index.md`（実行予算表・scope outbox relay の段落）, `docs/backend_implementation_example.md`（`adapters/html` の 1 段落のみ。storage 側の記述変更は無し）
- スキップ: `apps/web/app/components/**`, `apps/web/app/routes/notes/**`, `apps/web/app/routes/workspaces/**`, `apps/web/app/routeTree.gen.ts`, `apps/web/app/presentation/**` — フロントエンド担当
- スキップ: `packages/core/src/application/note/**`, `packages/core/src/domain/note/**`（`ports/htmlProcessor.ts` は `HTML_PROCESSOR_NOTE_ERROR_CODES` の定義だけ参照）, `packages/core/src/adapters/html/**`（`allowList.ts` の SVG 部分集合だけ参照）, `spec/testcases/note/**`, `spec/usecases/note.md`, `spec/domains/note.md`, `spec/adr/013-html-sanitization-policy.md`, `spec/manual-tests/editing.md`, `spec/pages/index.md`, `spec/presentation/index.md`, `spec/usecases/job.md` — note / サニタイズ担当
- スキップ: `packages/core/src/application/tag/**`, `packages/core/src/domain/tag/**`, `packages/core/src/adapters/*/repositories/tagAssignmentRepository.ts`, `packages/core/src/adapters/*/__tests__/scopeGuards.test.ts`, `packages/core/src/adapters/conformance/tagAssignmentRepository.ts`, `spec/domains/tag.md`, `spec/usecases/tag.md`, `spec/testcases/tag/**` — tag 担当
- スキップ: `packages/core/src/application/identity/**`, `packages/core/src/application/di/**`, `packages/core/src/application/scope.ts`, `packages/core/src/application/ports/noteMovePort.ts`, `packages/core/src/application/ports/noteRouteStore.ts`, `packages/core/src/application/workers/scopeTaskRunner.ts`（settle の意味論だけ参照）, `packages/core/src/adapters/*/repositories/noteRepository.ts`, `packages/core/src/adapters/conformance/noteRepository.ts`, `packages/core/src/adapters/conformance/noteRouteStore.ts`, `packages/core/src/adapters/cloudflare/__tests__/ports/scopeBusiness.ts`, `packages/core/src/adapters/cloudflare/__tests__/runtimeComposition.test.ts`, `packages/core/package.json`, `pnpm-lock.yaml` — note 永続化 / 配線 / 依存の担当
