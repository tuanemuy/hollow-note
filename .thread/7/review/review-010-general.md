### general

ゼロベースで現在のツリーを検証した。手元で `pnpm typecheck` / `pnpm lint` / `pnpm format:check` / `pnpm test:node`（152 files, 2506 passed / 3 skipped）/ `pnpm exec vitest run --project workers …/runtimeComposition.test.ts`（6 passed）/ `pnpm install --frozen-lockfile --offline` をすべて通した。3 件の skipped は `adapters/oauth/__tests__/conformance.test.ts` の Google 資格情報ゲート（本 PR 外・既存）。

#### Blockers

なし

#### Warnings

- **[W-001]** `spec/testcases/storage/collectOrphanMedia.md` に本 PR が足した行「turn 全体が失敗する — 位置を確認する」が `spec/inventory/test.md` に載っていない
  - 場所: `spec/testcases/storage/collectOrphanMedia.md:26`（新規行）、`spec/inventory/test.md`（`collectOrphanMedia.md#` を指す行は 20 本、spec の表は 21 行）、`packages/core/src/application/storage/__tests__/collectOrphanMedia.test.ts`（`TC-storage-258: a failed turn keeps the position it started from …`）
  - 理由: 台帳は「1 行 = 1 テストケース。ID は本ファイルの行が持つ」と定める。本 PR で変更した 20 本の `spec/testcases/*.md` を表の行数と台帳の行数で突き合わせると、この 1 本だけが 21 対 20 でずれる。実装側のテストは既存 ID `TC-storage-258`（台帳の定義は「turn 全体が失敗する — 実行する」＝翌日へ張り直す・`collectedCount: 0`）を 2 ケースに相乗りさせているので、台帳から「失敗した turn は開始位置を保つ」という契約へ辿れない
  - 提案: storage 群の末尾に `TC-storage-277 | collectOrphanMedia: turn 全体が失敗する — 位置を確認する | … | 開始した位置をそのまま張り直しの payload と nextCursor に載せる` を採番し、当該 `it` 名の ID を付け替える

- **[W-002]** `adrReference.test.ts` の置き場所の根拠が事実と食い違う — `apps/web` の横断規則テストの既存の家は `apps/web/app/__tests__/` にある
  - 場所: `apps/web/app/presentation/__tests__/adrReference.test.ts:17-19`（「置き場所が `presentation/__tests__/` なのは、`apps/web` でここだけが特定のルート / コンポーネントに属さない横断的な規則を持つためである」）、既存の `apps/web/app/__tests__/serverFunctionRegistration.test.ts`（`routes/__root.tsx` への action 登録をソース走査で導く同種の規則テスト）
  - 理由: JSDoc の「ここだけ」が偽。`app/__tests__/serverFunctionRegistration.test.ts` はまさに「特定のルート / コンポーネントに属さない、リポジトリのソースを走査する規則」であり、そこが先例になっている。`docs/test.md` の Frontend 節は `presentation/__tests__/` を「`presentation/` の純関数（status mapping, redaction, …）」の置き場と定義しているので、`spec/adr/` の実在を `packages/core` まで含めて走査するテストをそこに置くと、層の説明と中身が一致しない
  - 提案: `apps/web/app/__tests__/adrReference.test.ts` へ移し（`REPO_ROOT` の相対段数を 1 つ減らす）、JSDoc の置き場所の段落は「リポジトリ横断の規則テストは `app/__tests__/`（`serverFunctionRegistration` と同じ）」の 1 文にする。移さない場合でも「ここだけ」の一文は削る

- **[W-003]** `docs/test.md` が本 PR で増えた 2 種類のテストを分類できない
  - 場所: `docs/test.md` Frontend の段落（「The exception is the pure functions of `apps/web/app/presentation/` … A pure function lifted out of an island … carries its tests in a `__tests__/` directory next to the component」）、新規の `apps/web/app/routes/__tests__/storage.delivery.test.ts`（route の GET ハンドラーを memory backend 越しに叩く）と `apps/web/app/presentation/__tests__/adrReference.test.ts`（リポジトリ横断の規約テスト）
  - 理由: `docs/test.md` は「テストの層と命名」の正典で、`apps/web` に何がテストを持つかを閉じた列挙で書いている。route ハンドラーを実際に呼ぶテストも、規約を機械で見張るテスト（`serverFunctionRegistration` / `conformanceCoverage` / `adrReference`）も、その列挙に無い。ラウンド 008 で「`.thread/` の ADR 番号を引かない規約は回帰テストで機械的に見張る」と決めた以上、その置き場と性格は docs 側にも 1 行要る。`docs/test.md` は本 PR の変更ファイルに含まれていない
  - 提案: Frontend の段落に 2 文足す — (1) 「route が自前の `server.handlers` を持つとき（`storage.$.tsx`）、その配信規則は `routes/__tests__/` で memory backend 越しに検証する」、(2) 「ソース走査で規約を見張るテスト（`app/__tests__/serverFunctionRegistration`、ADR 参照の解決）は層に属さず `app/__tests__/` に置く」。W-002 の移動と同時に直すのが自然

- **[W-004]** `storeMedia.test.ts` の TC-storage-273 が、退役した導出の経緯を `it` 名とコメントで語っている
  - 場所: `packages/core/src/application/storage/__tests__/storeMedia.test.ts:998`（`"… far past the expansion bound the ceiling used to be derived from"`）、`:1009-1014`（「The retired derivation read … which is why the promise moved to …」）
  - 理由: 記述の衛生。ラウンド 008 で「128 KB の導出は canon から降ろす」と決着した後も、テスト名が「かつて何から導いていたか」を主語にしている。読み手が守るべき性質は「`MEDIA_SVG_MAX_BYTES` ちょうどの SVG が `maxExpansionFactor` 倍を超えて直列化されても、拒否は Storage の再計測が `FileTooLarge` で答える」であり、それだけで十分に読める。`toBeLessThan(800_000)` はコメント自身が「1.7% の余裕であって約束ではない」と言うとおり、この経路が境界翻訳ではなく再計測で落ちることの前提条件として置くならその旨を書くべきで、退役した根拠の反証としては置かない
  - 提案: `it` 名を「an SVG at the ceiling that serializes past the expansion factor is refused by storage's own re-measure, not by the body limit」程度に改め、コメントは「`< 800_000` は本文上限の翻訳経路に入らないことの前提」と現在形で 1 文にする（挙動の変更なし）

#### テスト保証

- `parse5` が workerd で構文解析・直列化まで実行できること（`application/di/cloudflareRuntime.ts:createCloudflareRuntime` → `adapters/html/htmlProcessor.ts:createHtmlProcessor`）— 守っているテスト: `packages/core/src/adapters/cloudflare/__tests__/runtimeComposition.test.ts:parses and serializes HTML on workerd, not only imports the parser`（workers プロジェクトで手元実行し pass）
- request / worker コンテナ双方に `htmlProcessor` が載ること（`application/di/types.ts`）— 守っているテスト: 同ファイルの `REQUEST_PORTS` / `WORKER_PORTS` 列挙
- ADR 013 の URL 表を 3 適用点が 1 本の純関数で共有すること（`domain/note/services/urlPolicy.ts:isAllowedUrl` / `filterAllowedSrcset`）— 守っているテスト: `packages/core/src/domain/note/__tests__/urlPolicy.test.ts:UrlSchemePolicy.isAllowedUrl — spec/adr/013 「許可する URL スキーム」`（表駆動）、`reads the scheme through interleaved control characters`、`splits on candidates rather than commas so a data URL survives`
- コード・`spec/`・`docs/` から `.thread/` を引かず、`ADR nnn` が `spec/adr/` に実在すること — 守っているテスト: `apps/web/app/presentation/__tests__/adrReference.test.ts:resolves every ADR number to a file under spec/adr/` / `cites no work-log path from source, spec/ or docs/`。加えて差分の追加行（`.thread/` 配下を除く）から `ADR[- ]nnn` を抜いて `spec/adr/` と突き合わせ、未解決 0 件を手元で確認
- 適合スイートの本数が宣言（45 + 1）と一致し、`only` / `skip` で抜ける経路が無いこと — 守っているテスト: `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`（`PERSISTENCE_SUITES = 45` は `backupRecordRepository` / `tagAssignmentRepository` の 2 本追加と整合）
- 編集島から持ち上げた純関数（`NoteEditor/textNodes.ts:diffTextNodeEdits`、`preferences.ts:parseDraft` / `isExpired`、`saveStatus.ts:classifySaveFailure`、`highlight.ts:tokenizeHtml` / `sameMarkupStructure`）— 守っているテスト: `apps/web/app/components/note/NoteEditor/__tests__/{textNodes,preferences,saveStatus,highlight}.test.ts`。いずれも DOM / `window` に触れず `node` プロジェクトで走る。`expected` の向き（確定値を送る）、欠けた経路を「空へ書き換えた」と読まないこと、未来の時刻の下書きを捨てないこと、など否定側の分岐を個別に固定している
- ルートツリーの整合（`apps/web/app/routeTree.gen.ts`）— テストではなく、`@tanstack/router-generator` を scratchpad へ出力させて委託ファイルと比較（Start プラグインが付ける末尾の `declare module` ブロック以外は同一）。`routes/__tests__/storage.delivery.test.ts` はルートとして拾われない

#### 確認した事実（指摘に上げなかったもの）

- **依存方向**: `apps/web/app/components/note/NoteEditor/{editor,surfaces}.tsx` が `@repo/core/domain/note/services/urlPolicy` を直接 import するのは presentation → domain の内向きで、CLAUDE.md の方向に反しない。同じ形の先例が `identity/services/identityPolicy` / `identity/services/sameOriginPolicy` / `storage/services/uploadValidationPolicy` にあり、`urlPolicy.ts` は fetch も base URL 解決も DOM も持たない純関数のみ。`exports: "./*": "./src/*.ts"` で 1:1 に解決し、typecheck と node テストで実証済み
- **`packages/core/package.json` / `pnpm-lock.yaml`**: 追加は `parse5 ^8.0.1`（→ `parse5@8.0.1` + `entities@8.0.0`）の 1 件で、`dependencies` に置くのは両 composition root（`memoryRuntime.ts:2,181` / `cloudflareRuntime.ts:63,244`）が使うので正しい。`entities` の `engines.node >=20.19.0` はルートの `>=22.12.0` の内側。`--frozen-lockfile --offline` が通り、lockfile の差分は importer 1 項目 + packages / snapshots 各 2 項目のみ。`parse5` の import 元は `adapters/html/htmlProcessor.ts` だけで、`apps/web` へ漏れていない
- **テストの配属**: 新設テストの vitest プロジェクトは path で決まる。`domain/note/__tests__/urlPolicy.test.ts`、`NoteEditor/__tests__/*.test.ts`、`routes/__tests__/storage.delivery.test.ts` は `node`、`adapters/cloudflare/__tests__/{runtimeComposition,scopeGuards}.test.ts` は `workers`。命名は `__tests__/<target>.test.ts` に従う（`storage.delivery` は対象 `storage.$` に対する意図的な別名）
- **台帳**: コードの `it` 名にある TC ID（1,027 種）はすべて `spec/inventory/test.md` に実在、ADP ID（適合スイートのケース名）も `adapter.md` に実在、両台帳に重複 ID なし。本 PR で触れた 20 本の `spec/testcases/*.md` は W-001 の 1 本を除き表の行数と台帳の行数が一致。`TC-note-830` / `TC-storage-275` / `TC-storage-276` は各群末尾に採番済み。新規ポートメソッド（`NoteRepository.findNextPurgeDeadline` = ADP-note-057、`BackupRecordRepository.listByNote` = ADP-integration-030、`TagAssignmentRepository.{insert,listByNote,deleteByNote}`、`StoredFileRepository.{listDeletableByNote,listByPurposeOlderThan}`）は ADP 行あり。新規ユースケース 21 本はすべて `usecase.md` に UC 行あり。`frontend.md` は PAGE-p12-001〜008 / PAGE-p14-001〜004 が既存で、本 PR の `spec/pages` 差分は状態・注記の追記に留まり新しい操作行を要さない
- **`docs/`**: `backend_implementation_example.md` の `adapters/html/` 追記（「両 composition root が同じ実装を配線するので Node 専用 API に触れない純 JS」）はコードと一致。`frontend_implementation_example.md` / `runtime_node.md` に本 PR で偽になる記述は見当たらない（`NoteList` の例は現存の形）。`docs/test.md` については W-003
- **`ScopeToken`**: `canWrite` は `AppShell` に `scope` を渡す workspace 側 3 画面（`notes/index.tsx:70`、`notes/trash.tsx:66`、`settings/route.tsx:76`）すべてで loader の `role` から作って渡している。ノート詳細 / 編集 / 新規は `ReaderShell` でトークンを出さないので省略の影響を受けない。`listing.ts` の「省略は出さないと読む」JSDoc はこの実態と整合
- **記述の衛生**: 差分の追加行（`.thread/` 除く）に「ラウンド」「レビュー指摘」「修正前 / 後」「previously」などの経緯語は無く、`no longer` の出現はいずれも挙動の説明（「もう参照していない本文」など）。例外が W-004 の `used to be derived from` / `The retired derivation`
- **lint の 2 infos** は `biome.json` のスキーマ版（2.4.15 vs CLI 2.5.5）で本 PR 外

#### カバレッジ

- 確認: `packages/core/package.json`, `pnpm-lock.yaml`, `apps/web/app/routeTree.gen.ts`, `apps/web/app/components/layout/ScopeToken/index.tsx`, `apps/web/app/components/layout/ScopeToken/listing.ts`, `apps/web/app/routes/workspaces/$workspaceId/settings/-action.tsx`, `apps/web/app/routes/workspaces/$workspaceId/settings/route.tsx`, `apps/web/app/routes/workspaces/$workspaceId/-action.tsx`（canWrite の出所）, `apps/web/app/presentation/__tests__/adrReference.test.ts`, `apps/web/app/routes/__tests__/storage.delivery.test.ts`（配置のみ）, `apps/web/app/components/note/NoteEditor/__tests__/highlight.test.ts`, `apps/web/app/components/note/NoteEditor/__tests__/preferences.test.ts`, `apps/web/app/components/note/NoteEditor/__tests__/saveStatus.test.ts`, `apps/web/app/components/note/NoteEditor/__tests__/textNodes.test.ts`, `packages/core/src/domain/note/services/urlPolicy.ts`, `packages/core/src/domain/note/__tests__/urlPolicy.test.ts`, `packages/core/src/adapters/cloudflare/__tests__/runtimeComposition.test.ts`, `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`, `packages/core/src/application/di/memoryRuntime.ts`, `packages/core/src/application/di/cloudflareRuntime.ts`（html の配線のみ）, `docs/backend_implementation_example.md`, `docs/frontend_implementation_example.md`, `docs/runtime_node.md`, `docs/test.md`, `spec/inventory/test.md`, `spec/inventory/adapter.md`, `spec/inventory/usecase.md`, `spec/inventory/frontend.md`, `spec/inventory/domain.md`, `spec/testcases/**`（行数の突き合わせ）, `spec/pages/index.md`（追加行のみ）, `packages/core/src/application/storage/__tests__/storeMedia.test.ts:998-1025`, `packages/core/src/application/storage/__tests__/collectOrphanMedia.test.ts`（`it` 名のみ）
- スキップ: `packages/core/src/adapters/html/` — HtmlProcessor の内部実装は backend-note 担当
- スキップ: `packages/core/src/application/note/`, `packages/core/src/domain/note/`（`urlPolicy` 以外）, `packages/core/src/adapters/{memory,cloudflare}/**`（`runtimeComposition` 以外）, `packages/core/src/adapters/conformance/`, `packages/core/src/application/cleanup/`, `packages/core/src/application/workers/`, `packages/core/src/application/identity/`, `packages/core/src/application/execution/`, `packages/core/src/application/ports/`, `packages/core/src/application/scope.ts`, `spec/domains/note.md`, `spec/usecases/note.md`, `spec/adr/013-*.md`, `spec/database/index.md`, `spec/platform/index.md` — backend-note 担当
- スキップ: `packages/core/src/application/storage/`（上記 2 テストの該当箇所以外）, `packages/core/src/application/usage/`, `packages/core/src/domain/storage/`, `spec/domains/storage.md`, `spec/usecases/{storage,usage}.md`, `apps/web/app/routes/storage.$.tsx` — backend-storage 担当
- スキップ: `packages/core/src/application/tag/`, `packages/core/src/domain/tag/`, `packages/core/src/application/integration/`, `packages/core/src/domain/integration/`, `spec/domains/{tag,integration}.md`, `spec/usecases/{tag,integration,job}.md` — backend-tag 担当
- スキップ: `apps/web/app/components/note/**`（`NoteEditor/__tests__` 以外）, `apps/web/app/routes/notes/**`, `apps/web/app/routes/workspaces/$workspaceId/notes/**`, `apps/web/app/presentation/errorDisplay.ts` と同テスト, `spec/presentation/index.md`, `spec/manual-tests/editing.md` — frontend 担当
