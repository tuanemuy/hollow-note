### general

ゼロベースで現在のツリーを検証した。手元で `pnpm typecheck`（両パッケージ green）、`pnpm lint`（違反 0、info 2 は Biome の設定移行案内のみ）、`pnpm test:node`（152 files、2528 passed / 3 skipped。skipped は既存の Google 資格情報ゲート）、`pnpm test:workers`（25 files、562 passed）を通した。

#### 休止中 backend-storage の範囲 — ラウンド 012 の書き換えの実測

前ラウンドがこの範囲に入れた 3 件を、書かれた主張と現在の実装・外部の事実を自分で突き合わせた。

| 主張 | 場所 | 突き合わせ | 判定 |
| --- | --- | --- | --- |
| TC-storage-074 は Usage の `applyStorageDelta`（UC-usage-003）と一緒に Issue #6 で着地する | `deleteStoredObjects.ts:15-19`、`deleteFiles.test.ts:206-209` | `spec/inventory/usecase.md:132` で UC-usage-003 = `applyStorageDelta`。`gh issue view 6` の本文チェックリストに `UC-usage-003 applyStorageDelta` と TC-usage-001〜006 の行があり、コメントに本 PR からの handoff 文が付いている。`packages/core/src` に `applyStorageDelta` の購読者は無く（`moveNote.ts:393` の同名ローカル関数のみ）、`workers/subscribers.ts` の `storage.fileDeleted` 購読者は `deleteStoredObjects` の 1 件 | 一致 |
| 本スライスが固定するのは「`markProcessed` を呼ばない」まで（TC-storage-069 / 070） | 同上 | `deleteFiles.test.ts:316-355` が 1 回目の `deleteMany` を `SystemError` で落とし、`h.backend.idempotency.values()` が空であることと、同じ event の 2 回目が実体を消すことを主張している。`deleteStoredObjects.ts` は `IdempotencyStore` を触らない（`objectStorage.deleteMany` の 1 行） | 一致・検出力あり |
| `readOrphanMediaSweepTurn` は `{ cursor, fromPayload }` を返し、鍵を名指しながら読めない payload だけを warn する | `collectOrphanMedia.ts:167-195`、`scopeTaskRunner.ts:173-185` | `{}` → `{ cursor: null, fromPayload: true }`（`sweepPayload(null)` は `{}` を書くので日次の形と一致）、片方だけ・非文字列・空白 id・読めない日付 → `fromPayload: false`。runner は `!fromPayload` で `logger.warn` を 1 行出し、`spec/domains/index.md:333` の「後者を選んだ実装は `logger.warn` を 1 行残す」に沿う。`collectOrphanMedia.test.ts:562-618`（TC-storage-255 の 2 本）が reader の全分岐と「読めない位置で warn 1 件・日次 payload で warn 0 件」を主張している | 一致・検出力あり |
| `spec/usecases/usage.md:69` の呼び出し元 = `storeUpload` / `startBulkUpload` / `storeMedia`、`storeAvatar` は呼ばない | `usage.md:16` と `:69` | `:16` と同じ 3 者。`spec/usecases/storage.md:41`（`startBulkUpload` 手順 4）・`:93,97`（`storeUpload`）・`:151`（`storeMedia` 手順 4）が `ensureUploadAllowed` を呼び、`:194` が `storeAvatar` は呼ばないと明記。実装は `storeMedia.ts:395` の 1 か所（`storeUpload` / `startBulkUpload` は未実装で、`usage.md` はその 2 者を spec 上の呼び出し元として列挙している。実装と食い違う主張ではない） | 一致 |

#### Blockers

なし

#### Warnings

- **[W-001]** `spec/inventory/test.md` が「`spec/testcases/` から生成した台帳（1 行 = 1 テストケース）」と自称しながら、本 PR が触った testcases 12 ファイルの範囲で **37 行が testcases の表と食い違っている**（期待結果の相違 22 行、`要素` 列＝前提・操作の相違 15 行）。うち期待結果の 22 行中 17 行は本 PR が書いた行（差分に `+| TC-… |` がある）
  - 場所: `spec/inventory/test.md`（先頭の「生成元: `spec/testcases/`」）と `spec/testcases/{note/deleteNotesForOwner,note/emptyTrash,note/listNotes,note/listTrashedNotes,note/purgeExpiredTrash,note/purgeNote,note/restoreNoteRevision,note/trashNote,note/updateNoteBody,storage/collectOrphanMedia,storage/storeMedia}.md`。機械的に突き合わせた全 37 行は末尾の「突き合わせの手順」で再現できる。実装と照らして向きが決まる代表例:
    - `TC-note-077`（`test.md:1047`）: 台帳は「失敗として返る（キューの再試行と DLQ に委ねる）」、testcases は「継続要求を積まず、現在 task を backoff させて `stalled` として返る（`note` 成分は ack しない）」。`deleteNotesForOwner.test.ts:323` は `view.status === "stalled"` と `attempt: 1` の backoff 行を主張しており、**台帳側が偽**（DLQ は存在しない経路）
    - `TC-note-819`（`test.md:2614`）: 台帳の「従来は 26 秒焼いてから `RangeError`」は経緯の記述で、testcases はすでに現在形（「深さがそのまま走査を二乗にする」）に直っている
    - `TC-note-817`: 逆向き。testcases の前提は「`<div>` を 2,000 段 = 22 KB」だけだが、`htmlProcessor.test.ts:873-890` は 2,000 段と 50,000 段（550 KB、2 秒以内）の 2 本を持ち、台帳の「2,000 段 = 22 KB / 50,000 段 = 550 KB」のほうが実装に合う
    - `TC-note-113`（`test.md:1083`）: 台帳「版の競合（`ConflictError`）」、testcases「`ConflictError("OPTIMISTIC_LOCK_FAILURE")`」。ラウンド 012 [W-003] が 1081 / 1748 を直した際の取り残し（実装は `emptyTrash.ts:isSkippableRefusal` が `OPTIMISTIC_LOCK_FAILURE` に絞っている）
    - storage 範囲（本レビューの持ち分）: `TC-storage-253` / `256` は `要素` 列の前提・操作の切り方が testcases と違い（testcases「サニタイズ後も 128 KB 以内に収まる — 128 KB ちょうどの SVG をアップロードする」に対し台帳は前提に操作の一部を含む）、`TC-storage-259` / `260` / `264` / `265` / `270` / `273` / `274` / `276` は期待結果の括弧内が片側にしか無い。`TC-storage-270` は台帳だけが「breakout tag が無ければ foreign content から出ないので複製が起きず」を残しており、これはラウンド 008 で 128 KB の根拠から降ろした論証（ADR 013 の資源上限方針）の名残
  - 理由: 台帳は ID の正典であり `it` 名の TC ID から辿る先がこの行なので、行が testcases と別のことを言うと「テスト名 → 台帳 → testcases」の連鎖のどこかで読み手が偽の主張を受け取る。ラウンド 010〜012 で「文言の書き換えが偽の主張を作る」が繰り返された原因の一つが、この 2 表を別々に手で直していることにある（`TC-note-077` は testcases だけが直り、`TC-note-817` は台帳だけが直った）
  - 提案: 触った 12 ファイルについて、台帳の `要素` / `実装されるべき振る舞いの要点` を testcases の表から機械的に写し直す（ID と行位置は保つ）。その前に testcases 側で偽になっている `TC-note-817` の前提に「/ 50,000 段 = 550 KB」を戻す。写し直しの手順は末尾に置いた。以後は台帳を手で編集せず、testcases を直したら同じ手順で行を再生成する

- **[W-002]** `TC-storage-255` の行（testcases・台帳とも）が、ラウンド 012 [W-002] の修正で加わった「鍵を名指しながら読めない payload は `logger.warn` を 1 行残す／日次の `{}` は warn しない」を書いていない
  - 場所: `spec/testcases/storage/collectOrphanMedia.md:23`、`spec/inventory/test.md:2041`（どちらも「失敗させず先頭からやり直す（掃引行を `failed` に駐車させないため）」のみ）。`collectOrphanMedia.test.ts:597-618` の 2 本目は warn の有無を主張している
  - 理由: `spec/domains/index.md:333` が warn を規約として要求し、テストがそれを固定しているのに、TC 行がその半分を持たない。行だけ読むと「黙って先頭へ戻る」実装でも要件を満たすと読める（ラウンド 012 で指摘された状態そのもの）
  - 提案: 行の期待結果を「失敗させず先頭からやり直す（掃引行を `failed` に駐車させないため）。位置を名指しながら読めない payload では `logger.warn` を 1 行残し、位置を持たない日次の payload では残さない」に揃える（[W-001] の写し直しと同じ手順で台帳へ反映）

#### 横断的な整合

- **レイヤーの依存方向**: `apps/web/app` の非テストコードが `@repo/core/domain/*` を引くのは 23 件で、`urlPolicy` は `NoteEditor/{editor,surfaces}.tsx` の 2 件。どれも presentation → domain の内向き（`domain/identity/services/sameOriginPolicy` / `domain/storage/services/uploadValidationPolicy` と同じ形）。`@repo/core/adapters/*` を引く非テストコードは `worker/node/runner.ts`（合成ルート）と `routes/dev/-action.tsx`（dev IdP、既存）だけ。`components/note/__tests__/schema.test.ts` が `adapters/html/htmlProcessor` を引くのはテスト（`storage.delivery.test.ts` が memory adapter を引くのと同じ扱い）
- **テストの構成**: 新設テストの置き場所は `docs/test.md` の規則どおり。`adapters/cloudflare/__tests__/scopeGuards.test.ts` は workers、`adapters/memory/__tests__/scopeGuards.test.ts` は node（backend-local）。`routes/__tests__/storage.delivery.test.ts` と `app/__tests__/adrReference.test.ts` は `docs/test.md:85` の追記どおり。`NoteEditor/__tests__/*.test.ts` は「島から持ち上げた純関数はコンポーネント隣の `__tests__/`」に合う。conformance の登録は memory / cloudflare 両方の entry に `describeBackupRecordRepositoryContract` / `describeTagAssignmentRepositoryContract` / `describeStoredFileRepositoryContract` があり、`PERSISTENCE_SUITES = 45` を通る
- **台帳（機械的突き合わせ）**: `packages/core/src` / `apps/web/app` の `it` 名と本文に現れる TC ID 1,030 個・ADP ID 203 個はすべて `spec/inventory/{test,adapter}.md` に行がある。逆向きに、plan.md の TC 範囲で行だけあって ID を名乗るテストが無いのは storage / usage / integration / tag では **0 件**（TC-storage-074 は [W-001 of R012] の handoff 済み）。note 範囲では 18 件（TC-note-020 / 021 / 027 / 028 / 105 / 347 / 357 / 358 / 359 / 361 / 365 / 366 / 367 / 371 / 372 / 476 / 482 / 724）で、内訳は読み取りモデル・公開ページ・使用量・Drive・global recovery・タグ・参照記録といった他スライスの購読者側（020 / 021 / 027 / 028 / 347 / 365 / 366 / 367 / 371 / 372 / 476 / 482）、画面文言（105）、ID を名乗らずに固定されているもの（357 / 358 / 359 は `subscribers.test.ts` の「clears the files, assignments and backup records of the purged note」と各 follower のテスト、724 は `schema.test.ts:106` の「lets an oversized body reach NOTE_CONTENT_TOO_LARGE, not INVALID_INPUT」、361 は `purgeNote.test.ts` の TC-note-353 / 780 が「`purging` 切替後の応答喪失を再送すると同じ operation ID で再開し `note.purged` が 1 件」を主張）。ADR 058 は `it` 名の TC ID を推奨に留めているので指摘にはしない。`spec/inventory/usecase.md` は本 PR の UC 19 件すべてに行がある
- **ADR 参照**: `spec` / `docs` / `packages/core/src` / `apps/web/app` の `ADR nnn` / `ADR-nnn` / `spec/adr/nnn-` 参照はすべて `spec/adr/` に実在。`.thread/` の引用は `adrReference.test.ts` 自身（除外対象）以外に 0 件
- **storage / usage / integration の実装と spec**: `deleteStoredObjects` は `spec/usecases/storage.md:366`（`IdempotencyStore` を使わない理由）と一致。`collectOrphanMedia` の 3 段構成（読み取り transaction → transaction 外で解析 → 1 件ごとの削除 transaction）、`ORPHAN_MEDIA_NOTE_BUDGET` / `ORPHAN_MEDIA_BODY_BUDGET_CHARS` での打ち切りと位置の前進、失敗 turn の位置保持は `storage.md:428,436` と一致。`armOrphanMediaSweepOnFirstMedia` は `storeMedia` と `relocateFilesForNote.stageTarget` の 2 経路から行の挿入前に呼ばれる。`scopeTaskRunner.ts` の `ORPHAN_MEDIA_TASK_KIND` / `NOTE_{FILE,ASSIGNMENT,BACKUP}_DELETE_TASK_KIND` の登録は前ラウンドから変わっていない
- **依存関係とビルド**: `packages/core/package.json` の追加は `parse5 ^8.0.1` のみ。`pnpm-lock.yaml` は `packages/core` importer に `parse5 8.0.1`（→ `entities 8.0.0`、`engines node >=20.19.0`）を持ち、他 importer に増減なし。`adapters/html/*.ts` に `node:` / `require` / `Buffer` / `process` の参照は無く、`memoryRuntime.ts:181` / `cloudflareRuntime.ts:244` の両合成ルートが `createHtmlProcessor()` を配線している（`docs/backend_implementation_example.md:63,495` の主張と一致）。`routeTree.gen.ts` の 44 ルートは `apps/web/app/routes/` の実ファイル 35 本（`-*` / `__root` を除く）と 1:1 で、`/notes/{new,trash}`・`/notes/$noteId_/edit` と workspace 側の 3 本が揃っている
- **`docs/`**: `frontend_implementation_example.md:213` が挙げる `NoteListBoard` / `NoteDetailIsland` / `IdentityBoard` / `ProfileEditor` / `CreateNoteButton` は 5 つとも実在の export。`docs/test.md` の追記 2 文（`routes/__tests__/`、`app/__tests__/`）は実ファイルと一致。`docs/runtime_node.md` は task kind を列挙しておらず、本 PR で偽になる記述は無い
- **カバレッジの穴埋め**: `settings/-action.tsx:87` は `WorkspaceRole.atLeast(settings.role, "editor")` を `canWrite` として loader に載せ、`settings/route.tsx:76` で `ScopeToken` に渡す。`ScopeToken/index.tsx:255-266` は workspace で `canWrite === true` のときだけ「ゴミ箱」を出し、`workspaceAuthorization.ts:55` の `viewTrash: "editor"` と `listTrashedNotes.ts:85` の `ensureCan(access.role, "viewTrash")` に整合する（`canWrite` の名と省略時の扱いはラウンド 008 [W-008] で wont-fix 済み）。`routes/storage.$.tsx` は `PUBLICLY_SERVED_PURPOSES` を `FilePurpose` で型付けし `nosniff` / `inline` / `immutable` を付け、`storage.delivery.test.ts` の 4 本（TC-storage-175 / 176 と purpose 集合の補集合 404）が固定している

#### テスト保証

- `deleteStoredObjects` が処理済みを記録せず、失敗した配送の再試行を弾かない（`deleteStoredObjects.ts`） — 守っているテスト: `application/storage/__tests__/deleteFiles.test.ts:TC-storage-069 / TC-storage-070: records nothing as processed, so a delivery that failed is not filtered out on retry`
- 鍵を指定した削除の冪等性と失敗鍵の隔離（同上） — `deleteFiles.test.ts:TC-storage-065 / TC-storage-068`、`TC-storage-066 / TC-storage-067`、`TC-storage-072`
- 孤児掃引 payload の読み取りと warn の有無（`collectOrphanMedia.ts:readOrphanMediaSweepTurn`、`scopeTaskRunner.ts:ORPHAN_MEDIA_TASK_KIND`） — `application/storage/__tests__/collectOrphanMedia.test.ts:TC-storage-255: starts a fresh pass from the head when the payload carries no readable position`、`TC-storage-255: warns once when a row names a position it cannot read, and stays quiet on the daily payload`
- 掃引のキーセット前進・予算打ち切り・失敗時の位置保持（`collectOrphanMedia.ts:sweepOnce / collectOrphanMedia`） — `collectOrphanMedia.test.ts:TC-storage-254 / 258 / 259 / 276 / 277`、runner 経由の継続 `TC-storage-027`
- 版の本文を参照元に数える（`collectOrphanMedia.ts:readNoteBodies`） — `collectOrphanMedia.test.ts:TC-storage-257`
- 初流入での掃引の arm（`collectOrphanMedia.ts:armOrphanMediaSweepOnFirstMedia`） — `collectOrphanMedia.test.ts:TC-storage-026`、`relocateFilesForNote.test.ts:TC-storage-026`
- `ensureUploadAllowed` の不在行の扱いと LLM 側の主体（`usage/ensureUploadAllowed.ts`） — `usage/__tests__/ensureUploadAllowed.test.ts:TC-usage-037〜042`
- `note.purged` 追随の受理・継続・完了（`cleanup/notePurgeFanOut.ts`、`storage/deleteFilesForNote.ts`、`integration/deleteBackupRecordsForNote.ts`） — `deleteFilesForNote.test.ts:TC-storage-058〜061`、`integration/__tests__/deleteBackupRecordsForNote.test.ts:TC-integration-021〜024`、`workers/__tests__/subscribers.test.ts:clears the files, assignments and backup records of the purged note …`
- `BackupRecordRepository` / `StoredFileRepository.listByPurposeOlderThan` の契約 — `adapters/conformance/backupRecordRepository.ts:ADP-integration-008 / 014 / 030`、`adapters/conformance/storedFileRepository.ts:ADP-storage-007 / 010`（memory は node、cloudflare は workers で両方 green）
- 配信口が公開 purpose だけを通す（`routes/storage.$.tsx:PUBLICLY_SERVED_PURPOSES`） — `routes/__tests__/storage.delivery.test.ts:serves exactly the purposes spec/domains/storage.md publishes, and refuses the rest of the key space`
- ADR 参照の解決と `.thread/` 引用禁止 — `apps/web/app/__tests__/adrReference.test.ts`

#### カバレッジ

- 確認: `packages/core/src/application/storage/deleteStoredObjects.ts`, `packages/core/src/application/storage/collectOrphanMedia.ts`, `packages/core/src/application/storage/deleteFilesForNote.ts`（差分）, `packages/core/src/application/storage/relocateFilesForNote.ts`（arm の位置）, `packages/core/src/application/storage/storeMedia.ts`（`ensureUploadAllowed` の呼び出し）, `packages/core/src/application/storage/__tests__/deleteFiles.test.ts`, `packages/core/src/application/storage/__tests__/collectOrphanMedia.test.ts`, `packages/core/src/application/storage/__tests__/deleteFilesForNote.test.ts`（`it` 名）, `packages/core/src/application/storage/__tests__/relocateFilesForNote.test.ts`（`it` 名）, `packages/core/src/application/storage/__tests__/storeMedia.test.ts`（`it` 名）, `packages/core/src/application/usage/ensureUploadAllowed.ts`, `packages/core/src/application/usage/__tests__/ensureUploadAllowed.test.ts`（`it` 名）, `packages/core/src/application/integration/deleteBackupRecordsForNote.ts`, `packages/core/src/application/integration/__tests__/deleteBackupRecordsForNote.test.ts`（`it` 名）, `packages/core/src/application/cleanup/notePurgeFanOut.ts`, `packages/core/src/application/cleanup/participants.ts`, `packages/core/src/application/cleanup/personalCleanup.ts`（`readPersonalBarrierPruneTurn`）, `packages/core/src/application/workers/scopeTaskRunner.ts`, `packages/core/src/application/workers/subscribers.ts`（購読者表）, `packages/core/src/application/workers/__tests__/subscribers.test.ts`（`it` 名）, `packages/core/src/application/di/{memoryRuntime,cloudflareRuntime}.ts`（`createHtmlProcessor` の配線）, `packages/core/src/domain/storage/services/uploadValidationPolicy.ts`（差分）, `packages/core/src/domain/storage/ports/storedFileRepository.ts`, `packages/core/src/domain/integration/{backupRecord,valueObject,errorCode}.ts`, `packages/core/src/domain/integration/ports/backupRecordRepository.ts`, `packages/core/src/domain/workspace/services/workspaceAuthorization.ts`（`viewTrash`）, `packages/core/src/adapters/conformance/{storedFileRepository,backupRecordRepository}.ts`（実行のみ）, `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`, `packages/core/src/adapters/html/*.ts`（`node:` 参照の有無のみ）, `packages/core/src/adapters/html/__tests__/htmlProcessor.test.ts:873-905`（TC-note-817 / 819 の fixture のみ）, `packages/core/src/application/note/__tests__/{emptyTrash,purgeNote,deleteNotesForOwner}.test.ts`（TC-note-113 / 353 / 780 / 077 の `it` 本文のみ）, `packages/core/package.json`, `pnpm-lock.yaml`, `apps/web/app/routeTree.gen.ts`, `apps/web/app/__tests__/adrReference.test.ts`, `apps/web/app/routes/storage.$.tsx`, `apps/web/app/routes/__tests__/storage.delivery.test.ts`, `apps/web/app/routes/workspaces/$workspaceId/settings/-action.tsx`, `apps/web/app/routes/workspaces/$workspaceId/settings/route.tsx`, `apps/web/app/components/layout/ScopeToken/index.tsx`, `apps/web/app/components/layout/ScopeToken/listing.ts`, `apps/web/app/components/note/schema.ts`（`NOTE_HTML_TRANSPORT_MAX`）, `apps/web/app/components/note/__tests__/schema.test.ts`（`it` 名）, `docs/backend_implementation_example.md`, `docs/frontend_implementation_example.md`, `docs/test.md`, `docs/runtime_node.md`, `spec/usecases/usage.md`, `spec/usecases/storage.md`（`ensureUploadAllowed` / `collectOrphanMedia` / `deleteStoredObjects` の節）, `spec/usecases/integration.md`（差分）, `spec/domains/index.md:320-333`（継続要求の規則）, `spec/domains/storage.md`（差分）, `spec/domains/integration.md`（差分）, `spec/testcases/storage/*.md`, `spec/testcases/integration/deleteBackupRecordsForNote.md`, `spec/testcases/note/*.md`（台帳との突き合わせのみ）, `spec/inventory/test.md`, `spec/inventory/adapter.md`, `spec/inventory/usecase.md`
- スキップ: `packages/core/src/adapters/html/{allowList,css,htmlProcessor}.ts`（上記以外）, `packages/core/src/adapters/html/__tests__/`（上記以外） — HtmlProcessor の内部実装は backend-note 担当
- スキップ: `packages/core/src/application/note/**`（上記以外）, `packages/core/src/domain/note/**`, `packages/core/src/adapters/{memory,cloudflare}/**`（実行のみ）, `packages/core/src/adapters/conformance/{noteRepository,noteRouteStore,backend}.ts`, `packages/core/src/application/identity/**`, `packages/core/src/application/ports/{noteMovePort,noteRouteStore,objectStorage}.ts`, `packages/core/src/application/{scope,execution/unitOfWork}.ts`, `spec/domains/{note,index}.md`（上記以外）, `spec/usecases/{note,job,identity}.md`, `spec/database/index.md`, `spec/platform/index.md`, `spec/adr/013-html-sanitization-policy.md` — backend-note 担当
- スキップ: `packages/core/src/application/tag/**`, `packages/core/src/domain/tag/**`, `packages/core/src/adapters/*/repositories/tagAssignmentRepository.ts`, `packages/core/src/adapters/conformance/tagAssignmentRepository.ts`, `packages/core/src/adapters/*/__tests__/scopeGuards.test.ts`（実行のみ）, `spec/domains/tag.md`, `spec/usecases/tag.md`, `spec/testcases/tag/**` — backend-tag 担当
- スキップ: `apps/web/app/components/note/**`（`schema.ts` の上限と `__tests__/schema.test.ts` の `it` 名以外）, `apps/web/app/routes/notes/**`, `apps/web/app/routes/workspaces/$workspaceId/notes/**`, `apps/web/app/presentation/**`, `spec/pages/index.md`, `spec/presentation/index.md`, `spec/manual-tests/editing.md` — frontend 担当

#### 突き合わせの手順（[W-001] / [W-002] の再現）

`spec/testcases/<domain>/<uc>.md` の表の各行（前提 / 操作 / 期待結果）を `<uc>: <前提> — <操作>` で `spec/inventory/test.md` の `要素` 列と突き合わせ、一致した行の `実装されるべき振る舞いの要点` を期待結果と文字列比較する。本 PR の変更ファイル一覧にある testcases 12 ファイルに掛けると、`要素` が一致しない 15 行と、一致するが要点が違う 22 行が出る（行数はどのファイルも一致）。台帳の生成元宣言に従い、testcases を正として写し直す — ただし `TC-note-817` は先に testcases 側を実装（2,000 段と 50,000 段の 2 本）に合わせる。
