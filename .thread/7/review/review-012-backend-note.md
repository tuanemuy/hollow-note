### backend-note

ゼロベースで現在のツリーを検証した。差分本文（`7-round-012.diff`）の担当分を 1 回で読み、判断に必要な差分外ファイル（`domain/note/note.ts` / `valueObject.ts`、`workspaceAuthorization.ts`、`scopeTaskScheduler.ts`、`storage/deleteFilesByOwner.ts`、`storage/storeMedia.ts`、`cloudflare/sql/session.ts`、`spec/database/index.md`、`spec/domains/index.md`、`spec/pages/index.md`、`spec/inventory/*.md`）を突き合わせた。加えて次の 2 つを実行した。

- `HtmlProcessor` に対する迂回入力 90 種（エンティティ経由の `javascript:`、制御文字混入、`data:` の各 MIME、`srcset` 候補、SVG の integration point 経由の `<style>` / `<script>`、`noscript` mXSS、`math`、`template`、`xmp` / `plaintext`、CSS のエスケープ・コメント・bad-string・url-token・`var()`・`!important`、`</style` を含む取得 CSS の `inlineStylesheets`、`editTextNodes` によるマークアップ注入、`rewriteReferences` の往復）を一時テストから実際に通し、出力・`removed`・`process(process(x)) === process(x)` を確認した。素通りした危険な形は無い（`behavior:` / `-moz-binding:` / `expression()` / `url(javascript:)` は残るが、ADR 013 が除去対象に置かない旧ブラウザ専用の構文であり、現行ブラウザで実行されない。前ラウンドまでの判定どおり新事実にならない）
- `pnpm vitest run --project node` を `adapters/html` / `application/note` / `domain/note` / `memory/__tests__/conformance.test.ts` / `application/cleanup` / `application/workers` に対して実行: 27 files / 1120 tests green

前ラウンドの書き換え箇所は実装と一致している:

- `spec/usecases/note.md:978`（継続の媒体）— `deleteNotesForOwner` は `NOTE_OWNER_PURGE_TASK_KIND`、`deleteFilesByOwner` は `STORAGE_OWNER_DELETE_TASK_KIND` の専用行で継続し、どちらもドメインイベントを再投入しない。「`deleteFilesByOwner` も同じ形をとる」は真。参照する `spec/domains/index.md` の「継続要求」節と「購読者を 1 つに保つ」も実在する
- `emptyTrash.ts:isSkippableRefusal` — `OPTIMISTIC_LOCK_FAILURE` / `NotFoundError` / `NOTE_NOT_TRASHED` の 3 つに絞られ、`spec/usecases/note.md` の emptyTrash 節・エラー表・`spec/testcases/note/emptyTrash.md` と一致。`TC-note-778`（3 件）が `WORKSPACE_DELETING` を実際に張って要求ごと失敗することと、purge 済み件数が戻らないことを状態で検証している
- `domain/note/ports/htmlProcessor.ts` の `ContentTooLarge` 記述 — `editTextNodes` / `inlineStylesheets` の `NOTE_CONTENT_TOO_LARGE` テストは実際に 800,100 バイトを注入して `BusinessRuleError` の code を見ている（検出力あり）

#### Blockers

なし

#### Warnings

- **[W-001]** `spec/domains/note.md` の `NoteErrorCode` 列挙に `HtmlTooComplex` が無い
  - 場所: `spec/domains/note.md:741-746`（`NoteErrorCode = | "InvalidId" | ... | "NoteLockedByJob"`）
  - 理由: 本 PR が `packages/core/src/domain/note/errorCode.ts:15` に `HtmlTooComplex: "NOTE_HTML_TOO_COMPLEX"` を足し、`spec/usecases/note.md:502` / `:1062` は `BusinessRuleError(HtmlTooComplex)` を、同 `spec/domains/note.md:389` / `:391` は `BusinessRuleError(NOTE_HTML_TOO_COMPLEX)` を書いている。しかし同ファイルのエラーコード節（ドメインの語彙の正典）は旧 13 値のままで、`spec/` の中で「存在しないコード名」を参照している状態になる。PR 起因の乖離
  - 提案: 列挙に `"HtmlTooComplex"` を足す。あわせて同ファイル内の表記を `HtmlTooComplex`（コード名）か `NOTE_HTML_TOO_COMPLEX`（値）のどちらかに揃える

- **[W-002]** ADP-note-057 の「同一 UoW で `active → trashed` に反転させた行を答えに含める」節に実行形が無い
  - 場所: `packages/core/src/domain/note/ports/noteRepository.ts:37-44`（JSDoc の契約節）、`spec/inventory/adapter.md:263`、`packages/core/src/adapters/conformance/noteRepository.ts:116-135`（ADP-note-057 のケース）、`packages/core/src/application/note/__tests__/purgeExpiredTrash.test.ts:265-283`
  - 理由: `trashNote.ts:86` の `armRetentionSweep` は `save` の直後・同じ `run` の内側で `findNextPurgeDeadline()` を引き、その答えが「いま捨てたノート」を含むことを port JSDoc と inventory が契約として宣言している（`spec/database/index.md` の「同一 UoW の読み」(3) にも名指しで載る）。ADR 026 は「契約的な振る舞いを足すときは port JSDoc と conformance スイートの両方に触る」と定めるが、ADP-note-057 のケースは `backend.forScope()` の repository に `insert` して読むだけで、**`save` で反転させた行を同じ unit of work の内側から読む形を一度も取らない**。cloudflare 側でこの節を成立させているのは `session.readRows` の `matches` overlay（`noteRepository.ts:findNextPurgeDeadline`）だが、それを契約として固定するテストが無い。usecase 層も同じ穴を持つ: `UC-note-021: trashing arms the scope's alarm at the earliest deadline` は 2 件捨ててから `dueAt: first.purgeAfter` を見るので、`findNextPurgeDeadline` が staged 行を無視して 1 件目で `null` → 行なし、2 件目で 1 件目の期限を返す実装でも緑のまま通る（空のゴミ箱に 1 件目を捨てた turn がアラームを張ることは、どのテストも直接には主張していない）。memory バックエンドは書き込みが即時に表へ乗るため現状は正しく動くが、契約の検出力がゼロの状態は前ラウンドで見つかった fixture の穴と同型
  - 提案: (a) conformance に「`insert(active)` → 1 つの `scopeUnitOfWorkProvider.run` の内側で `save(Note.trash(...))` → `findNextPurgeDeadline()` がその `purgeAfter` を返す」ケースを足す（memory / cloudflare 両方が同じスイートを通る）。(b) `trashNote` 側に「空のゴミ箱へ 1 件捨てた直後、`tasks(h)` が `{ kind: TRASH_EXPIRY_TASK_KIND, dueAt: view.purgeAfter }` の 1 行」を主張するテスト（TC-note-793 の単独版）を足す

- **[W-003]** `spec/inventory/test.md` の TC-note-111 / TC-note-778 行が前ラウンドで狭めた判定を映していない
  - 場所: `spec/inventory/test.md:1081`（TC-note-111「`ConflictError` / `NOTE_NOT_TRASHED` / 不在」）、`:1748`（TC-note-778「`SystemError(DatabaseError)` など」）
  - 理由: `spec/testcases/note/emptyTrash.md` は前ラウンドで「`ConflictError("OPTIMISTIC_LOCK_FAILURE")`」に絞り、TC-note-778 側に「scope 全体の書き込み障壁 `WORKSPACE_DELETING` / `ACCOUNT_DELETING` を含む」と明記したが、inventory の 2 行は旧文言のままで、「`ConflictError` 全体を飛ばす」というまさに実装が拒む読みを台帳が残している。inventory は testcases の写しなので、写しだけが古い状態
  - 提案: 2 行を testcases の現行文言に合わせる（`ConflictError("OPTIMISTIC_LOCK_FAILURE")` と、障壁の 2 コードを飛ばさない旨）

#### テスト保証

- `HtmlProcessor.process` の許可リスト・URL スキーム・CSS 宣言単位の除去（`adapters/html/htmlProcessor.ts` / `css.ts` / `allowList.ts`）— 守っているテスト: `adapters/html/__tests__/htmlProcessor.test.ts: HtmlProcessor.process — ADP-note-001 sanitize allow-list`（TC-note-682〜720 のテーブル駆動、TC-note-823 / 824 の bad-string・関数トークン）。本ラウンドの迂回入力 90 種でも素通りなし
- `process` の不動点（`process(process(x)) === process(x)`）— 守っているテスト: 同ファイル `ADP-note-001 is a fixed point`（全サニタイズケース＋未閉じ CSS）
- 5 つの資源上限（`HtmlProcessorLimit`、`createMeteredTreeAdapter` / `enforceNestingLimit` / `filterBlock` / `spend`）— 守っているテスト: 同ファイル `ADP-note-001 is bounded by resources`（TC-note-817〜822 / 825〜829。境界値 256/257 段・32/33 段・50,000/50,001 節点、`<form>` / `</template>` 前置きの素の経路、`RangeError` でなく `business` kind であること）
- `editTextNodes` の経路解決・`expected` 不一致・`<style>` / `<script>` の経路不割り当て・注入テキストのエスケープ — 守っているテスト: 同ファイル `ADP-note-005`（TC-note-001〜006 / 011 / 012）。本ラウンドの probe で `</p><script>` を注入しても `&lt;` に直列化されることを確認
- `inlineStylesheets` の CSS 規則適用と `</style` 中和・`ContentTooLarge` — 守っているテスト: 同ファイル `ADP-note-004`（重なり綴り・大文字・空白・800,100 バイト）
- `UrlSchemePolicy`（`domain/note/services/urlPolicy.ts`）の表・制御文字除去・`srcset` 候補分割 — 守っているテスト: `domain/note/__tests__/urlPolicy.test.ts: UrlSchemePolicy.isAllowedUrl / schemeOf / filterAllowedSrcset`
- `NoteAccessPolicy.ensureCanDelete`（`domain/note/services/noteAccessPolicy.ts`）— 守っているテスト: `domain/note/__tests__/noteAccessPolicy.test.ts: NoteAccessPolicy.ensureCanDelete`（個人 / workspace ロール表 / trashed）
- 編集系の入口門と commit 門（`application/note/editing.ts: resolveEditableNote / claimNoteForEdit / ensureExpectedVersion / ensureNotTrashed`）— 守っているテスト: `updateNoteBody.test.ts: TC-note-726 / 727 / 731 / 732`、`renameNote.test.ts: TC-note-404 / 406 / 785`、`changeNoteStyleMode.test.ts: TC-note-024 / 025 / 786`、`applyTextNodeEdits.test.ts: TC-note-015 / 016 / 784`、`restoreNoteRevision.test.ts: TC-note-474 / 477 / 787`
- `updateNoteBody` の版保持 20・ジョブロック・参照取り込み登録の重複防止（`updateNoteBody.ts` / `jobs.ts`）— 守っているテスト: `updateNoteBody.test.ts: TC-note-725 / 728〜730 / 733〜740 / 697〜702`
- `applyTextNodeEdits` の再 `process`・全件 skip 時の無書き込み（`applyTextNodeEdits.ts`）— 守っているテスト: `applyTextNodeEdits.test.ts: TC-note-001 / 005 / 012 / 014 / 017`
- `restoreNoteRevision` の再サニタイズ・参照の巻き戻しと再登録・他ノートの版の不在化・ジョブロック（`restoreNoteRevision.ts`）— 守っているテスト: `restoreNoteRevision.test.ts: TC-note-470〜484 / 830 / 787`
- `listNoteRevisions` の 20 件上限・作者解決・抜粋（`listNoteRevisions.ts`）— 守っているテスト: `listNoteRevisions.test.ts: TC-note-220〜227`
- `trashNote` の強制終端・本文回復の順序・同一 transaction・冪等・応答 `version`（`trashNote.ts`）— 守っているテスト: `trashNote.test.ts: TC-note-662〜681 / 812 / 813`
- `trashNote` 手順 5（空のゴミ箱へ 1 件目を捨てた turn がその `purgeAfter` でアラームを張る／`findNextPurgeDeadline` が同一 UoW の反転行を含む）— 守られていない → [W-002]
- `restoreNote` の `NOTE_NOT_TRASHED`・公開状態の復活・応答 `version`（`restoreNote.ts`）— 守っているテスト: `restoreNote.test.ts: TC-note-463〜469 / 814`
- `purgeNote` の route saga（claim → local delete → public remove → tombstone）・abort の白リスト・commit 後の応答喪失で abort しないこと・`RESUME_CLAIM` による再開（`purgeNote.ts`）— 守っているテスト: `purgeNote.test.ts: TC-note-348〜370 / 780`、`adapters/conformance/noteRouteStore.ts: ADP-note-043`（2 件）
- `emptyTrash` の同期 / ジョブ経路の分岐・列挙の打ち切り・飛ばす拒否の 3 種と障壁の非スキップ（`emptyTrash.ts`）— 守っているテスト: `emptyTrash.test.ts: TC-note-098〜115 / 778（3 件）/ 779`
- `purgeExpiredTrash` の 3 通りの決着（backoff / 直後再予定 / 次期限へ移動・空なら完了）（`purgeExpiredTrash.ts:settle`）— 守っているテスト: `purgeExpiredTrash.test.ts: TC-note-343〜346 / 350`、`backs the row off instead of re-arming`、`UC-note-021`（3 件）
- `deleteNotesForOwner` の持ち回り（`StuckPurge`）・ack の条件・ページ幅 `batchSize - carried`・payload 破損の park（`deleteNotesForOwner.ts`）— 守っているテスト: `deleteNotesForOwner.test.ts: TC-note-067〜082 / 781〜783 / 788 / 789 / 827`、`faults on a carried purge it cannot read`、`parks the continuation ...`
- `NoteRepository.listPurgeable` の順序・`findNextPurgeDeadline` の `now` 非依存（memory / cloudflare）— 守っているテスト: `adapters/conformance/noteRepository.ts: ADP-note-013 / ADP-note-057`（同一 UoW の反転行だけ未カバー → [W-002]）
- `getNote` の `version` / `trashedAt`、`listNotes` / `listTrashedNotes` の行 `version`・`viewTrash` 門 — 守っているテスト: `getNote.test.ts: TC-note-815 / 816`、`listNotes.test.ts`（6 件）、`listTrashedNotes.test.ts: PAGE-p14-001〜003`
- `note.purged` の 3 購読者の登録（`application/workers/subscribers.ts`）— 守っているテスト: `application/workers/__tests__/subscribers.test.ts`（担当外だが `note.purged` 行の存在は grep で確認）

#### カバレッジ

- 確認: `packages/core/src/domain/note/errorCode.ts`, `packages/core/src/domain/note/noteRevision.ts`, `packages/core/src/domain/note/ports/htmlProcessor.ts`, `packages/core/src/domain/note/ports/noteRepository.ts`, `packages/core/src/domain/note/services/noteAccessPolicy.ts`, `packages/core/src/domain/note/services/urlPolicy.ts`, `packages/core/src/domain/note/__tests__/noteAccessPolicy.test.ts`, `packages/core/src/domain/note/__tests__/urlPolicy.test.ts`, `packages/core/src/application/note/applyTextNodeEdits.ts`, `packages/core/src/application/note/changeNoteStyleMode.ts`, `packages/core/src/application/note/createBlankNote.ts`, `packages/core/src/application/note/deleteNotesForOwner.ts`, `packages/core/src/application/note/editing.ts`, `packages/core/src/application/note/emptyTrash.ts`, `packages/core/src/application/note/getNote.ts`, `packages/core/src/application/note/jobs.ts`, `packages/core/src/application/note/listNoteRevisions.ts`, `packages/core/src/application/note/listNotes.ts`, `packages/core/src/application/note/listTrashedNotes.ts`, `packages/core/src/application/note/moveNote.ts`, `packages/core/src/application/note/purgeExpiredTrash.ts`, `packages/core/src/application/note/purgeNote.ts`, `packages/core/src/application/note/renameNote.ts`, `packages/core/src/application/note/restoreNote.ts`, `packages/core/src/application/note/restoreNoteRevision.ts`, `packages/core/src/application/note/trashNote.ts`, `packages/core/src/application/note/updateNoteBody.ts`, `packages/core/src/application/note/view.ts`, `packages/core/src/application/note/__tests__/applyTextNodeEdits.test.ts`, `packages/core/src/application/note/__tests__/changeNoteStyleMode.test.ts`, `packages/core/src/application/note/__tests__/deleteNotesForOwner.test.ts`, `packages/core/src/application/note/__tests__/editingHarness.ts`, `packages/core/src/application/note/__tests__/emptyTrash.test.ts`, `packages/core/src/application/note/__tests__/getNote.test.ts`, `packages/core/src/application/note/__tests__/listNoteRevisions.test.ts`, `packages/core/src/application/note/__tests__/listNotes.test.ts`, `packages/core/src/application/note/__tests__/listTrashedNotes.test.ts`, `packages/core/src/application/note/__tests__/purgeExpiredTrash.test.ts`, `packages/core/src/application/note/__tests__/purgeNote.test.ts`, `packages/core/src/application/note/__tests__/renameNote.test.ts`, `packages/core/src/application/note/__tests__/restoreNote.test.ts`, `packages/core/src/application/note/__tests__/restoreNoteRevision.test.ts`, `packages/core/src/application/note/__tests__/trashNote.test.ts`, `packages/core/src/application/note/__tests__/updateNoteBody.test.ts`, `packages/core/src/application/ports/noteMovePort.ts`, `packages/core/src/application/ports/noteRouteStore.ts`, `packages/core/src/application/scope.ts`, `packages/core/src/application/di/types.ts`, `packages/core/src/application/execution/unitOfWork.ts`, `packages/core/src/application/cleanup/notePurgeFanOut.ts`, `packages/core/src/adapters/html/htmlProcessor.ts`, `packages/core/src/adapters/html/allowList.ts`, `packages/core/src/adapters/html/css.ts`, `packages/core/src/adapters/html/__tests__/htmlProcessor.test.ts`, `packages/core/src/adapters/memory/repositories/noteRepository.ts`, `packages/core/src/adapters/cloudflare/do/repositories/noteRepository.ts`, `packages/core/src/adapters/conformance/noteRepository.ts`, `packages/core/src/adapters/conformance/noteRouteStore.ts`, `packages/core/src/adapters/cloudflare/do/schema.ts`, `packages/core/src/adapters/memory/store.ts`, `packages/core/src/adapters/memory/scopeUnitOfWork.ts`, `packages/core/src/application/workers/subscribers.ts`（`note.purged` 購読の存在のみ）, `spec/usecases/note.md`, `spec/domains/note.md`, `spec/adr/013-html-sanitization-policy.md`, `spec/testcases/note/applyTextNodeEdits.md`, `spec/testcases/note/changeNoteStyleMode.md`, `spec/testcases/note/deleteNotesForOwner.md`, `spec/testcases/note/emptyTrash.md`, `spec/testcases/note/getExportStatus.md`, `spec/testcases/note/getNote.md`, `spec/testcases/note/listNotes.md`, `spec/testcases/note/listTrashedNotes.md`, `spec/testcases/note/purgeExpiredTrash.md`, `spec/testcases/note/purgeNote.md`, `spec/testcases/note/renameNote.md`, `spec/testcases/note/restoreNote.md`, `spec/testcases/note/restoreNoteRevision.md`, `spec/testcases/note/trashNote.md`, `spec/testcases/note/updateNoteBody.md`, `spec/inventory/adapter.md`（note 行）, `spec/inventory/test.md`（note 行）, `spec/inventory/usecase.md`（note 行）, `spec/domains/index.md`（note 行）
- スキップ: `apps/web/**` — フロントエンド担当
- スキップ: `docs/*.md`, `packages/core/package.json`, `pnpm-lock.yaml` — 横断・依存関係担当
- スキップ: `packages/core/src/adapters/cloudflare/__tests__/**`, `packages/core/src/adapters/memory/__tests__/**`, `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts` — adapter 横断担当（note の conformance ケース自体は `conformance/noteRepository.ts` / `noteRouteStore.ts` で確認）
- スキップ: `packages/core/src/adapters/{cloudflare/do,memory}/repositories/{storedFile,tagAssignment,backupRecord}Repository.ts`, `packages/core/src/adapters/conformance/{backend,backupRecordRepository,storedFileRepository,tagAssignmentRepository}.ts` — storage / tag / integration 担当
- スキップ: `packages/core/src/application/{storage,tag,usage,integration,identity}/**`, `packages/core/src/application/cleanup/{participants,personalCleanup}.ts`, `packages/core/src/application/di/{cloudflareRuntime,memoryRuntime}.ts`, `packages/core/src/application/ports/objectStorage.ts`, `packages/core/src/application/workers/{scopeTaskRunner.ts,__tests__/**}` — 各ドメイン / 基盤担当
- スキップ: `packages/core/src/domain/{integration,storage,tag}/**` — 各ドメイン担当
- スキップ: `spec/{database,pages,platform,presentation,manual-tests}/**`, `spec/domains/{integration,storage,tag}.md`, `spec/testcases/{integration,storage,tag}/**`, `spec/usecases/{identity,integration,job,storage,tag,usage}.md` — 各ドメイン / 横断担当
