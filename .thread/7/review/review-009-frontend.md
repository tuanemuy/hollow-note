### frontend

#### Blockers
- **[B-001]** `scrubForSurface` の URL スキーム判定が制御文字の迂回（`java&#9;script:` / `java&#10;script:` / 先頭の C0 制御文字）を素通りさせる。同じリポジトリ内に正しい判定が 2 つ（サーバーの `stripControls` → `schemeOf`、同じ島の `isSafeLinkUrl`）あるのに、面へ載せる側だけが `^\s*javascript:` の素朴な前置き比較で止まっている
  - 場所: `apps/web/app/components/note/NoteEditor/surfaces.tsx:313`（`/^\s*javascript:/i`）、対照: `apps/web/app/components/note/NoteEditor/editor.tsx:2474`（`isSafeLinkUrl` は `codePointAt <= 0x20` を落としてから判定）、`packages/core/src/adapters/html/htmlProcessor.ts:148`（`stripControls` は `[\u0000-\u0020\u007f]` を落としてから判定）
  - 理由: JSDoc（`surfaces.tsx:235-278`）が「未保存の本文が live DOM へ入る経路はすべてこれを通す」「属性ハンドラーと `javascript:` URL を落とす責任は渡す側にある」と定め、CSP に `script-src` が無いことも同所に明記している。つまりこの関数が編集画面の唯一の防波堤であり、その防波堤に既知の迂回が残っている。ブラウザーは URL のスキームを読む前に ASCII タブ / 改行 / 復帰と先頭・末尾の C0 制御文字を捨てるので、`<a href="java&#9;script:alert(1)">` はパース後の属性値が `"java\tscript:alert(1)"` になり、この正規表現には当たらないまま HTML モードのプレビュー（shadow root だが navigation は隔離されない）・ビジュアルの面・WYSIWYG の貼り付けに live で載る。ED-03 の中核シナリオが「保存済み Web ページのソースを貼る」である以上、貼られる HTML は設計上そのまま第三者由来であり、プレビューのリンクをクリックした時点でアプリのオリジンでスクリプトが走る（保存時のサニタイズは永続化を防ぐが、セッション中の実行は防がない）。サーバー側は `stripControls` でこの迂回を正しく塞いでおり、ADR 013「許可する URL スキーム」に対する適用点が 3 か所（サーバー・ツールバー・面）に散って、そのうち 1 か所だけ規則が違う状態になっている
  - 提案: URL スキームの判定を **1 つの純関数に集約**し、3 つの適用点すべてがそれを呼ぶ形にする。置き場所はフレームワーク非依存の純ロジックなので `packages/core/src/domain/note/services/`（ADR 013 の「許可する URL スキーム」表の実行形）が自然で、`htmlProcessor.ts` の `stripControls` / `schemeOf` / `isAllowedUrl(url, "navigation" | "resource")` をそこへ移して adapter がそれを使い、`surfaces.tsx` の `scrubForSurface` は URL 属性ごとに `isAllowedUrl(value, name === "href" || name === "xlink:href" ? "navigation" : "resource")` が偽なら属性を落とす、`editor.tsx` の `isSafeLinkUrl` は `isAllowedUrl(url, "navigation")` に置き換える。これで `node` プロジェクトに `java&#9;script:` / `javascript:` / `JAVASCRIPT:` / `data:text/html` の表駆動テストを 1 本置けば 3 適用点が同時に守られる（`scrubForSurface` そのものは DOM 依存で `node` プロジェクトでは走らないが、判定だけを外へ出せば規則は検証できる）。併せて、`scrubForSurface` が navigation 属性で `data:` を通していることも同じ表で揃う（現状は `javascript:` しか見ていない）

#### Warnings
- **[W-001]** 版を握る 3 つの島が、`OPTIMISTIC_LOCK_FAILURE`（や `NOTE_IS_TRASHED` 等の版が進んだことを示す失敗）のあとに**版を取り直さない**ので、共通文言の「もう一度お試しください」に従って押し直しても同じ古い版を送り続け、画面を離れるまで永久に失敗する
  - 場所: `apps/web/app/components/note/NoteDetail/detail.tsx:85`（`versionRef` は成功時にしか進まない。`saveTitle` / `onStyleMode` / `onTrash` の `catch`（147 / 197 / 221）は `setError` だけで `reconcile()` も再読も行わない）、`apps/web/app/components/note/NoteList/board.tsx:153`（`onTrash` の `catch` は `setError` して `return`、`row.version` は loader 由来で `reconcile()` されない）、`apps/web/app/components/note/TrashList/board.tsx:94` / `:111`（同上）
  - 理由: 3 か所とも `expectedVersion` を「画面が見た版」として運ぶ設計（`-action.tsx` の `updateNoteBodyFn` JSDoc）で、それ自体は正しい。しかし競合を検出したあとの遷移が無い — 編集画面（`editor.tsx`）は `conflict` 状態と `readNoteEditStateFn` による版の引き直しを持つのに、詳細・一覧・ゴミ箱の島は失敗を文言に落として終わる。ワークスペースでは他のメンバーが本文を保存するだけで詳細画面のタイトル自動保存・表示スタイル切替・削除がすべて競合し、その画面では二度と成功しない。一覧とゴミ箱では楽観的除去が巻き戻った行がそのまま古い版を持ち続ける。`spec/pages/index.md:283` の P-11「操作実行中 — 楽観的更新と失敗時の巻き戻し」は巻き戻しまでしか定めておらず、巻き戻したあとに再試行が成立する状態へ戻す責務が抜けている
  - 提案: 共通原因は「失敗時に版の出所を更新しない」ことなので、集約先を 1 つにする。一覧・ゴミ箱は失敗の `catch` でも `reconcile()`（loader の再実行で `rows[].version` が更新される）を呼ぶ — 成功時と同じ関数で済む。詳細の島は `versionRef` が loader の外にあるので、失敗の `catch` で `readNoteEditStateFn`（既存）を呼んで `versionRef.current` / `savedTitle` / `styleMode` を正本で揃え直すか、サーバーコンポーネント側で `<NoteDetailIsland key={note.version} …>` として `reconcile()` のたびに版ごと組み直す（`trashed` 状態は `onTrash` が `reconcile()` しないので消えない）。どちらにせよ「競合したら版を引き直す」を 1 関数（`refreshVersion`）に閉じて 4 つの `catch` から呼ぶ
- **[W-002]** HTML モードの「HTML の構文を補正しました」が、構文の補正ではなく**直列化の差**にも出る
  - 場所: `apps/web/app/components/note/NoteEditor/surfaces.tsx:373-384`（`analyzeMarkup`: `repaired: parsed === source ? null : parsed`）、表示は `:483-488`
  - 理由: `template.innerHTML` の読み戻しは正規化された直列化を返すので、`<br/>`（正しい HTML5）、`<p class='a'>`（単引用符）、`<P>`（大文字）、`&#x27;` / `&apos;` などのエンティティ表記、属性値の中の `"` の表記、`<img src=x>` の引用符省略（これは HTML5 では正しい構文）がすべて「閉じていないタグや引用符の無い属性値を補いました」の警告になる。ED-03「構文が壊れている場合、保存前に警告し、補正後の結果をプレビューで示す」の「壊れている」と一致しておらず、TC-18 の手順 1 は通るが、正しいソースを貼った利用者にも常時警告が出て、本当に壊れているときの警告が埋もれる
  - 提案: ブラウザーのパーサーは構文エラーを報せないので、比較の前に**両辺を同じ正規化に通す** — `source` を一度パースして直列化した `parsed` を、もう一度パースして直列化した `reparsed` と比べる形では常に等しくなってしまうため、代わりに `source` 側だけを「直列化の差になりうる書き方」を潰した形（`tokenizeHtml` の出力を使って属性の引用符・タグ名の大文字小文字・自己終了記法を正規化した文字列）にしてから `parsed` と比べる。それが重いなら、少なくとも文言を「HTML を正規化しました」に落とし、「閉じていないタグや引用符の無い属性値を補いました」と断言しない。TC-18 手順 1 の期待結果の断言もそれに合わせる
- **[W-003]** 新規作成の初回保存で、タイトル未入力のときに本文の先頭行からタイトルを付ける（ED-01 異常系、TC-02 手順 2）経路がどこにも無い
  - 場所: `apps/web/app/routes/notes/-action.tsx:388-462`（`createNoteWithBodyFn` は `createBlankNote({ title: "" })` → `updateNoteBody` で、応答の `title` は `read.title`（本文反映前の「無題」））。`packages/core/src/domain/note/note.ts:336`（`Note.updateBody` はタイトルに触れない。先頭行からの派生は `applyConversionResult:274` の変換経路にしか無い）
  - 理由: `spec/scenario/editing.md:28`「タイトルが未入力のまま保存した場合、本文の先頭行から自動で仮のタイトルを付ける。本文も空なら「無題」とする」と、本 PR が編集した `spec/manual-tests/editing.md` TC-02 手順 2「タイトルが `最初の行です` になっている」が、この経路を前提にしている。plan.md は ED-01 を「含まれないもの」に置くが、同時に「本スライスは `/notes/new` の編集体験（PAGE-p12-002）だけを扱う」と言っており、初回保存はまさにその編集体験である。TC-02 は AC-13 の範囲（TC-04〜34）外なので受け入れ基準の違反ではないが、本 PR が TC-02 手順 3 を書き換えて手順 2 をそのまま残した以上、手順書としては「誰も実装していない期待結果」を canon に残した形になる
  - 提案: 判断を 1 つに倒す。(a) 本スライスで持つなら `createNoteWithBodyFn` で `title` が空のときに `HtmlProcessor` の結果（`ProcessedHtml` に先頭見出し / 先頭行があるなら）から `NoteTitle.create(_, "auto")` を作って `renameNote` ではなく作成時の `title` に渡す（`createBlankNote` の前にサニタイズが要るので、順序は `process` → `createBlankNote(title)` → `updateNoteBody`）。(b) 持たないなら TC-02 手順 2 に「本スライスでは未実装（ED-01 の派生は取り込み経路のみ）」の確認ポイントを添え、`spec/scenario/editing.md:28` の記述を「取り込み由来の本文に限る」のかどうかを明確にする。いずれでも `createNoteWithBodyFn` の応答 `title` が「サーバーが決めた値」である点は保つ
- **[W-004]** `commit()` の中で、保存が**成功したあと**に走る `reseedIfUnchanged` → `reseedFromServer` の通信失敗が、保存の失敗（`retry: { kind: "save" }`）として分類される
  - 場所: `apps/web/app/components/note/NoteEditor/editor.tsx:799` / `:821`（`reseedIfUnchanged(sent)` が `try` の内側）、`:843`（`catch` は一律 `classifySaveFailure(error, { kind: "save" })`）
  - 理由: 経路単位の保存（`saveTextNodes`）や丸ごとの保存（`saveBody`）はもう通っており `rememberIdentity` で版も進んでいるのに、`confirm(sent, …)` に届く前に `readEditState` が落ちると確定値が 1 つ前のまま残る。そこで出る「再試行」は `commit(true)` を送り直すので、ビジュアルでは適用済みの編集が再送されて全件 `contentChanged`（「反映できなかった編集があります」の誤報）、HTML / WYSIWYG では同じ本文で `updateNoteBody` がもう一度走って版と `Revision` を 1 つずつ無駄にする。`RetryTarget` を分けた JSDoc（`:161-171`）の趣旨「落ちた往復そのものを送り直す」に対して、ここだけ落ちた往復（載せ直し）と送り直す往復（保存）が食い違う
  - 提案: 保存の `try` は `confirm(sent, settled(…))` と `markSaved()` まで（保存の確定）で閉じ、載せ直しは `try` の外に出して `applyMode` と同じ `classifySaveFailure(error, { kind: "mode", mode, acknowledged: false })` で分類する。確定してから載せ直す順序にすれば、載せ直しが落ちても次の保存は「丸ごと送る枝」に自然に落ちる（`takeSnapshot` の `pathwise` は確定値が握る）ので、`reseedIfUnchanged` が偽を返したときと同じ自己修復に乗る
- **[W-005]** 端末に保持した既定モードが `visual` で、開いたノートに編集可能なテキストノードが無いとき、逃げ先を `wysiwyg` に固定しているため、取り込み由来（`mayLoseDecoration`）のノートでは開いた直後に何も操作していないのに WYSIWYG 警告が立つ
  - 場所: `apps/web/app/components/note/NoteEditor/editor.tsx:549`（`preferred === "visual" && !visualAvailable ? "wysiwyg" : preferred`）
  - 理由: 逃げ先は「どんな本文でも開ける面」であるべきで、この島自身が `initialMode` の説明（`:436-441`）と `surfaceModeFor` の JSDoc で「それは HTML である」と結論している。`wysiwyg` へ倒すと `needsWysiwygWarning` の門に掛かって警告と `pendingMode` が立ち、利用者は「取りやめ」を押して初めて HTML の面で作業を始められる。ED-05「選んだモードは端末に保持」の趣旨（利用者の操作に由来しない警告を出さない）から外れる
  - 提案: `!visualAvailable` の逃げ先を `initialMode` にする（1 行）。`initialMode` が `wysiwyg`（装飾を失わないノート）なら従来どおり、`html` なら警告無しで開く
- **[W-006]** `spec/pages/index.md` の P-10 / P-11 状態表に「削除直後（元に戻す）」の状態が無い。コードは両画面にこの状態を持ち（`detail.tsx:284-306` の `trashed` Alert、`NoteList/board.tsx` の `trashed` Alert）、`spec/scenario/editing.md` ED-09 と `spec/manual-tests/editing.md` TC-12 手順 2 / 6、TC-33 もそれを前提にしている
  - 場所: `spec/pages/index.md:283`（P-11 状態表「操作実行中」の直後）、P-10 状態表（`:240` 前後）
  - 理由: 状態表は画面の canon で、島の `TrashedState` はそこに無い状態を実装している。「削除直後は画面上の『元に戻す』で取り消せる」は ED-09 の基本フローであり、P-11 の「操作実行中」（楽観的更新と巻き戻し）にも「見つかりません」にも含まれない — 削除後に画面に留まるという判断（`detail.tsx:222-224`「ここでは読み直さない」）は状態表に書かれて初めて設計になる
  - 提案: P-10 / P-11 の状態表に「削除直後 | ゴミ箱への移動が成功（画面に留まり「元に戻す」を出す。読み直しは行わず、元に戻すと表示へ戻る。応答の版を「元に戻す」の `expectedVersion` に使う）」を 1 行ずつ足す
- **[W-007]** 島から持ち上げた純関数にテストが無い。`docs/test.md:85` は「島から持ち上げた純関数は隣の `__tests__/` にテストを持つ」と定めており、`schema.ts` はそれに従っているが、同じスライスで増えた純関数は 1 つも守られていない
  - 場所: `apps/web/app/components/note/NoteEditor/editor.tsx:257`（`classifySaveFailure` — P-12 状態表「保存できません（内容の拒否）」の「補集合として閉じる」規則の実行形。module-private なので現状テスト不能）、`:2474`（`isSafeLinkUrl`、B-001 の集約先へ吸収されるなら不要）、`apps/web/app/components/note/NoteEditor/highlight.ts:109`（`tokenizeHtml` — 引用符の中の `>`、`a < b` の素の不等号、コメント、閉じていないタグの扱いを JSDoc で約束している）、`apps/web/app/components/note/NoteEditor/textNodes.ts:83`（`diffTextNodeEdits`）、`apps/web/app/components/note/NoteEditor/preferences.ts:93` / `:112`（`parseDraft` / `isExpired` — 読めない値と期限切れを落とす規則）
  - 理由: `classifySaveFailure` は「列挙していないコードは既定で `rejected` へ落ちる」という向きに閉じることが安全性の根拠（JSDoc `:213-226`）で、閉じる向きが逆転しても typecheck は通る。spec の閉じた表（P-12 状態表の「保存失敗」の集合 = `system` / `unknown` / `unauthorized`）をそのまま検証できる形であり、`docs/test.md` が例外として挙げる「閉じた spec の一覧を符号化した純関数」そのものである。`tokenizeHtml` / `parseDraft` は DOM を使わないので `node` プロジェクトでそのまま走る
  - 提案: `classifySaveFailure` と `TRANSIENT_ERROR_KINDS` / `BLOCKING_ERROR_CODES` を `NoteEditor/saveStatus.ts` へ持ち上げて export し、`NoteEditor/__tests__/saveStatus.test.ts` に「`system` / `unknown` / `unauthorized` → `failed`、`OPTIMISTIC_LOCK_FAILURE` → `conflict`、`NOTE_LOCKED_BY_JOB` → `locked`、4 つの blocking code → `blocked`、それ以外の `business` / `validation` / `notFound` → `rejected`」の表を置く。`highlight.ts` / `preferences.ts` / `textNodes.ts` の `diffTextNodeEdits` にも同じ場所へ表駆動のケースを足す
- **[W-008]** `errorDisplay.test.ts` の「辞書に載っていること」の検証が `not.toBe(共通文言)` で、検出力が無い
  - 場所: `apps/web/app/presentation/__tests__/errorDisplay.test.ts:119-127`
  - 理由: 辞書の値が空文字でも、別のコードの文言と取り違えていても、共通文言と違いさえすれば通る。同じファイルの他のケース（`NOTE_LOCKED_BY_JOB` の「待って」、`STORAGE_FILE_TOO_LARGE` の「5 MB」「200 MB」）は `toContain` で意味を固定しており、この 1 本だけが規則から外れている
  - 提案: `EDITING_CODES` を `[kind, code, 期待するキーワード]` の 3 列にして `toContain(keyword)` で固定する（`NOTE_INVALID_TITLE` → "200"、`REVISION_NOT_FOUND` → "20 版"、`NOTE_NOT_TRASHED` → "再読み込み"、`USAGE_STORAGE_QUOTA_EXCEEDED` → "容量" など）。数値はドメイン定数（`MEDIA_*_MAX_BYTES`、`TITLE_MAX_LENGTH`）から組んで、辞書の数字がドメインから逸れたら落ちる形にする
- **[W-009]** `adrReference.test.ts` が見張るのはソース（`apps/web/app` / `packages/core/src`）だけで、CLAUDE.md が同じく禁じている `spec/` / `docs/` からの `.thread/` 引用は検査していない
  - 場所: `apps/web/app/presentation/__tests__/adrReference.test.ts:27`（`SOURCE_ROOTS`）、`:39`（`.thread/` の検査）
  - 理由: CLAUDE.md「Never cite it from code, `spec/`, or `docs/`」の 3 つのうち 2 つが規則の外にある。現時点で `spec/` / `docs/` に違反は無い（grep で確認済み）ので実害は無いが、ラウンド 008 で「以後この違反は緑のまま通らない」と決めた仕組みが半分しか効いていない
  - 提案: `.thread/` 引用の検査だけは対象に `spec/` と `docs/` の `.md` を加える（ADR 番号の解決は markdown では相対リンクで担保されるので、そちらは対象外のままでよい）

#### テスト保証
- `renderErrorMessage の編集 / ゴミ箱コードの辞書（errorDisplay.ts:MESSAGE_BY_CODE）` — 守っているテスト: `apps/web/app/presentation/__tests__/errorDisplay.test.ts:tells the reader to wait rather than retry while a job holds the note` / `tells the reader what to change rather than to retry an unprocessable body` / `names both upload purposes in the shared storage codes`。「辞書に載っていること」の網羅は `not.toBe` で検出力なし → [W-008]
- `httpStatusFor の kind 写像（errorResponse.ts:httpStatusFor）` — 守っているテスト: `apps/web/app/presentation/__tests__/errorResponse.test.ts:keeps the editing and trash codes on their kind mapping`
- `本文の転送上限が 800 KB の業務拒否より緩いこと（schema.ts:updateNoteBodySchema / createNoteWithBodySchema）` — 守っているテスト: `apps/web/app/components/note/__tests__/schema.test.ts:takes a raw body of exactly the ceiling and refuses one past it` / `applies the same ceiling to the first save of a new note` / `lets an oversized body reach NOTE_CONTENT_TOO_LARGE, not INVALID_INPUT`
- `配信口が avatar / media だけを公開し他の purpose を 404 にすること（storage.$.tsx:PUBLICLY_SERVED_PURPOSES）` — 守っているテスト: `apps/web/app/routes/__tests__/storage.delivery.test.ts:serves exactly the purposes spec/domains/storage.md publishes, and refuses the rest of the key space` / `TC-storage-176`
- `ADR 参照が spec/adr/ に解決し .thread/ を引かないこと（adrReference.test.ts）` — 守っているテスト: `apps/web/app/presentation/__tests__/adrReference.test.ts:resolves every ADR number to a file under spec/adr/` / `cites no work-log path from source`。`spec/` / `docs/` は対象外 → [W-009]
- `面へ載せる前の on* / javascript: URL の除去（surfaces.tsx:scrubForSurface）` — 守られていない（制御文字迂回が通る） → [B-001]
- `保存失敗の分類が補集合として閉じること（editor.tsx:classifySaveFailure）` — 守られていない → [W-007]
- `HTML ソースの色分け（highlight.ts:tokenizeHtml）` / `退避の解析と期限（preferences.ts:parseDraft / isExpired）` / `経路差分（textNodes.ts:diffTextNodeEdits）` — 守られていない → [W-007]
- `競合後に版を取り直して再試行が成立すること（detail.tsx / NoteList/board.tsx / TrashList/board.tsx）` — 守られていない（実装そのものが無い） → [W-001]
- `構文補正の警告が壊れたソースにだけ出ること（surfaces.tsx:analyzeMarkup）` — 守られていない → [W-002]
- `初回保存でタイトルが本文の先頭行から派生すること（-action.tsx:createNoteWithBodyFn、ED-01 / TC-02 手順 2）` — 守られていない（実装無し） → [W-003]
- `server function のスキーマと usecase 入力型の一致（schema.ts ↔ application/note/*.ts, storage/storeMedia.ts）` — 突き合わせ済み: `updateNoteBodySchema` ↔ `UpdateNoteBodyInput`（`reason` は `Extract<RevisionReason, …>` と一致、`importReferences` は optional に boolean を渡す）、`applyTextNodeEditsSchema` ↔ `TextNodeEdit`、`renameNoteSchema` ↔ `RenameNoteInput`、`restoreNoteRevisionSchema` ↔ `RestoreNoteRevisionInput`、`changeNoteStyleModeSchema` ↔ `ChangeNoteStyleModeInput`（`styleMode: string` に 2 値 enum）、`trashNoteSchema`（`excludingJobId: null` は handler が補う）↔ `TrashNoteInput`、`restoreNoteSchema` ↔ `RestoreNoteInput`、`purgeNoteSchema`（`kind: "userRequest"` は handler が補う）↔ `PurgeNoteInput`、`emptyTrashSchema` ↔ `EmptyTrashInput`、`noteMediaUploadSchema` ↔ `StoreMediaInput`（`body` は handler が `arrayBuffer` から作る）。応答側: `RenamedNoteView.title/version`、`NoteStyleModeView.styleMode/version`、`TrashedNoteView.version`、`RestoredNoteView.version`、`RestoredNoteRevisionView.version`、`AppliedTextNodeEditsView.skipped[].path`、`UpdatedNoteBodyView.removed`、`StoreMediaView.url/mimeType`、`EmptyTrashView.mode/purgedCount/jobIds`、`TrashedNoteListItemView.purgeAfter/trashedAt/version`、`NoteListItemView.version/visibility`、`NoteDetailView.trashedAt/sourceFileId/styleMode/permissions.canDelete` はいずれも島の読み方と一致。`useBlocker({ shouldBlockFn, enableBeforeUnload })` は `@tanstack/react-router@1.170.18` の型と一致 — 型で守られている（不一致は typecheck で落ちる）
- `spec/presentation/index.md からの「ストレージの配信元」行の削除` — `StorageUrlPolicy.create` が `deliveryBaseUrl` を `ObjectStorage.publicUrl` から受け取り（ADR 049）、`AppConfig` / `.env.example` / DI に該当項目が無いことを確認。記述は実装と一致
- `spec/manual-tests/editing.md の TC-04〜TC-34 の書き換え` — TC-06 手順 2（往復中は「了解して進む」が押せない: `disabled={busy}`）、TC-08 手順 6（書式バー・挿入・「版を復元」は `busy` に掛からない、各版の「復元」だけ `busy`）、TC-10 手順 6、TC-13 手順 5〜7（`jobIds` の表示）、TC-19 / 20（`rejected` に再試行と退避が無い）、TC-21 手順 8、TC-23 手順 6〜7、TC-24 手順 4、TC-28 手順 3（`!canEdit` は StateNotice）、TC-30 手順 2（空のときボタンごと消える）、TC-32 手順 3（settings loader が `canWrite` を渡す）をコードと突き合わせ、いずれも一致。TC-02 手順 2 だけが実装と一致しない → [W-003]。TC-18 手順 1 の断言は [W-002] の文言に従属

#### カバレッジ
- 確認: `apps/web/app/components/layout/ScopeToken/index.tsx`, `apps/web/app/components/layout/ScopeToken/listing.ts`, `apps/web/app/components/note/NoteBody/index.tsx`, `apps/web/app/components/note/NoteDetail/detail.tsx`, `apps/web/app/components/note/NoteDetail/index.tsx`, `apps/web/app/components/note/NoteDetail/menu.tsx`, `apps/web/app/components/note/NoteEditor/editor.tsx`, `apps/web/app/components/note/NoteEditor/frame.tsx`, `apps/web/app/components/note/NoteEditor/highlight.ts`, `apps/web/app/components/note/NoteEditor/index.tsx`, `apps/web/app/components/note/NoteEditor/preferences.ts`, `apps/web/app/components/note/NoteEditor/skeleton.tsx`, `apps/web/app/components/note/NoteEditor/surfaces.tsx`, `apps/web/app/components/note/NoteEditor/textNodes.ts`, `apps/web/app/components/note/NoteList/board.tsx`, `apps/web/app/components/note/NoteList/index.tsx`, `apps/web/app/components/note/TrashList/action.ts`, `apps/web/app/components/note/TrashList/board.tsx`, `apps/web/app/components/note/TrashList/index.tsx`, `apps/web/app/components/note/__tests__/schema.test.ts`, `apps/web/app/components/note/schema.ts`, `apps/web/app/presentation/__tests__/adrReference.test.ts`, `apps/web/app/presentation/__tests__/errorDisplay.test.ts`, `apps/web/app/presentation/__tests__/errorResponse.test.ts`, `apps/web/app/presentation/errorDisplay.ts`, `apps/web/app/routeTree.gen.ts`, `apps/web/app/routes/__tests__/storage.delivery.test.ts`, `apps/web/app/routes/notes/$noteId_.edit.tsx`, `apps/web/app/routes/notes/-action.tsx`, `apps/web/app/routes/notes/new.tsx`, `apps/web/app/routes/notes/trash.tsx`, `apps/web/app/routes/storage.$.tsx`, `apps/web/app/routes/workspaces/$workspaceId/-action.tsx`, `apps/web/app/routes/workspaces/$workspaceId/notes/$noteId_.edit.tsx`, `apps/web/app/routes/workspaces/$workspaceId/notes/index.tsx`, `apps/web/app/routes/workspaces/$workspaceId/notes/new.tsx`, `apps/web/app/routes/workspaces/$workspaceId/notes/trash.tsx`, `apps/web/app/routes/workspaces/$workspaceId/settings/-action.tsx`, `apps/web/app/routes/workspaces/$workspaceId/settings/route.tsx`, `spec/manual-tests/editing.md`, `spec/pages/index.md`, `spec/presentation/index.md`（計 42 件）
- スキップ: `packages/core/package.json`, `pnpm-lock.yaml` — 依存とロックファイル（担当外。判断に必要な範囲で `@tanstack/react-router` の版だけ node_modules から参照）
- スキップ: `packages/core/src/adapters/**`（31 件）— アダプター層（担当外。契約の突き合わせのため `adapters/html/htmlProcessor.ts` の URL 判定だけを参照）
- スキップ: `packages/core/src/application/**`（69 件）— ユースケース層（担当外。入力 / 出力型の突き合わせのため `note/*.ts` / `storage/storeMedia.ts` / `storage/view.ts` / `note/view.ts` の型定義だけを参照）
- スキップ: `packages/core/src/domain/**`（20 件）— ドメイン層（担当外。`note.ts` の `updateBody` / `applyConversionResult`、`valueObject.ts` の `NoteTitle`、`uploadValidationPolicy.ts` の定数、`storageUrlPolicy.ts` だけを参照）
- スキップ: `spec/adr/013-html-sanitization-policy.md`, `spec/database/index.md`, `spec/domains/index.md`, `spec/domains/integration.md`, `spec/domains/note.md`, `spec/domains/storage.md`, `spec/domains/tag.md`, `spec/inventory/adapter.md`, `spec/inventory/test.md`, `spec/inventory/usecase.md`, `spec/platform/index.md`, `spec/testcases/**`（20 件）, `spec/usecases/**`（4 件）— 設計側の担当外ドキュメント（35 件）
