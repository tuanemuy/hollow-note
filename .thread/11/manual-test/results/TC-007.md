# TC-007: 既存 Node 参照ランタイムの回帰（アプリの起動と主要動線）

**結果**: PASS
**対応する受け入れ基準**: AC-7
**セッション**: verify-tc-007
**サーバー**: http://localhost:3000 （`pnpm dev`）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `cp apps/web/.env.example apps/web/.env` し `APP_URL=http://localhost:3000` / `OAUTH_DEV_MODE=true` を設定 | 設定が反映される | `.env` 17 行目 `APP_URL=http://localhost:3000`、30 行目 `OAUTH_DEV_MODE=true` | PASS |
| 2 | `pnpm dev` で開発サーバーを起動 | サーバーが起動する | `VITE v8.1.5 ready in 1344 ms` / `➜ Local: http://localhost:3000/`。初回リクエストで `[server.node] worker runner started` と `[outbox] pruned 0 processed event(s)` | PASS |
| 3 | ブラウザで `http://localhost:3000` を開く | トップページが表示される | `get title` = `Hollow`、`get url` = `http://localhost:3000/`。`heading "チームのための ひとつのノート。" [level=1]` / `link "無料ではじめる"` / `link "サインイン"` を確認 | PASS |
| 4 | 開発用サインインでアカウントを作成しサインイン | サインインが完了する | 「無料ではじめる」→ `/signup`（`heading "アカウントを作る"`）→「Google で続ける」→ `/dev/oauth/authorize?client_id=dev-google&redirect_uri=...`（`heading "開発用 ID プロバイダー"`）。メール `manual-test-11@example.com` / 表示名 `Manual Test 11` を入力し「許可する」→ `http://localhost:3000/notes` に着地。`heading "個人" [level=1]` と `button "アカウントメニュー"` を確認 | PASS |
| 5 | ノートを 1 件新規作成し、開いて表示されることを確認 | 一覧と詳細の両方で表示される | 「白紙から書く」→ `http://localhost:3000/notes/01a03c63-42b4-737b-b3dc-426232b760a0`。詳細本文 `非公開·2026年8月26日` / `# 無題` / `このノートは白紙です。`。「ノート一覧」へ戻ると `region "ノート"` 内に `link "無題 非公開。 更新 8月26日 13:45 8月26日"`。その行を再度クリックすると同じ URL の詳細に戻り、同じ本文を表示 | PASS |

## 確認ポイント

### 起動時コンソールの Cloudflare 関連エラー・警告

**なし。** サーバーログ全文に対する `grep -inE "wrangler|workerd|miniflare|cloudflare|d1|durable|r2|binding|ERROR|WARN|error:"` は 0 件（`(no match)`）。

ログに出た行は以下のみで、いずれも通常の起動・イベント配信ログ:

- `[vite] (client|ssr|rsc) Re-optimizing dependencies because lockfile has changed` — 依存追加後の初回起動で出る vite の情報ログ（Cloudflare 由来ではない）
- `VITE v8.1.5 ready in 1344 ms` / `➜ Local: http://localhost:3000/`
- `[server.node] worker runner started`
- `[outbox] pruned 0 processed event(s)`
- `[queue] received identity.user.created` / `identity.user.emailVerified` / `identity.identity.added` / `note.created` と、それぞれに対応する `[subscribers] no subscriber for ...`

### ブラウザコンソールの新規エラー

**なし。** `agent-browser ... errors` は空。`console` の出力は `[debug] [vite] connecting... / connected.` と `[info] Download the React DevTools ...` のみ。

## 使用データ

- 開発用 IdP でサインインしたアカウント: `manual-test-11@example.com` / 表示名 `Manual Test 11`
- 作成したノート: `01a03c63-42b4-737b-b3dc-426232b760a0`（無題・白紙）

永続化は in-memory のため、サーバー停止で消滅する。後始末は不要。
