### 合成・スキーマ・テストハーネス・spec/docs

#### Blockers

なし。

#### Warnings

- **[W-001]** `.thread/11/testing.md` の確認項目 4（AC-5）の「確認ポイント」が最終状態と矛盾する
  - 場所: `.thread/11/testing.md:確認項目 4「deleteFilesByOwner の SQL 文数の実測」`
  - 理由: 「**件数を変えても文数が変わらないこと（比例していないこと）を観測しているか**」と書いてあるが、確定した実測は `4n + 3` 文で、文の総数と読み側の RPC 往復は**件数に比例する**。比例しないのは commit の回数（常に 1）と、outbox flush（多行 INSERT 1 文）だけである。この決着は `spec/platform/index.md`「実行予算と分割単位」→「Scope DO」・`spec/adr/056` のコンテキスト・`deleteFilesByOwner.test.ts` の 4 ケースがそろって述べており、canon 側は一貫している。取り残されているのは testing.md の 1 行だけだが、この 1 行は Phase 4 の動作検証が読む手順書であり、そのまま実行すると「比例している → 失敗」という偽の赤を出す。手順 2（「実測値が 3 文でなかった場合、spec の該当行が実測値に改められているかを確認する」）と期待結果は改訂後の姿を許容しているので、確認ポイントだけが古い
  - 提案: 確認ポイントを実測後の主張へ置き換える。たとえば「**commit が件数によらず 1 回**であること（`counts.commits` が 10 件でも 40 件でも 1）と、outbox flush が多行 INSERT 1 文であること。文の総数と読みの往復は件数に比例してよく、その比例定数（`4n + 3`）が `spec/platform/index.md` の記述と一致していること」。あわせて目的の「『列挙 1 ＋ 多行 DELETE 1 ＋ 多行 outbox INSERT 1 の 3 文』という設計目標」も、現在の spec の文言（「書き込みをバッチ件数によらず 1 回の原子適用にまとめる」）へ揃えると、手順書を単体で読んだ人が改訂前の目標を再導入しない

- **[W-002]** `.thread/11/testing.md` の確認項目 6（AC-7）が期待する node プロジェクトの件数が、増分の説明を持たない
  - 場所: `.thread/11/testing.md:確認項目 6「既存 Node 参照ランタイムの回帰」の期待結果`
  - 理由: 「既存 76 ファイル・978 件が失敗 0 で通る（**本 Issue が適合スイートにケースを足した場合はその増分だけ増える**）」とあるが、実測は 77 ファイル・984 passed / 3 skipped で、しかも増分の出どころは適合スイートではない。内訳は (a) 新設の `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`（node プロジェクト、+1 ファイル・+4 ケース）、(b) `application/workers/__tests__/scopeTaskRunner.test.ts` に足した 2 ケース。`packages/core/src/adapters/conformance/` は 1 行も変わっていない（`git diff origin/main...HEAD -- packages/core/src/adapters/conformance/` が空）ので、括弧内の免責はこの差分を説明しない。Phase 4 は「76 / 978」と突き合わせたときに、説明の付かないずれとして扱わざるを得ない
  - 提案: 期待結果を「77 ファイル・984 passed / 3 skipped」へ更新し、増分の内訳（上の (a) (b)）を 1 行添える。あわせて確認ポイントの「`packages/core/src/adapters/memory/` と `apps/web/` に振る舞いの変更が入っていないこと」は今回の実測どおり **差分ゼロ**（`git diff origin/main...HEAD -- packages/core/src/adapters/memory apps/web` が空出力）なので、そちらが AC-7 の本体であることを期待結果側に上げてもよい

#### 受け入れ基準の実測

すべて本ワークツリー（`issue/11/cloudflare-d1-do-r2-adapters` @ `1cacbcc`）で実行した。

- **AC-1（実装チェックリスト全行・スタブ不可）— 満たしている。**
  Issue 本文の 5 行はいずれも実体を持つ。(1) 共有適合スイートは `packages/core/src/adapters/conformance/` に既存の 30 スイート（本 PR では 1 行も変更なし）、(2) global D1 は `d1/migrations/0001_global_schema.sql`（22 表）＋ `d1/repositories/` 19 実装、(3) scope DO は `do/schema.ts`（15 表）＋ `do/repositories/` 10 実装＋ `do/scopeObject.ts` の alarm / `transactionSync` 境界、(4) R2 は `r2/objectStorage.ts`、(5) 全アダプターが適合スイートを通る（下記 AC-2）。`packages/core/src/adapters/cloudflare/` と `application/di/cloudflareRuntime.ts` に `TODO` / `FIXME` / `not implemented` / 空実装は 0 件（唯一の grep ヒットは `scopeTaskQueue.ts:31` の JSDoc 本文「a backend answering with an empty array has not implemented it」で、契約の説明文）。

- **AC-2（30 の共有スイートを全件パス）— 満たしている。**
  `pnpm test:workers` → **22 ファイル / 366 passed / 0 skipped / 0 failed（8.43s、exit 0）**。`conformanceCoverage.test.ts` が絶対値と集合の両方を固定している — `PERSISTENCE_SUITES = 30`、`sorted(cloudflareCalls) === sorted(memoryCalls)`、`exported.size === 31`（永続 30 ＋ `signInOAuthClient`）、`sorted(namesIn(testFiles)) === sorted(exported)`。CF 側の入口は `__tests__/conformance/` の 7 ファイルで、いずれも `describeXxxContract(BACKEND, makeCloudflareConformanceBackend)` を行頭の平坦な呼び出しとして並べている。

- **AC-3（実バインディングに対して走る／モック代替なし）— 満たしている。**
  `conformanceCoverage.test.ts` の `hands each backend's suites that backend's own factory` が、CF 入口ファイル群に現れる `make*ConformanceBackend` 識別子の集合が `["makeCloudflareConformanceBackend"]` 1 件だけであることを固定する（memory 側も対称）。実バインディングであることは `harness.test.ts` が実物で観測 — `env.{GLOBAL_DB,OBJECT_STORAGE,SCOPE_OBJECT}` の存在、`applyD1Migrations` 後の `sqlite_master` に `GLOBAL_TABLES` 全 22 表、DO 初回接触での `SCOPE_TABLES` 全表生成、`json_each` の展開、`nodejs_compat` 由来の `node:async_hooks` / `crypto.subtle`。`conformanceBackend.ts` は `env.GLOBAL_DB` / `env.OBJECT_STORAGE` / `env.SCOPE_OBJECT` だけを握り、代替実装への読み替え経路を持たない。

- **AC-4（transaction / 再試行 / 冪等性 / lease 回収の統合テスト）— 満たしている。**
  plan.md が名指した 4 点がそれぞれケースとして存在し、全件緑。(a) D1 batch の原子性: `durability.test.ts` の `keeps no part of a D1 batch whose middle statement is refused` / `keeps no part of a global unit of work whose commit is refused` / `rolls a scope write-set back inside transactionSync and publishes no index`。(b) 冪等性: `idempotency.test.ts` の `applied_operations` / `processed_events` / outbox id 衝突（`folds a re-saved outbox id onto the stored row instead of replacing it`）。(c) lease 回収: `lease.test.ts` の `lets a second writer reclaim a lapsed lease without moving the row`。(d) R2: `r2.test.ts` の `leaves one whole object behind when two writes race for a key` / `treats a delete of absent keys as done` / `spends the 1,000-key delete limit in chunks`。加えて `alarm.test.ts` 19 ケース、`globalConcurrency.test.ts` 13 ケース、`projectionConcurrency.test.ts` 7 ケース、`routeGuard.test.ts`。

- **AC-5（`deleteFilesByOwner` の文数を実測し、届かなければ spec を実測値へ改める）— 満たしている（W-001 は手順書側の取り残し）。**
  実測は **1 turn `4n + 3` 文**（読み `2n + 2` ＋ commit 内 `2n + 1`、commit は件数によらず 1 回）。`deleteFilesByOwner.test.ts` が 4 ケースで固定 — `n=10` で reads 22 / commitStatements 21 / 合計 43、`n=40` で executed 163、`commits` は両方 1、差分（`60 = 2×30`）で比例部分を分離。`countExecuted` が DO 内の `state.storage.sql.exec` を包んで「送った文数＝実行した文数」まで観測しており、executor 呼び出し数の言い換えになっていない。spec 側は `spec/platform/index.md`「実行予算と分割単位」→「Scope DO」を実測値へ改訂済み（旧「3 文」の行を削除し、設計目標を「書き込みは件数によらず 1 回の原子適用」へ、`4n + 3` を上限ではなく設計目標として明記、どのバックエンドが届かないかは ADR 056 へ寄せる — ADR 056 決定 2 / 3 の通り）。`spec/adr/056` のコンテキストも「今日ある 2 つのバックエンドはいずれも届かない」＋ CF の根拠へ更新済み。**`spec/testcases/storage/deleteFilesByOwner.md` は変更されていない**（変更ファイル一覧に不在）。

- **AC-6（settle の fencing の決着）— 満たしている。**
  「`leaseMs` を十分に取る運用で足りる」を選び、契約は変えていない（claim token を足していないので適合スイート・`spec/inventory/adapter.md` の追随は不要）。前提の明文化は 3 か所にそろっている。(1) ポート JSDoc `application/ports/scopeTaskScheduler.ts` — 「The lease is advisory: settling addresses a row by `(kind, operationId)` alone and carries no fencing token」＋ `leaseMs` の下限（最悪ケースの turn 所要時間）と上限（age SLO）の両方、＋ 破れたときの帰結（personal cleanup の鎖が切れて `deleting` が残る）。(2) `spec/platform/index.md`「Scope Alarm」— 「1 つの scope に対する同時 writer は、その object 自身の Alarm turn 1 本を既定とする」、driver ごとの writer 多重度の根拠、レジストリが driver を決めること、中央 runner 配備では「起動が重ならないことが単一 writer 前提そのもの」、実配備前に設計し直す条件。(3) 対比として `GlobalMaintenanceRunStore` 側は「run の lease は fencing である」と明示的に別扱いにしてある。値の注入点も決着済み — `SCOPE_TASK_LEASE_MS`（中央 runner は環境変数、object は同名の `ScopeObjectEnv` 変数、未設定は定数、不正値は `dataIntegrityError`）で、`do/scopeObject.ts:53-84` と `docs/runtime_node.md` の環境変数表が一致する。

- **AC-7（既存 Node ランタイムが緑のまま）— 満たしている。**
  `git diff origin/main...HEAD -- packages/core/src/adapters/memory apps/web` → **空出力**（memory バックエンドと Node entry に 1 行の変更もない）。`pnpm typecheck` exit 0（root `tsgo` ＋ `packages/core` の `tsgo && tsgo -p tsconfig.cloudflare.json` ＋ `apps/web`）。`pnpm lint` exit 0（530 ファイル、fix 0、info 2 は既存の biome migrate 告知）。`pnpm format:check` exit 0（543 ファイル）。`pnpm test:node` → 77 ファイル / 984 passed / 3 skipped、exit 0。`pnpm test` → 99 ファイル / 1350 passed / 3 skipped、exit 0（77+22 / 984+366 で両プロジェクトの和と一致し、二重実行も取りこぼしもない）。`pnpm build:node` exit 0。件数の増分は本文 W-002 の通り 76→77 ファイル・978→984 件で、適合スイート由来ではない。TZ ピンは `vitest.config.ts` の node プロジェクトに残り、workers 側は設定していない。

- **AC-8（乖離の解決が ADR 046 に従う／適合スイートに足したなら memory も通る）— 満たしている。**
  `packages/core/src/adapters/conformance/` の差分は **0 ファイル**。したがって「ケースを足したなら memory も同じケースを通る」条件は空虚に成立し、`spec/inventory/adapter.md` に新規 ADP 行も要らない（ADR 052 の採番規則に触れない）。倒した乖離はすべて「契約を実態へ寄せる／責務を呼び出し側へ移す」側で、JSDoc・spec 本文・台帳の要点欄が同時に動いている — `AuthTokenRepository.findPendingByUserAndPurpose`（「複数 pending は許され、どれが返るかは未定義」: ポート JSDoc ＋ `spec/domains/identity.md` ＋ ADP-identity-039 ＋ DOM-identity-060 ＋ `spec/usecases/identity.md` の「部分一意制約下の」削除）、`PublicNoteProjectionWriter.removeForPurge`（ack 契約の撤回: ポート JSDoc ＋ `spec/domains/note.md`）、`{Local,Public}NoteProjectionWriter` の `ConflictError("OPTIMISTIC_LOCK_FAILURE")`（ポート JSDoc ＋ `spec/domains/note.md` のエラーケース ＋ 畳まない理由）、`ScopeTaskScheduler.claimDue` の同（ポート JSDoc ＋ 呼び出し側 `scopeTaskRunner.ts` の 2 ケース）、cursor の「署名」撤回（下記 AC-9）。

- **AC-9（新規の物理スキーマと spec の持ち分変更が反映されている）— 満たしている。**
  migration は **`0001_global_schema.sql` の 1 本だけ**（`d1/migrations/` にほかのファイルなし）。新規に決めた形はどちらも `spec/database/index.md` に節を持ち、DDL と 1 対 1 — `#### _occ_guard`（`id integer PRIMARY KEY, CONSTRAINT _occ_guard_conflict CHECK (id <> 0)`、行を持たないこと、条件付き更新の直前に積む理由、制約名を固定する理由）と `#### scope_task_due_index`（4 列 PK ＋ `due_at` / `priority` / `lease_expires_at`、索引 `(priority, due_at, kind, operation_id)`、publish がスライス全置換であること、有界化（優先度ごと 25 / 全体 100）、drift の非対称と再試行 alarm）。両者とも `d1/schema.ts` / `do/schema.ts` / migration の実物と一致する。`spec/` の持ち分を変えた決定は 4 系統あり、いずれも実装と 1 対 1 だった。
  - **ADR 063 の新設と cursor の「署名」撤回** — `spec/adr/063-public-cursor-not-authenticated.md` を起こし、`spec/adr/index.md` の一覧と前提依存マップの両方へ登録済み。波及先は `spec/adr/021`（対象 5 メソッドを名指しし、公開 workspace 一覧は署名のまま据え置く旨を明記）、`spec/platform/index.md`、`spec/domains/note.md`、`spec/usecases/{identity,note}.md`、`spec/testcases/` 4 ファイル、`spec/inventory/{adapter,domain,frontend,test,usecase}.md`。実装側は `cloudflare/cursor.ts` が base64url + fingerprint 照合のみで鍵を持たず、JSDoc が「not authenticated / never a capability」を明言。spec に残る「署名 cursor」は `spec/database/index.md:992`（`workspace_directory` の公開一覧）だけで、これは ADR 063 が「範囲外として残るもの」と自分で宣言し ADR 021 が同じことを述べている箇所なので整合している。
  - **「有界な掃引 / 削除」の規約の新設** — `spec/database/index.md:16` に「期限切れ / 旧世代の回収は**表キー順**に 1 回最大 100 件。`expires_at <= now` / `retain_until <= asOf` / `auth_epoch < current` は絞り込みの述語であって順序ではない」を置き、`identity_removal_receipts` / `sessions` / `auth_tokens` / `login_attempts` / `oauth_flow_states` / `account_deletion_manifests` の索引欄をすべてこの節へ参照させた。Round 006 の決着（`account_deletion_manifests` の DDL は動かさず spec の索引欄だけを揃える）が `:180` に反映済み。実装も一致 — `identitySupport.ts:89,138` は `ORDER BY ${spec.keyColumn}`、`accountDeletionManifestStore.ts:340,773` は `ORDER BY operation_id`。`pruneTerminal` の keyset は spec / 台帳 / テストケース（TC-identity-104）で `operationId` に統一され、`(expiresAt, operationId)` の旧表記は残っていない。
  - **scope 平面の outbox 掃引** — Round 006 の決着どおり「実装は動かさず canon を将来形へ倒す」で閉じている。`spec/database/index.md:1071` の索引正当化が「部分索引はどちらも両 plane に置く…`pruneProcessed` は駆動の affected-row count を読むため scope 平面の executor がそれを答えられるようになるまで global 平面に限られる」となり、`outboxRepository.ts` の class JSDoc（`applyCounted` の名指し、今日 scope 平面から呼ぶ配線が 0 件であること）と一致する。実装は `pruneProcessed` の先頭で `refuseStaged` を撃つ。
  - **`FOREIGN KEY` を張らない判断** — `spec/database/index.md:14` に「`ON DELETE CASCADE` / `RESTRICT` は論理的な所有関係の宣言であり、物理制約としての `FOREIGN KEY` 宣言を要求しない」を追記し、migration 冒頭のコメントが同じことを ADR 026 / 046 に紐づけて述べている。
  - そのほか `spec/domains/index.md` の manifest header `status` 5 値と `DistributedOperationStore.state` 3 値の切り分け、`spec/domains/workspace.md` の `preparing/running` → `running`、`spec/platform/index.md` の R2「1 回の delete 1,000 key」上限行の追加も、それぞれ migration の CHECK（`account_deletion_manifests.status` 5 値 / `distributed_operations.state` 3 値）と `r2.test.ts` の `spends the 1,000-key delete limit in chunks` に対応している。
  - `.thread/11/adr.md` への参照は**本番ソース・spec・docs から全廃されている**（`packages/`・`apps/`・`spec/`・`docs/`・`README.md`・`CLAUDE.md`・`.github/` を横断して `.thread` / `adr.md` の grep が 0 件、`ADR-0NN` 形式の grep ヒットも既存の `spec/adr/008` / `010` 参照のみ）。`Issue #11` / `#11` の残置も 0 件（`cleanup/participants.ts` と `personalCleanup.ts` の 2 か所が本 PR で外れた）。

##### 観点上の補足（指摘ではない）

- **適合ハーネスの名前空間分離は D1 / DO / R2 の 3 面すべてで効いている。** DO は `scopeObjectName(scope, namespace)` で名前ごと分ける（新しい名前＝新しい object なので掃き漏らしが原理的に起きない）、R2 は `objectKeyPrefix` で分ける、D1 だけは 1 つの DB を factory 呼び出しの先頭で全消しする。`harness.test.ts` の `hands out backends that cannot see one another on any plane` が 3 面を実物で観測し、D1 だけが共有＋消去である非対称も明示している。消し漏らしの側は `GLOBAL_TABLES_TO_WIPE` を `GLOBAL_TABLES` から**導出**したうえで `leaves no migrated table out of the wipe` が `sqlite_master` と突き合わせるので、migration に表を足して wipe に入れ忘れる経路が塞がっている。contentless FTS5 が `DELETE FROM` で空にできない点も `delete-all` コマンドで別扱いになっている。
- **`conformanceCoverage.test.ts` の任意メンバー検査は AC-2 / AC-3 の保証として十分。** `backend.ts` から `/^\s{2}([a-z][A-Za-z]*)\?\(/gm` で任意メンバーを導出すると今日は `seedMembershipEdges` 1 件で（`ConformanceBackendOptions` の `?:` プロパティは開き括弧が無いので混入しない）、`expect(optional).not.toEqual([])` が集合の消滅を、続くループが両ハーネスの提供を固定する。正規表現は `ScopedConformancePorts` を含む `backend.ts` 全体を走るので、任意メンバーが増えた場合も同じ網に掛かる。実物側の観測は `harness.test.ts` の `offers the optional membership-edge seed the suites need` が担い、テキスト検査と実オブジェクト検査の二段になっている。ADR 097（`.thread/11/adr.md`）が採らないと決めた「ケース数の絶対値固定」も持ち込まれていない。
- **ビルド / 設定。** 2 プロジェクトの境界が `vitest.shared.ts` の 1 文字列に集約され、node は `${CLOUDFLARE_ADAPTER_DIR_FROM_ROOT}/**` を除外、workers は `testFilesIn(CLOUDFLARE_ADAPTER_DIR)`（vitest の既定 include パターンから導出）で取り込む。拡張子盲点の穴が塞がれており、実測でも和が一致する（77 + 22 = 99）。tsconfig は 4 本で、CF ツリーと `di/cloudflare*.ts` が `tsconfig.cloudflare.json`（`lib: ESNext` + workers-types）へ、それ以外が `packages/core/tsconfig.json` へ排他的に振り分けられ、`packages/core` の `typecheck` script が両方を回す。root `tsconfig.json` の `include: ["*.ts"]` が `vitest.config.ts` / `vitest.shared.ts` を拾う。`wrangler.test.jsonc` は D1 / R2 / SQLite-backed DO の 3 バインディングと `nodejs_compat` を宣言し、`new_sqlite_classes` で KV backend ではないことを固定している。
- **CI。** テストが 2 ジョブに割れている（`lint-typecheck-unit`（10 分、lint / format / typecheck / `test:node`）と `unit-tests-workers`（20 分、`test:workers` のみ）、加えて既存の `build`）。分離理由がコメントで残っており、CF の typecheck は node ジョブの `pnpm typecheck` → `packages/core` の 2 本目の `tsgo` で拾われるので、どちらのジョブからも落ちていない。
- **セキュリティ / パフォーマンス（担当範囲）。** cursor の非認証化は ADR 063 の前提（可視性の述語が毎回掛かる）に依存しており、ポート JSDoc と `cursor.ts` の双方が「capability として扱わない」を呼び出し側にも課している。`shareTokenKeyRing` / `deletionTicketKeyRing` が `CloudflareRuntimeOptions` で**必須かつ既定なし**なのは、isolate ごとに鍵が生えると共有リンクと削除チケットが境界で壊れるという理由込みで書かれており、`oauth` と `mailSender` も同様に「黙って安全でない既定へ落ちない」形。`_occ_guard` は行を持たない CHECK 専用表で、`GLOBAL_TABLES_TO_WIPE` に含めても実害がない（そのことも JSDoc にある）。性能面では due index の publish が有界（優先度ごと 25 / 全体 100）、outbox `save` の JSON バインディングが 1,000,000 バイト上限、R2 の delete が 1,000 key チャンクと、いずれも `spec/platform/index.md` の実上限に紐づいている。

#### カバレッジ

変更ファイル一覧 187 行との対応（ディレクトリ単位でまとめた箇所あり）。

##### 確認

- ビルド / 設定: `.github/workflows/ci.yml`, `package.json`, `packages/core/package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `vitest.config.ts`, `vitest.shared.ts`, `packages/core/vitest.workers.config.ts`, `packages/core/wrangler.test.jsonc`, `packages/core/tsconfig.json`, `packages/core/tsconfig.cloudflare.json`
- ドキュメント: `README.md`, `docs/test.md`, `docs/runtime_node.md`
- spec（変更 21 ファイル全数）: `spec/adr/021-scope-sharded-data-plane.md`, `spec/adr/056-performance-budget-placement.md`, `spec/adr/063-public-cursor-not-authenticated.md`, `spec/adr/index.md`, `spec/database/index.md`, `spec/platform/index.md`, `spec/domains/{index,identity,note,workspace}.md`, `spec/usecases/{identity,note}.md`, `spec/testcases/identity/{deleteAccount,listPublicProfiles}.md`, `spec/testcases/note/{projectNoteChanges,searchPublicNotes}.md`, `spec/inventory/{adapter,domain,frontend,test,usecase}.md`
- DI 合成: `packages/core/src/application/di/runtime.ts`, `packages/core/src/application/di/cloudflareRuntime.ts`
- 物理スキーマ: `packages/core/src/adapters/cloudflare/d1/migrations/0001_global_schema.sql`, `packages/core/src/adapters/cloudflare/d1/schema.ts`, `packages/core/src/adapters/cloudflare/do/schema.ts`
- テストハーネス: `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`, `packages/core/src/adapters/cloudflare/__tests__/{conformanceBackend.ts,worker.ts,env.d.ts,harness.test.ts,runtimeComposition.test.ts,deleteFilesByOwner.test.ts}`, `.../__tests__/conformance/{directory,identity,projection,route,scopeBusiness,scopeInfra,unitOfWork}.test.ts`（7）, `.../__tests__/ports/{deps,directory,identity,projection,route,scopeBusiness,scopeInfra}.ts`（7）
- バックエンド固有テスト 12 ファイル（AC-4 の充足確認のためケース名一覧まで。個々の観測内容の妥当性は uow / scope / identity / routing 観点の担当）: `.../__tests__/{alarm,durability,globalConcurrency,idempotency,lease,projectionConcurrency,r2,routeGuard,searchEdges,sessionOverlay,support,unitOfWork}.test.ts`
- アダプター実装のうち、本観点の論点に触れる部分だけを確認: `packages/core/src/adapters/cloudflare/cursor.ts`（ADR 063 の実装）, `.../d1/repositories/outboxRepository.ts`（Round 006 の canon 追随と `refuseStaged`）, `.../d1/repositories/{accountDeletionManifestStore,identitySupport}.ts`（「有界な掃引 / 削除」の `ORDER BY`）, `.../do/scopeObject.ts`（`SCOPE_TASK_LEASE_MS` の注入点と `ScopeObjectEnv`）
- 契約の追随（AC-8 の手続き確認のみ。振る舞いは identity / uow 観点の担当）: `packages/core/src/application/errors.ts`, `packages/core/src/application/cleanup/{participants,personalCleanup}.ts`, `packages/core/src/application/ports/{noteRouteFanOutReader,scopeTaskScheduler}.ts`, `packages/core/src/domain/identity/ports/authTokenRepository.ts`, `packages/core/src/domain/note/ports/{localNoteProjectionWriter,publicNoteProjectionWriter,publicNoteQueryService}.ts`
- `.thread/11/`: `plan.md`, `progress.md`, `testing.md`, `review/triage-keys.md`, `steps.md`（冒頭のポート総数表と束 11 / 12 / 13 のみ通読し、plan.md の受け入れ基準との対応を確認）, `adr.md`（対象を絞った確認 — 本番ソース / spec / docs からの参照が全廃されていること、AC-5 / AC-6 の決着の受け皿として存在すること。282KB の全文は未読）

##### スキップ

- `.thread/11/review/review-00{1..6}{,-composition,-identity,-routing,-scope,-uow}.md`（36 ファイル）, `.thread/11/review/triage.md` — 前ラウンドの記録。ゼロベースでレビューする指示のため参照せず、既出判定は `triage-keys.md` だけを使った
- `.thread/11/foundation.md` — 計画フェーズの入力で、最終状態との一致を求められる成果物ではない（Phase 4 が読むのは `testing.md` / `progress.md`）
- `packages/core/src/adapters/cloudflare/d1/repositories/` の残り 16 ファイル（`authTokenRepository`, `distributedOperationStore`, `globalMaintenanceRunStore`, `idempotencyStore`, `identityRemovalReceiptStore`, `identityRepository`, `identityUniqueDirectory`, `loginAttemptStore`, `noteRouteFanOutReader`, `noteRouteStore`, `oauthStateStore`, `publicNoteProjection`, `publicNoteQueryService`, `sessionRepository`, `userBatchReader`, `userRepository`） — SQL とポート契約の突き合わせは identity / routing / uow 観点の担当
- `packages/core/src/adapters/cloudflare/do/` の `{alarm,dueIndex,scheduledTasks,scopeName,scopeStub}.ts` と `do/repositories/` の 10 ファイル — scope 平面の振る舞いは scope 観点の担当
- `packages/core/src/adapters/cloudflare/execution/`（4）, `sql/`（7） — 二平面 UoW と write-set / セッションの機構は uow 観点の担当
- `packages/core/src/adapters/cloudflare/projection/`（4）, `search/`（2）, `r2/objectStorage.ts`, `scopeRouter.ts`, `scopeTaskQueue.ts` — 投影 / FTS / R2 / routing は routing・scope 観点の担当
- `packages/core/src/application/identity/{requestPasswordReset,resendVerificationEmail}.ts`, `packages/core/src/application/workers/scopeTaskRunner.ts`, `packages/core/src/application/storage/__tests__/deleteFilesByOwner.test.ts`, `packages/core/src/application/workers/__tests__/scopeTaskRunner.test.ts` — ユースケース / ワーカーの振る舞いは identity / scope 観点の担当（node プロジェクトのケース数増分 +2 の出どころとしてのみ確認した）
