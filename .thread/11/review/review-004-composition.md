### 合成・スキーマ・テストハーネス・spec/docs

#### Blockers

なし。

検証した受け入れ基準:

- **AC-7** — `git diff origin/main...HEAD -- packages/core/src/adapters/memory apps/web` は空。vitest の 2 プロジェクト境界は `vitest.shared.ts` の 1 文字列（`packages/core/src/adapters/cloudflare`）だけで決まり、`node` の `exclude` が `${CLOUDFLARE_ADAPTER_DIR_FROM_ROOT}/**`、`workers` の `include` が `testFilesIn(CLOUDFLARE_ADAPTER_DIR)`（vitest 既定の拡張子パターン由来）で、disjoint かつ和集合が全テストファイル。拡張子違いで両方から落ちる穴も塞がっている。
- **AC-2 / AC-3** — CF 側 `__tests__/conformance/` の 7 ファイルが呼ぶスイートは directory 4 + identity 8 + projection 4 + route 5 + scopeBusiness 6 + scopeInfra 2 + unitOfWork 1 = 30 で、`conformanceCoverage.test.ts` の `PERSISTENCE_SUITES = 30` と一致。全件が実バインディング（`wrangler.test.jsonc` の D1 / SQLite-backed DO / R2）に対して走り、`harness.test.ts` が「バインディングが実在すること」「両平面のスキーマが当たること」「FTS5 / `json_each` が両平面で使えること」「`nodejs_compat` が `node:async_hooks` を供給すること」を独立に固定している。
- **AC-3 の新ケース**（`conformanceCoverage.test.ts:90` "hands each backend's suites that backend's own factory"）は、`make[A-Za-z]*ConformanceBackend` の識別子集合が各バックエンドのテストファイル群でちょうど 1 つであることを要求する。他方の factory を import すれば集合が 2 要素になって落ち、factory を一切名乗らない自前バックエンドを渡せば空集合で落ちる。残る穴は「正しい factory を import しつつ実引数には別物を渡す」1 通りだけで、CF 側 7 ファイルはいずれも 25 行前後の呼び出し列なので実効的な保証として十分と判断した（Round 003 で決着済みの論点でもある）。
- **名前空間分離（D1 / DO / R2 の 3 面）** — DO は `scopeObjectName` の接頭辞で毎 factory 呼び出しごとに別 object、R2 は `objectKeyPrefix`、D1 は factory 冒頭の `GLOBAL_WIPE_STATEMENTS`（`GLOBAL_TABLES` からの導出 + contentless FTS5 の `delete-all`）。3 面すべてを `harness.test.ts:138` が 1 ケースで観測しており、`d1/schema.ts` の導出と `harness.test.ts:62`（migrate した表がすべて wipe 対象に入っている逆方向）が二重管理を塞いでいる。`scope_task_due_index` だけが名前空間を持たない例外だが、これは `dueIndex.ts` の JSDoc に制約として明記され（Round 001 の同指摘の決着どおり）、実際に UoW 経由で scope task を触るテスト（`conformance/unitOfWork.test.ts` / `runtimeComposition.test.ts`）はファイルが分かれ、後者は scope id を `di-scope-${seq}` で毎回変えている。
- **AC-9 / 物理スキーマ** — `0001_global_schema.sql` の全表・全 CHECK・全索引を `spec/database/index.md` の列表と突き合わせた。`identity_unique_reservations.user_version`、`membership_directory`（`membership_id` の相関 CHECK と `(user_id, operation_id)` 索引）、`note_routes`（`migration_id` / `last_migration_id`、`state` を含めない 2 本の索引）、`distributed_operations`（`kind` 起点の 4 索引、`request_key` NOT NULL、`terminal_at`）、`account_deletion_manifests` / `_items`、`global_maintenance_runs` / `_lanes`、`_occ_guard`、`scope_task_due_index`、`outbox_events` の 2 本の部分索引、`processed_events`、`public_note_search*` の 4 索引 — いずれも spec と 1 対 1。`do/schema.ts` も `notes` / `note_search*` / `scheduled_tasks` の 3 本の部分索引 / `applied_operations` / 新設 `_scope_identity` まで spec と一致。
- **manifest header の状態語彙 5 値** — `application/ports/accountDeletionManifestStore.ts:55`（型）、`d1/repositories/accountDeletionManifestStore.ts` の遷移全体、DDL の `CHECK IN ('building','built','rollingBack','completed','rejected')`、`spec/domains/index.md:120`、`spec/database/index.md`（`account_deletion_manifests` と `distributed_operations` 節の相互参照）、`spec/usecases/identity.md`、`spec/testcases/identity/deleteAccount.md`、`spec/inventory/test.md` が全一致。`preparing` / `committing` / `compacting*` / `buildingMemberships` / `buildingAuthorRoutes` は spec 全体から消えている（`compacting` の残存 2 件は `workspace_deletion_manifests` / `job_removal_manifests` の別状態機械で、正しい）。`expiresAt` → `retainUntil` の列名追随も完了。
- **ADR 021 と ADR 063 の適用範囲** — 021:41 は「公開検索と Note route 二次キー走査（`PublicNoteQueryService` の 3 メソッドと `NoteRouteFanOutReader` の 2 メソッド）は認証しない／公開 workspace 一覧は署名 cursor のまま」と範囲を明示し、063 の「範囲外として残るもの」と一致する。spec に残る「署名 cursor」は `spec/database/index.md:990`（workspace_directory）と `spec/platform/index.md:110`（公開 workspace 一覧）だけで、どちらも carve-out の対象。ポート JSDoc・`application/errors.ts`・inventory 3 種・testcases も `tampered` → `unreadable` へ揃っている。本番ソースに「署名 cursor」の語は残っていない。
- **migration / CI** — migration は `0001` 1 本。`wrangler d1 migrations apply` が `d1_migrations` で適用済みを追うので本番の再実行は no-op、テスト側は `migrateOnce()` がファイルごと 1 回だけ当てる。CI は node ジョブ（lint / format / typecheck / `test:node`、typecheck は `tsgo -p tsconfig.cloudflare.json` も含む）と workers ジョブ（`test:workers`、20 分）に分かれ、`allowBuilds: workerd: true` も入っている。
- **`.thread` 参照** — `grep -rn "\.thread" packages apps spec docs README.md` は 0 件。置き換えられた記述（migration ヘッダーの FK 不在の理由、`dueIndex.ts` の drift 非対称、`spec/platform/index.md` の単一 writer 前提と run lease fencing）はいずれも「なぜこの形か」を述べる設計文で、指摘への弁明や修正の経緯にはなっていない。
- **AC-5** — `deleteFilesByOwner.test.ts:325` が n=10 で reads 22 / commitStatements 21 / 計 43 を実測固定し、`spec/platform/index.md:153` の「1 turn `4n + 3` 文（読み `2n + 2` ＋ commit 内 `2n + 1`）、commit は件数によらず 1 回」と一致する。

#### Warnings

- **[W-001]** 投影 writer 2 ポートのエラー契約を JSDoc 側だけ広げ、spec の「エラーケース」行を追随させていない
  - 場所: `packages/core/src/domain/note/ports/publicNoteProjectionWriter.ts:19`、`packages/core/src/domain/note/ports/localNoteProjectionWriter.ts:80` ↔ `spec/domains/note.md:620`
  - 理由: 本 PR は両ポートの JSDoc に「`replaceSnapshotIfNewer` / `remove` / `redactAuthor` は `ConflictError("OPTIMISTIC_LOCK_FAILURE")` を投げうる」を追記した（triage の `publicNoteProjection:replace/remove の occGuard` 修正に伴うもの）。一方、この 2 ポートを定義する `spec/domains/note.md` の `### LocalNoteProjectionWriter / PublicNoteProjectionWriter` 節の末尾は **`**エラーケース**: `SystemError(DatabaseError)`` のまま**である。同じ PR が `spec/domains/identity.md:482` の `findPendingByUserAndPurpose` は JSDoc 変更に合わせて書き換えているので、追随の基準が一貫していない。他ドメインの同種の節（`spec/domains/usage.md:158` / `storage.md:262` / `note.md:423` など）はいずれもエラーケース行に `ConflictError("OPTIMISTIC_LOCK_FAILURE")` を並べており、この節だけが例外になる。適合スイートを動かさない判断（triage 406 行・419 行）は妥当だが、それは「スイートを変えない」であって「canon を変えない」ではない。
  - 提案: `spec/domains/note.md:620` を `**エラーケース**: ConflictError("OPTIMISTIC_LOCK_FAILURE")（読みと適用のあいだに行が動いた場合。再配送が読み直して収束する）、SystemError(DatabaseError)` にする。`ScopeTaskScheduler.claimDue` に足した同種の追記は、spec 側に対応する節が無いので追随先が無い（W-002 も参照）。

- **[W-002]** `spec/platform/index.md` が `spec/domains/index.md` の存在しない節を参照している
  - 場所: `spec/platform/index.md:197`（本 PR で追加された「1 つの scope に対する同時 writer は…」の段落）
  - 理由: 「settle（`complete` / `backoff` / `schedule`）は行キー `(kind, operationId)` だけで撃たれ、claim を同定するトークンを持たない（[domains/index.md](../domains/index.md) の `ScopeTaskScheduler`）」とあるが、`spec/domains/index.md` に `ScopeTaskScheduler` の語は 1 度も現れない（`grep -n "ScopeTaskScheduler" spec/domains/index.md` が 0 件）。同ファイルの見出しは `ScopeKey と永続化境界` / `新設する横断的ポート`（MailSender / TimeZoneResolver / OAuthStateStore / IdempotencyStore）/ `継続要求` で、`ScopeTaskScheduler` の契約は `scheduled_tasks` の運用として `spec/database/index.md` 側にしかない。AC-6 の決着（fencing 不要の根拠）を canon に残すのがこの段落の役目なので、その根拠の参照先が空振りしているのは効き目が落ちる。`spec/platform/index.md:220` の run lease fencing 段落も同じ参照を経由して対比している。
  - 提案: 参照先を `spec/database/index.md` の `scheduled_tasks` / `scope_task_due_index` 節へ差し替えるか、`spec/domains/index.md` の「新設する横断的ポート」に `ScopeTaskScheduler` / `ScopeTaskQueue` の節を起こして settle の鍵とエラー契約（W-001 で触れた `claimDue` の `OPTIMISTIC_LOCK_FAILURE` を含む）をそこに置く。後者なら `spec/inventory/adapter.md` の ADP 行欠落（#52）の受け皿にもなる。

- **[W-003]** `FOREIGN KEY` を置かない決定が canon に着地しておらず、`.thread/11/adr.md` にしか無い
  - 場所: `packages/core/src/adapters/cloudflare/d1/migrations/0001_global_schema.sql:11-18`、`packages/core/src/adapters/cloudflare/do/schema.ts:39-40` ↔ `spec/database/index.md:14`（共通の規約「外部キー」）、`:294` / `:317` / `:332`（`identities` / `sessions` / `auth_tokens` の `FK → users.id ON DELETE CASCADE`）、`:572` / `:582` / `:618` / `:1184`
  - 理由: 共通の規約は「同じ database / domain の親子は原則 `ON DELETE CASCADE`。…Workspace→Membership/Invitation と Job parent→children は `RESTRICT`」と**物理制約の指示として**書かれ、各表の「制約」列にも `FK → …` が入っている。両バックエンドの DDL はこれを意図的に 1 本も置かない。理由は `.thread/11/adr.md` ADR-006 に書かれており AC-9 の「反映しない差分は adr.md に理由とともに残っている」は満たすが、`.thread/` は canon ではなく、CLAUDE.md の「spec に書いてあることはコードについて真であるはず」に照らすと、次のバックエンド実装者（そして #3〜#10 のスライス）はこの規約を物理制約の指示として読む。本 PR は同じ「共通の規約」リストの隣接項目（scope 検証）を実装に合わせて書き換えているので、ここだけ手を付けていないのは非対称でもある。
  - 提案: `spec/database/index.md:14` に 1 文足して「`ON DELETE CASCADE` / `RESTRICT` の記述は論理的な所有関係の宣言であり、物理制約の宣言を要求しない。親子の後始末はドメインイベントの購読者が行う」と明記する（ADR-006 の決定文をそのまま canon 語に落とすだけ）。列の「制約」列はそのままでよい。ADR に昇格させる価値があるなら `spec/adr/064` としてもよいが、規約 1 文で足りる。

- **[W-004]** スライスが着地したのに `#11` を未来形で指す参照が本番ソースと `docs/` に残っている
  - 場所: `docs/runtime_node.md:5`（「The final target … arrives as Issue #11 and swaps only the adapter + entry layers」）、同 `:94`（表見出し `Cloudflare (Issue #11)`）、同 `:102`、`packages/core/src/application/cleanup/personalCleanup.ts:92`、`packages/core/src/application/cleanup/participants.ts:54`
  - 理由: README は本 PR で「adapter group と DI wiring は in place、残るのは entry point と deployment configuration」へ正確に書き換えられたが、`docs/runtime_node.md` は据え置きで「adapter + entry layers をこれから足す」と読める。`personalCleanup.ts:92` の「the full-page branch … is carried untested until a backend that can hold several exists — #11」は、CF の `ScopeCleanupAdmissionStore`（`applied_operations` を `operation_id` PK で持ち、1 scope に複数の terminal barrier receipt を保持できる）が到着した時点で前提条件が満たされており、待っていた条件が本 PR で成立したのにコメントは待ち続けている。`participants.ts:54` の `"#11 / the slice adding a scope outbox read side"` も、#11 が scope outbox の読み側を足さずに閉じたため誤誘導になる。
  - 提案: `docs/runtime_node.md:5` を README と同文の現在形へ（残るのは entry point と配備設定）。`personalCleanup.ts:92` は「#11」を外し、複数 receipt を保持できるバックエンドが実在する以上、観測を足すなら適合スイート側（ADR 046 の手続き）だと理由まで書くか、別 Issue 番号を指す。`participants.ts:54` の `#11` は「the slice adding a scope outbox read side」だけに縮める。

- **[W-005]** 5 つの台帳の「最終同期」日付が更新されていない
  - 場所: `spec/inventory/adapter.md:4`、`domain.md:4`、`test.md:4`、`usecase.md:4`（いずれも `最終同期: 2026-08-25`）、`frontend.md:4`（`2026-08-16`）
  - 理由: 本 PR は生成元（`spec/domains/` / `spec/testcases/` / `spec/usecases/`）と台帳の行を同じコミット群で書き換えているので、台帳は今日時点で同期済みである。日付だけが 1 日前を指すと、この欄が持つ唯一の役目（どこまで突き合わせたか）が嘘になる。
  - 提案: 行を触った 4 ファイル（`frontend.md` は生成元 `spec/pages/` が未変更なので任意）の日付を `2026-08-26` にする。

#### カバレッジ

**確認**

- 設定 / ビルド: `.github/workflows/ci.yml`, `package.json`, `packages/core/package.json`, `pnpm-workspace.yaml`, `vitest.config.ts`, `vitest.shared.ts`, `packages/core/vitest.workers.config.ts`, `packages/core/wrangler.test.jsonc`, `packages/core/tsconfig.json`, `packages/core/tsconfig.cloudflare.json`（差分外の `tsconfig.json`（root）, `packages/core/src/application/di/types.ts`, `packages/core/src/application/di/memoryRuntime.ts`, `packages/core/src/adapters/memory/store.ts` も照合のため参照）
- DI 合成: `packages/core/src/application/di/runtime.ts`, `packages/core/src/application/di/cloudflareRuntime.ts`
- 物理スキーマ: `packages/core/src/adapters/cloudflare/d1/migrations/0001_global_schema.sql`, `packages/core/src/adapters/cloudflare/d1/schema.ts`, `packages/core/src/adapters/cloudflare/do/schema.ts`（関連して `do/dueIndex.ts`, `do/scopeName.ts`, `do/scopeObject.ts` の冒頭、`r2/objectStorage.ts` の delete 上限）
- テストハーネス: `packages/core/src/adapters/__tests__/conformanceCoverage.test.ts`, `cloudflare/__tests__/conformanceBackend.ts`, `cloudflare/__tests__/worker.ts`, `cloudflare/__tests__/env.d.ts`, `cloudflare/__tests__/harness.test.ts`, `cloudflare/__tests__/runtimeComposition.test.ts`, `cloudflare/__tests__/deleteFilesByOwner.test.ts`（AC-5 の計数のみ）, `cloudflare/__tests__/ports/{deps,directory,identity,projection,route,scopeBusiness,scopeInfra}.ts`, `cloudflare/__tests__/conformance/{directory,identity,projection,route,scopeBusiness,scopeInfra,unitOfWork}.test.ts`（差分外の `adapters/conformance/scopeTaskScheduler.ts` も名前空間分離の検討で参照）
- 契約 / ユースケース側の差分: `packages/core/src/application/errors.ts`, `application/identity/requestPasswordReset.ts`, `application/identity/resendVerificationEmail.ts`, `application/ports/noteRouteFanOutReader.ts`, `application/ports/scopeTaskScheduler.ts`, `application/workers/scopeTaskRunner.ts`, `application/workers/__tests__/scopeTaskRunner.test.ts`, `application/storage/__tests__/deleteFilesByOwner.test.ts`, `domain/identity/ports/authTokenRepository.ts`, `domain/note/ports/{localNoteProjectionWriter,localNoteQueryService,publicNoteProjectionWriter,publicNoteQueryService}.ts`
- ドキュメント: `README.md`, `docs/test.md`（差分外の `CLAUDE.md`, `docs/runtime_node.md` も照合のため参照）
- spec 全 20 ファイル: `spec/adr/021-scope-sharded-data-plane.md`, `spec/adr/063-public-cursor-not-authenticated.md`, `spec/adr/index.md`, `spec/database/index.md`, `spec/domains/{identity,index,note,workspace}.md`, `spec/inventory/{adapter,domain,frontend,test,usecase}.md`, `spec/platform/index.md`, `spec/testcases/identity/{deleteAccount,listPublicProfiles}.md`, `spec/testcases/note/{projectNoteChanges,searchPublicNotes}.md`, `spec/usecases/{identity,note}.md`
- `.thread/11/`: `plan.md`, `review/triage-keys.md`, `review/triage.md`（既出判定の照合）, `adr.md`（ADR-006 の FK 判断のみ）

**スキップ**

- `packages/core/src/adapters/cloudflare/{cursor.ts,scopeRouter.ts,scopeTaskQueue.ts}`, `d1/repositories/*.ts`（19 本）, `do/alarm.ts`, `do/scheduledTasks.ts`, `do/scopeStub.ts`, `do/repositories/*.ts`（11 本）, `execution/*.ts`（4 本）, `projection/*.ts`（4 本）, `r2/objectStorage.ts`（delete 上限以外）, `search/*.ts`（2 本）, `sql/*.ts`（7 本） — ポート実装本体で、identity / routing / scope / uow 各観点の担当
- `packages/core/src/adapters/cloudflare/__tests__/{alarm,durability,globalConcurrency,idempotency,lease,projectionConcurrency,r2,routeGuard,searchEdges,sessionOverlay,support,unitOfWork}.test.ts` — バックエンド固有の振る舞い（AC-4）を観測するテストで、対応するポート実装と同じ観点の担当
- `pnpm-lock.yaml` — 生成物。`package.json` 側の宣言（`@cloudflare/vitest-plugin` 1.0.0 / `@cloudflare/workers-types` / `wrangler` ^4.125.0）で足りる
- `.thread/11/{foundation,progress,steps,testing}.md`, `.thread/11/review/review-00{1,2,3}*.md`（16 本）, `.thread/11/adr.md`（ADR-006 以外） — 過去ラウンドの作業記録。ゼロベースの再レビューでは前提にしない方針のため、既出判定に必要な `triage.md` / `triage-keys.md` と AC-9 の逃げ道を確認する `adr.md` の該当節だけを読んだ
